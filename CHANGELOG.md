# Changelog

## 2026-09-10

- QA уровня 11 «Что выше?» (`src/data/level11.ts`, шестой и последний в
  пачке этого прогона: уровни 6-11, размер пачки выпал случайно — 6).
  Багов не найдено: все 25 карточек уникальны по `id`, `value` и
  `funFact`, у всех 25 непустых `imageUri` файлы реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200 — пустых
  `imageUri` в этом уровне нет. Высоты правдоподобны (в т.ч. смешение
  зданий, статуй, гор и вулкана Марса для контраста масштабов —
  намеренный приём уровня), `comparisonType: 'max'` корректен (выше =
  больше метров). Playwright недоступен — уровень пройден
  фолбэк-способом: сборка + preview, `curl -sSI` по всем 25 картинкам
  (200 у каждой) и временный Node-скрипт `scratchpad/sim-level.mjs` — все
  10 раундов без исключений. `lastReviewedLevelId` теперь `11`. Этим
  уровнем завершена пачка из 6 уровней (6-11) за сегодня.

- QA уровня 10 «Что калорийнее?» (`src/data/level10.ts`, пятый в пачке
  этого прогона: уровни 6-11). Багов не найдено: все 21 карточка
  уникальны по `id`, `value` и `funFact`, 20 непустых `imageUri` реально
  лежат в `public/images/` и отданы preview-сервером с кодом 200; для
  единственной карточки без картинки (куриная грудка) подходящего
  неиспользуемого файла в `public/images/` не нашлось — оставлен честный
  эмодзи-фолбэк. Значения ккал на 100 г правдоподобны, `comparisonType:
  'max'` корректен (калорийнее = больше ккал). Playwright недоступен —
  уровень пройден фолбэк-способом: сборка + preview, `curl -sSI` по всем
  20 картинкам (200 у каждой) и временный Node-скрипт
  `scratchpad/sim-level.mjs` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `10`.

- QA уровня 9 «Что громче?» (`src/data/level9.ts`, четвёртый в пачке
  этого прогона: уровни 6-11). Багов не найдено: все 19 карточек
  уникальны по `id`, `value` и `funFact`, 3 непустых `imageUri` реально
  лежат в `public/images/` и отданы preview-сервером с кодом 200; для 16
  карточек без картинки (шепот, холодильник, разговор, пылесос,
  газонокосилка, рок-концерт, сирена, петарда, старт ракеты, шелест
  листьев, городской трафик, мотоцикл, бензопила, выстрел из пистолета,
  Тунгусский метеорит, столкновение черных дыр) подходящего
  неиспользуемого файла в `public/images/` не нашлось — оставлен честный
  эмодзи-фолбэк. Значения дБ и факты правдоподобны; экстремальные
  карточки «Тунгусский метеорит» (300 дБ) и «Столкновение черных дыр»
  (Неизмеримо, value 500) — намеренные шуточные карточки вне шкалы, как
  и аналогичные приёмы в других уровнях (например, «Бессмертная медуза» в
  8-м), не баг. `comparisonType: 'max'` корректен (громче = больше дБ).
  Playwright недоступен — уровень пройден фолбэк-способом: сборка +
  preview, `curl -sSI` по всем 3 картинкам (200 у каждой) и временный
  Node-скрипт `scratchpad/sim-level.mjs` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `9`.

- QA уровня 8 «Кто живет дольше?» (`src/data/level8.ts`, третий в пачке
  этого прогона: уровни 6-11). Багов не найдено: все 19 карточек
  уникальны по `id`, `value` и `funFact`, 9 непустых `imageUri` реально
  лежат в `public/images/` и отданы preview-сервером с кодом 200; для 10
  карточек без картинки (муха-поденка, беркут, галапагосская черепаха,
  кролик, шимпанзе, попугай ара, карп кои, гренландский кит,
  океанический куахог, остистая сосна) подходящего неиспользуемого файла
  в `public/images/` не нашлось (близкие по теме файлы — `falcon_...jpg`
  для беркута, `gorilla_...jpg` для шимпанзе, `blue_whale_...jpg` для
  гренландского кита — визуально принадлежат другим видам, подставлять
  не стал) — оставлен честный эмодзи-фолбэк. Значения продолжительности
  жизни и факты правдоподобны (в т.ч. про моллюска Мин — 507 лет,
  реальный рекордсмен), `comparisonType: 'max'` корректен (живёт дольше =
  больше лет). Playwright недоступен — уровень пройден фолбэк-способом:
  сборка + preview, `curl -sSI` по всем 9 картинкам (200 у каждой) и
  временный Node-скрипт `scratchpad/sim-level.mjs` — все 10 раундов без
  исключений. `lastReviewedLevelId` теперь `8`.

- QA уровня 7 «Что дороже?» (`src/data/level7.ts`, второй в пачке этого
  прогона: уровни 6-11). Багов не найдено: все 20 карточек уникальны по
  `id`, `value` и `funFact`, 9 непустых `imageUri` реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200; для 11 карточек
  без картинки (кофе, ноутбук, часы Rolex, слиток золота, частный
  самолет, бутылка воды, билет в кино, видеоигра, новый автомобиль, дом в
  США, истребитель F-35) подходящего неиспользуемого файла в
  `public/images/` не нашлось (единственные формально близкие по теме
  файлы — старые компьютеры `computer_...jpg`/`pc_...jpg` и
  `passenger_jet_...jpg` — визуально не соответствуют современному
  игровому ноутбуку и частному/военному самолету, подставлять не стал) —
  оставлен честный эмодзи-фолбэк. Цены правдоподобны, `comparisonType:
  'max'` корректен (дороже = больше $). Playwright недоступен — уровень
  пройден фолбэк-способом: сборка + preview, `curl -sSI` по всем 9
  картинкам (200 у каждой) и временный Node-скрипт
  `scratchpad/sim-level.mjs` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `7`.

- QA уровня 6 «Где глубже?» (`src/data/level6.ts`, первый в пачке этого
  прогона: размер пачки выпал случайно — 6, уровни 6-11). Багов не
  найдено: все 17 карточек уникальны по `id`, `value` и `funFact`, 10
  непустых `imageUri` реально лежат в `public/images/` и отданы
  preview-сервером с кодом 200; для 7 карточек без картинки (озеро
  Байкал, рекорд дайвинга, бассейн, пещера Крубера, станция метро,
  морской слон, клюворыл Кювье) подходящего неиспользуемого файла в
  `public/images/` не нашлось — оставлен честный эмодзи-фолбэк (единственный
  формально похожий файл, `dolphin_...jpg`, уже занят другим уровнем и
  визуально не соответствует клюворылу — подставлять не стал).
  `comparisonType: 'max'` корректен (глубже = больше метров). Playwright
  недоступен (пакет отсутствует в `devDependencies`, установка без
  интернета не гарантирована) — уровень пройден фолбэк-способом: сборка +
  preview, `curl -sSI` по всем 10 картинкам (200 у каждой) и временный
  Node-скрипт `scratchpad/sim-level.mjs` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `6`.

## 2026-09-09

- QA уровня 5 «Что горячее?» (`src/data/level5.ts`, четвёртый и последний в
  пачке этого прогона: 2-5, размер пачки в этом прогоне выпал случайно —
  4). Найден и исправлен неточный `funFact`: карточка «Поверхность
  Солнца» (5 500 °C) описывала температуру **ядра** Солнца («15
  миллионов градусов») — чужой факт, к тому же дублирующий тему отдельной
  карточки «Ядро Солнца» (`sun_core`, 15 000 000 °C) того же уровня.
  Заменён на факт именно о поверхности (фотосфере) — что это видимый нами
  слой звезды и самый холодный из её слоёв. Все 17 карточек уникальны по
  `id`, `value` и `funFact`. 9 непустых `imageUri` реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200; для 8 карточек
  без картинки (поверхность Солнца, лава, кипящая вода, жидкий азот,
  печь для пиццы, вольфрамовая нить, ядро Солнца, плавление железа)
  подходящего неиспользуемого файла в `public/images/` не нашлось —
  оставлен честный эмодзи-фолбэк. `comparisonType: 'max'` корректен
  (горячее = больше °C). Playwright недоступен — уровень пройден
  фолбэк-способом: сборка + preview, `curl -sSI` по всем 9 картинкам (200
  у каждой) и временный Node-скрипт `scratchpad/sim-level.mjs` — все 10
  раундов без исключений. `lastReviewedLevelId` теперь `5`. Этим уровнем
  завершена пачка из 4 уровней (2-5) за сегодня.

- QA уровня 4 «Что больше?» (`src/data/level4.ts`, третий в пачке этого
  прогона: 2-5). Багов не найдено: все 21 карточка уникальны по `id`,
  `value` и `funFact`, 15 непустых `imageUri` реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200; для 6 карточек
  без картинки (теннисный корт, дорожка боулинга, олимпийский бассейн,
  вагон поезда, авианосец, подводная лодка) подходящего неиспользуемого
  файла в `public/images/` не нашлось — оставлен честный эмодзи-фолбэк.
  Значения длины/высоты в метрах правдоподобны (Великая Китайская стена
  специально хранит суммарную длину в метрах — 21 196 000 м — это
  реальная справочная цифра с учётом всех ответвлений, не опечатка).
  `comparisonType: 'max'` корректен. Playwright недоступен — уровень
  пройден фолбэк-способом: сборка + preview, `curl -sSI` по всем 15
  картинкам (200 у каждой) и временный Node-скрипт
  `scratchpad/sim-level.mjs` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `4`.

- QA уровня 3 «Что древнее?» (`src/data/level3.ts`, второй в пачке этого
  прогона: 2-5). Найдены и исправлены 2 пары карточек с совпадающим
  `value` (пары с равным `value` не участвуют в сравнениях — см.
  `buildPairQueue`): «Интернет (ARPANET)» и «Высадка на Луну» стояли на
  одном 1969 годе, «Первый кинофильм» и «Радио» — на 1895. Вместо
  выдумки значения уточнены до реальных дат тех же событий: высадка на
  Луну — 20 июля 1969 (0.55 доли года), сообщение ARPANET — 29 октября
  1969 (0.83) — Луна раньше по факту, что и сохраняет верный порядок для
  `comparisonType: 'min'`; демонстрации радиосвязи Маркони — лето 1895
  (0.66), киносеанс братьев Люмьер — 28 декабря 1895 (0.99). `value`
  теперь дробный только для этих 4 карточек, `displayValue` не менялся
  (по-прежнему показывает только год), `funFact` у них дополнен точной
  датой. Все 24 карточки уникальны по `id`, `value` и `funFact`
  (проверено Node-скриптом). Непустые `imageUri` (19 из 24) реально лежат
  в `public/images/` и отданы preview-сервером с кодом 200; для 5 карточек
  без картинки (печатный станок, кинофильм, компас, телескоп, паровой
  двигатель) в `public/images/` не нашлось подходящего неиспользуемого
  файла — оставлен честный эмодзи-фолбэк. `comparisonType: 'min'`
  корректен («древнее» = меньшее/более отрицательное значение года).
  Playwright недоступен — уровень пройден фолбэк-способом: сборка +
  preview, `curl -sSI` по всем 19 картинкам (200 у каждой) и временный
  Node-скрипт `scratchpad/sim-level.mjs`, повторяющий
  `buildPairQueue`/`generateNewPair` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `3`.

- QA уровня 2 «Кто быстрее?» (`src/data/speeds.ts`, первый в пачке этого
  прогона из 4 уровней: 2-5, размер пачки в этом прогоне выбран случайно —
  выпало 4). Багов не найдено: все 20 `imageUri` реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200 (проверено
  `curl -sSI` по каждому пути), эмодзи-фолбэк на уровне не требуется.
  Значения максимальной скорости (км/ч) правдоподобны и соответствуют
  справочным данным, `funFact` у каждой карточки не пустой и без дублей
  между карточками. Совпадающие `value` у части пар (72, 65, 60 км/ч у
  разных животных) — реальные справочные цифры, а не баг: пары с равным
  `value` в игру всё равно не попадают (см. `buildPairQueue`), а из 20
  карточек остаётся более 180 уникальных пар для сравнения, чего с запасом
  хватает на 10 раундов. `comparisonType: 'max'` в `levels.ts` корректен
  (выше скорость — вернее ответ). Playwright/Chromium в окружении не
  установлен (нет `@playwright/test` в зависимостях и сети для его
  установки нет) — уровень пройден фолбэк-способом: `npm run build && npm
  run preview -- --port 4321 --strictPort`, затем `curl -sSI` по всем 20
  картинкам (200 у каждой) и временный Node-скрипт
  (`scratchpad/sim-level.mjs`, в репозиторий не коммитился), повторяющий
  логику `buildPairQueue`/`generateNewPair` из `GameLevel.tsx` — все 10
  раундов построены без исключений, ни одна пара с одинаковым `value` в
  игру не попала. `lastReviewedLevelId` теперь `2`.

## 2026-09-08

- QA уровня 1 «Кто тяжелее?» (`src/data/level1.ts`, последний в пачке
  этого прогона — 10 уровней: 7-15, 1, циклически). Найдена и исправлена
  1 пара карточек с совпадающим значением массы (пара с равным `value` не
  участвует в сравнениях — см. `buildPairQueue`): «Медведь Гризли» и
  «Зебра» обе стояли на 350 кг — вес гризли уточнён до 300 кг (типичный
  средний вес взрослого гризли по справочным данным лежит в широком
  диапазоне 180–360 кг, 300 — реалистичное среднее, отличное от веса
  зебры). Теперь все 30 карточек (20 из `LEVEL1_DATA` + 10 из
  `LEVEL1_ADDITIONAL`) уникальны по `id`, `value` и `funFact` (проверено
  скриптом `tsx` — 30/30, дублей после правки не осталось). Все 30
  `imageUri` реально лежат в `public/images/` и отданы preview-сервером с
  кодом 200 — картинок, требующих эмодзи-фолбэка, на этом уровне нет.
  `comparisonType: 'max'` корректен. Уровень пройден headless-браузером
  (Playwright/Chromium): `npm run build && npm run preview -- --port 4321
  --strictPort --base /GuessWhat/`, пройдены реальным кликом все 10
  раундов, появилась победная модалка, консоль чистая (кроме ожидаемого
  недоступного в песочнице Google Fonts CDN), ни одного 404 на картинки
  уровня. `lastReviewedLevelId` теперь `1`. Этим уровнем завершена пачка
  из 10 уровней (7-15, 1) за сегодня.

- QA уровня 15 «Кто спит дольше?» (`src/data/level15.ts`, девятый из 10
  уровней пачки этого прогона: 7-15, 1). Найдены и исправлены
  4 пары карточек с совпадающим значением сна (пары с равным `value` не
  участвуют в сравнениях — см. `buildPairQueue`), из 20 карточек это
  задевало сразу 8: «Лев» и «Золотистый хомячок» (обе 14 ч), «Домашняя
  кошка» и «Домовая мышь» (обе 12 ч), «Кролик», «Человек» и «Свинья» (все
  три 8 ч), «Корова» и «Овца» (обе 4 ч). Вместо удаления карточек значения
  уточнены до более точных десятичных цифр из того же источника, на
  который уже ссылается комментарий к файлу (сравнительные исследования
  сна млекопитающих Allison & Cicchetti, 1976): лев 13.5 ч, кошка 12.1 ч,
  кролик 8.4 ч, свинья 7.8 ч, корова 3.9 ч, овца 3.8 ч; хомячок (14 ч) и
  человек (8 ч) и мышь (12 ч) оставлены без изменений как более округлые
  общепринятые цифры. Теперь все 20 карточек уникальны по `id`, `value` и
  `funFact` (проверено скриптом `tsx` — 20/20). 8 из 20 `imageUri`
  реально лежат в `public/images/` и отданы preview-сервером с кодом 200;
  для оставшихся 12 карточек подходящих локальных фото нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен. Уровень заблокирован
  по умолчанию (открывается после уровня 14) — для QA прогресс временно
  разблокирован через localStorage, без изменений в самой игре. Пройден
  headless-браузером (Playwright/Chromium): `npm run build && npm run
  preview -- --port 4321 --strictPort --base /GuessWhat/`, пройдены
  реальным кликом все 10 раундов, появилась победная модалка, консоль
  чистая (кроме ожидаемого недоступного в песочнице Google Fonts CDN), ни
  одного 404 на картинки уровня. `lastReviewedLevelId` теперь `15`.

- QA уровня 14 «У кого крылья шире?» (`src/data/level14.ts`) — багов не
  найдено. Все 20 карточек уникальны по `id`, `value` и `funFact`
  (проверено скриптом `tsx` — 20/20). Размах крыльев сверен на
  правдоподобность: странствующий альбатрос 340 см (крупнейший размах
  среди живых птиц), сапсан 100 см при скорости пикирования свыше 300
  км/ч (самое быстрое живое существо), колибри 10 см с частотой взмахов
  до 50/с — расхождений не найдено. 2 из 20 `imageUri` (сапсан, страус)
  переиспользуют ассеты уровней 2 и 11 и реально отданы preview-сервером
  с кодом 200; для остальных 18 карточек подходящих локальных фото птиц в
  репозитории нет — честный эмодзи-фолбэк, как и задокументировано в
  комментарии к файлу. `comparisonType: 'max'` корректен. Уровень
  заблокирован по умолчанию (открывается после уровня 13) — для QA
  прогресс временно разблокирован через localStorage, без изменений в
  самой игре. Пройден headless-браузером (Playwright/Chromium): `npm run
  build && npm run preview -- --port 4321 --strictPort --base
  /GuessWhat/`, пройдены реальным кликом все 10 раундов, появилась
  победная модалка, консоль чистая (кроме ожидаемого недоступного в
  песочнице Google Fonts CDN), ни одного 404 на картинки уровня.
  `lastReviewedLevelId` теперь `14`.

- QA уровня 13 «Кто дольше вынашивает?» (`src/data/level13.ts`) — багов не
  найдено. Все 20 карточек уникальны по `id`, `value` и `funFact`
  (проверено скриптом `tsx` — 20/20). Сроки беременности сверены на
  правдоподобность: слон 660 дней (самый долгий срок среди наземных
  млекопитающих), свинья 114 дней (в точности «три месяца, три недели, три
  дня» — 90+21+3, как и говорит `funFact`), хомяк 16 дней (один из самых
  коротких сроков среди млекопитающих) — расхождений не найдено; для
  медведя и панды `value` намеренно учитывает весь срок при задержанной
  имплантации, как и задокументировано в комментарии к файлу. 14 из 20
  `imageUri` реально лежат в `public/images/` и отданы preview-сервером с
  кодом 200; для оставшихся 6 карточек (осёл, коза, овца, свинья, кролик,
  сирийский хомяк) подходящих локальных фото нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен. Уровень заблокирован
  по умолчанию (открывается после уровня 12) — для QA прогресс временно
  разблокирован через localStorage, без изменений в самой игре. Пройден
  headless-браузером (Playwright/Chromium): `npm run build && npm run
  preview -- --port 4321 --strictPort --base /GuessWhat/`, пройдены
  реальным кликом все 10 раундов, появилась победная модалка, консоль
  чистая (кроме ожидаемого недоступного в песочнице Google Fonts CDN), ни
  одного 404 на картинки уровня. `lastReviewedLevelId` теперь `13`.

- QA уровня 12 «Кто ядовитее?» (`src/data/level12.ts`) — багов не найдено.
  Все 22 карточки уникальны по `id`, `value` и `funFact` (проверено
  скриптом `tsx` — 22/22). `value` здесь — условный индекс токсичности
  (0–100), а не точное LD50 — намеренное решение, задокументированное в
  комментарии к файлу, чтобы не выдумывать точные мг/кг без доступа к
  первоисточникам; порядок значений сверен с общеизвестным консенсусом
  (тайпан Маклея, кубомедуза, ужасный листолаз — в топе; медоносная пчела,
  утконос — внизу) и расхождений не найдено. `imageUri` намеренно пуст у
  всех карточек (подходящих локальных фото ядовитых видов в
  `public/images/` нет) — честный эмодзи-фолбэк, тоже задокументировано в
  файле. `comparisonType: 'max'` корректен. Уровень по умолчанию
  заблокирован (открывается после уровня 11) — для QA прогресс временно
  разблокирован через localStorage (`guesswhat:progress:v1`), без изменений
  в самой игре. Пройден headless-браузером (Playwright/Chromium): `npm run
  build && npm run preview -- --port 4321 --strictPort --base
  /GuessWhat/`, пройдены реальным кликом все 10 раундов, появилась
  победная модалка, консоль чистая (кроме ожидаемого недоступного в
  песочнице Google Fonts CDN), ни одного 404 (картинок для запроса и нет —
  эмодзи не грузятся по сети). `lastReviewedLevelId` теперь `12`.

- QA уровня 11 «Что выше?» (`src/data/level11.ts`) — багов не найдено. Все
  25 карточек уникальны по `id`, `value` и `funFact` (проверено скриптом
  `tsx` — 25/25), и у всех 25 есть рабочий `imageUri` (реально лежат в
  `public/images/`, отданы preview-сервером с кодом 200) — картинок,
  требующих эмодзи-фолбэка, на этом уровне нет. Высоты сверены на
  правдоподобность: Бурдж-Халифа 828 м, Эверест 8848 м, K2 8611 м,
  Олимп на Марсе ~21900 м (высочайший вулкан Солнечной системы), Статуя
  Единства 182 м (самая высокая статуя в мире) — расхождений не найдено;
  факт про Шанхайскую башню (закручена на 120°, двойной фасад экономит
  энергию) уже актуален после правки в предыдущем прогоне QA.
  `comparisonType: 'max'` корректен. Уровень пройден headless-браузером
  (Playwright/Chromium): `npm run build && npm run preview -- --port 4321
  --strictPort --base /GuessWhat/`, пройдены реальным кликом все 10
  раундов, появилась победная модалка, консоль чистая (кроме ожидаемого
  недоступного в песочнице Google Fonts CDN), ни одного 404 на картинки
  уровня. `lastReviewedLevelId` теперь `11`.

- QA уровня 10 «Что калорийнее?» (`src/data/level10.ts`) — багов не
  найдено. Все 21 карточек уникальны по `id`, `value` и `funFact`
  (проверено скриптом `tsx` — 21/21). Калорийность на 100 г/мл сверена с
  типичными справочными значениями: огурец 15, брокколи 34, картофель 77,
  куриная грудка 165, говядина 250, сыр чеддер 402, шоколад 535, грецкий
  орех 654, сливочное масло 717, оливковое масло 884, сало 900, водка 231
  (за счёт спирта) — расхождений не найдено. 20 из 21 `imageUri` реально
  лежат в `public/images/` и отданы preview-сервером с кодом 200; для
  «Куриной грудки» подходящего локального фото нет — честный
  эмодзи-фолбэк (среди неиспользуемых файлов `public/images/` куриного
  мяса тоже не нашлось). `comparisonType: 'max'` корректен. Уровень
  пройден headless-браузером (Playwright/Chromium): `npm run build && npm
  run preview -- --port 4321 --strictPort --base /GuessWhat/`, пройдены
  реальным кликом все 10 раундов, появилась победная модалка, консоль
  чистая (кроме ожидаемого недоступного в песочнице Google Fonts CDN), ни
  одного 404 на картинки уровня. `lastReviewedLevelId` теперь `10`.

- QA уровня 9 «Что громче?» (`src/data/level9.ts`). Найдены и исправлены 2
  пары карточек с совпадающим значением громкости (пары с равным `value`
  не попадают в игру — см. `buildPairQueue`): «Рок-концерт» и «Бензопила»
  обе стояли на 110 дБ — концерт уточнён до 115 дБ (источники обычно дают
  диапазон 100–120 дБ у сцены, бензопила остаётся на типичных 110 дБ);
  «Сирена скорой» и «Раскат грома» обе стояли на 120 дБ — гром уточнён до
  130 дБ (ближний раскат грома нередко указывают в диапазоне 120–140 дБ),
  `funFact` грома заодно уточнён (добавлена причина — резкое расширение
  воздуха от жара молнии). Также переписан почти неинформативный и
  дублирующий по смыслу с «Газонокосилкой» факт «Городского трафика»
  (был: «Длительное воздействие может портить слух.» — то же самое уже
  говорит газонокосилка) на более конкретный: связь постоянного шума
  мегаполиса со стрессом и нарушениями сна (по данным ВОЗ). Все 19
  карточек теперь уникальны по `id`, `value` и `funFact` (проверено
  скриптом `tsx` — 19/19). 3 из 19 `imageUri` проверены — реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200; для оставшихся
  16 карточек подходящих локальных фото нет (единственный неиспользуемый
  файл `sound_...jpg` слишком общий, чтобы уверенно привязать к
  конкретному звуку — оставлен честный эмодзи-фолбэк, а не угадывание).
  `comparisonType: 'max'` корректен. Уровень пройден headless-браузером
  (Playwright/Chromium): `npm run build && npm run preview -- --port 4321
  --strictPort --base /GuessWhat/`, пройдены реальным кликом все 10
  раундов, появилась победная модалка, консоль чистая (кроме ожидаемого
  недоступного в песочнице Google Fonts CDN), ни одного 404 на картинки
  уровня. `lastReviewedLevelId` теперь `9`.

- QA уровня 8 «Кто живет дольше?» (`src/data/level8.ts`). Найден и
  исправлен 1 неточный факт: у карточки «Собака» было указано, что самая
  старая собака в мире прожила 31 год — по данным Книги рекордов Гиннесса
  рекорд принадлежит австралийской пастушьей собаке Bluey (1910–1939) и
  составляет 29 лет 5 месяцев; текст факта исправлен на «29 лет» со
  ссылкой на источник. Остальные 18 карточек уникальны по `id`, `value` и
  `funFact` (проверено скриптом `tsx` — 19/19 по всем трём).
  Продолжительности жизни сверены на правдоподобность: гренландская акула
  ~400 лет и рост ~1 см/год (исследование 2016 года), моллюск-океанический
  куахог 507 лет (знаменитый «Мин»), сосна долговечная 5000 лет (дерево
  Мафусаил, старше пирамид), секвойя 3000 лет — расхождений больше не
  найдено. 9 из 19 `imageUri` проверены — реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200; для оставшихся
  10 карточек (подёнка, беркут, галапагосская черепаха, кролик, шимпанзе,
  ара, карп кои, гренландский кит, куахог, сосна долговечная) подходящих
  локальных фото нет — честный эмодзи-фолбэк. `comparisonType: 'max'`
  корректен. Уровень пройден headless-браузером (Playwright/Chromium):
  `npm run build && npm run preview -- --port 4321 --strictPort --base
  /GuessWhat/`, пройдены реальным кликом все 10 раундов, появилась
  победная модалка, консоль чистая (кроме ожидаемого недоступного в
  песочнице Google Fonts CDN), ни одного 404 на картинки уровня.
  `lastReviewedLevelId` теперь `8`.

- QA уровня 7 «Что дороже?» (`src/data/level7.ts`, первый в пачке этого
  прогона — 10 уровней: 7-15, 1). Найден и исправлен 1 дефект: карточки
  «Игровой ноутбук» (`laptop`) и «Ноутбук Pro» (`laptop_pro`) описывали по
  сути один и тот же товар с одинаковой ценой $2500 — пара с равным
  `value` не участвует в сравнениях (см. `buildPairQueue`), а сами
  карточки почти дублировали друг друга по смыслу; менее содержательная
  «Ноутбук Pro» убрана. Оставшиеся 20 карточек уникальны по `id`, `value` и
  `funFact` (проверено скриптом `tsx` — 20/20 по всем трём). 9 из 20
  `imageUri` проверены — реально лежат в `public/images/` и отданы
  preview-сервером с кодом 200; для оставшихся 11 карточек (кофе, ноутбук,
  часы Rolex, слиток золота, частный самолёт, бутылка воды, билет в кино,
  видеоигра, новый автомобиль, дом в США, истребитель F-35) подходящих
  локальных фото нет — честный эмодзи-фолбэк, среди неиспользуемых
  файлов `public/images/` совпадений по смыслу тоже не нашлось.
  `comparisonType: 'max'` корректен («что дороже» — верно большее
  значение). Цены сверены на правдоподобность (Мона Лиза ~$860 млн
  страховой оценки, МКС ~$150 млрд, телескоп Хаббл ~$16 млрд, Бурдж
  Халифа ~$1.5 млрд постройки) — расхождений не найдено. Уровень пройден
  headless-браузером (Playwright/Chromium из `/opt/pw-browsers`, запущен
  без установки новых npm-пакетов через `NODE_PATH` на глобальный модуль
  `/opt/node22/lib/node_modules/playwright`): собранная прод-сборка
  поднята через `npm run build && npm run preview -- --port 4321
  --strictPort --base /GuessWhat/` (без явного `--base` preview-сервер
  раздаёт ассеты с корня, а собранный `index.html` ссылается на
  `/GuessWhat/assets/...` — с флагом пути совпадают), пройдены реальным
  кликом все 10 раундов, появилась победная модалка «Уровень Пройден!»,
  консоль браузера чистая (кроме ожидаемого `ERR_CONNECTION_RESET` при
  недоступном в песочнице Google Fonts CDN — не связано с игрой), ни
  одного 404 на картинки уровня. `lastReviewedLevelId` теперь `7`.

## 2026-09-07

- QA уровня 6 «Где глубже?» (`src/data/level6.ts`, последний в пачке этого
  прогона — 10 уровней: 12-15, 1-6). Все 17 карточек уникальны по `id`,
  `funFact` и значению глубины (проверено скриптом — 17/17 по всем трём).
  10 из 17 `imageUri` проверены — реально лежат в `public/images/` и
  отданы preview-сервером с кодом 200 (`curl -sSI`); для оставшихся 7
  карточек (Байкал, рекорд дайвинга, бассейн, пещера Крубера, метро,
  морской слон, клюворыл Кювье) подходящих локальных фото нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен. Значения сверены на
  правдоподобность: Марианская впадина 10,994 м, Кольская скважина 12,262
  м (глубочайшая скважина в мире), пещера Крубера 2,199 м (глубочайшая
  известная пещера), рекорд Ахмеда Габра 332 м (2014), станция
  «Арсенальная» в Киеве 105.5 м (глубочайшая станция метро в мире) —
  расхождений не найдено. Использование высоты Бурдж-Халифы (828 м) как
  карточки для наглядного масштаба глубин — намеренный приём, не баг.
  Багов не найдено, правок не потребовалось. Уровень пройден
  фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по всем 10 путям картинок (200), временный
  Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии по 10
  раундов без исключений. Этим уровнем завершена пачка 12-6 за сегодня;
  `lastReviewedLevelId` теперь `6`.

- QA уровня 5 «Что горячее?» (`src/data/level5.ts`). Найдены и исправлены 2
  недочёта: (1) «Лава» и «Магма» имели одинаковое значение 1200 °C — при
  том что физически это разные состояния одного вещества (магма ещё под
  землёй немного горячее, лава уже остыла при извержении), значение магмы
  поднято до 1300 °C, а `funFact` лавы уточнён («...немного остывает по
  пути»), пара перестала выпадать из сравнения; (2) у карточки «Горение
  бумаги» не было `imageUri`, хотя подходящее локальное фото бумаги уже
  есть в `public/images/` (используется уровнем 3 для карточки «Бумага») —
  подключено повторно. Остальные 16 карточек уникальны по `id` и `funFact`
  (проверено скриптом — 17/17). 9 из 17 `imageUri` (после правки)
  проверены — реально лежат в `public/images/` и отданы preview-сервером с
  кодом 200 (`curl -sSI`); для оставшихся 8 карточек (Солнце, лава,
  кипящая вода, жидкий азот, печь для пиццы, лампа накаливания, ядро
  Солнца, плавление железа) подходящих локальных фото нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен, значения сверены на
  правдоподобность (плавление железа 1538 °C, точка кипения воды на
  Эвересте ~68 °C, самовоспламенение бумаги по Фаренгейту 451 = 232 °C).
  Уровень пройден фолбэк-способом: `npm run build && npm run preview --
  --port 4321 --strictPort`, `curl -sSI` по всем 9 путям картинок (200),
  временный Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии
  по 10 раундов без исключений.

- QA уровня 4 «Что больше?» (`src/data/level4.ts`). Все 21 карточка
  уникальны по `id`, `funFact` и значению длины/высоты (проверено скриптом
  — 21/21 по всем трём). 15 из 21 `imageUri` проверены — реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200 (`curl -sSI`); для
  оставшихся 6 карточек (теннисный корт, дорожка для боулинга, олимпийский
  бассейн, вагон поезда, авианосец, подводная лодка) подходящих локальных
  фото нет — честный эмодзи-фолбэк. `comparisonType: 'max'` корректен.
  Значения сверены на правдоподобность: теннисный корт 23.77 м и дорожка
  для боулинга 18.29 м — точные официальные размеры (ITF/стандарт
  боулинга), Великая Китайская стена 21,196 км соответствует итогам
  обмера 2012 года, Бурдж-Халифа как самое высокое сооружение в мире —
  всё ещё актуально. Багов не найдено, правок не потребовалось. Уровень
  пройден фолбэк-способом: `npm run build && npm run preview -- --port
  4321 --strictPort`, `curl -sSI` по всем 15 путям картинок (200),
  временный Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии
  по 10 раундов без исключений.

- QA уровня 3 «Что древнее?» (`src/data/level3.ts`). Дубликатов `id` и
  `funFact` нет (проверено скриптом — 24/24 уникальных факта), 22 из 24
  значений года уникальны (Интернет/Высадка на Луну — оба 1969, Кино/Радио
  — оба 1895: не баг, а реальное историческое совпадение, оба события
  документально датированы этим годом). 19 из 24 `imageUri` проверены —
  реально лежат в `public/images/` и отданы preview-сервером с кодом 200
  (`curl -sSI`); для оставшихся 5 карточек (печатный станок, кино, компас,
  телескоп, паровой двигатель) подходящих локальных фото нет — честный
  эмодзи-фолбэк. `comparisonType: 'min'` корректен для «что древнее»
  (меньше/отрицательнее год — правильный ответ, отрицательные значения для
  дат до н.э. работают верно). Факты сверены на правдоподобность (Гутенберг
  1440, порох ~850 н.э., телескоп 1608/Галилей, первое фото Ньепса 1826 с
  8-часовой выдержкой, ARPANET/прилунение 1969, биткоин 2009 и т.д.) —
  расхождений не найдено. Багов не найдено, правок не потребовалось.
  Уровень пройден фолбэк-способом: `npm run build && npm run preview --
  --port 4321 --strictPort`, `curl -sSI` по всем 19 путям картинок (200),
  временный Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии
  по 10 раундов без исключений.

- QA уровня 2 «Кто быстрее?» (`src/data/speeds.ts`). Дубликатов `id` и
  `funFact` нет (проверено скриптом — 20/20 уникальных фактов), все 20
  `imageUri` реально лежат в `public/images/` и отданы preview-сервером с
  кодом 200 (`curl -sSI` по всем 20 путям). `comparisonType: 'max'`
  корректен, `subtitle` «Скоростные чемпионы» без опечатки (баг
  «Скорстные чемпионы» пофикшен ещё 2026-08-15). 16 из 20 значений скорости
  уникальны (совпадения: грейхаунд/лось по 72 км/ч, кенгуру 71 не
  совпадает ни с кем, койот/зебра/верблюд по 65 км/ч, жираф/дельфин по 60
  км/ч) — исключает всего 5 пар сравнения из 190 возможных, на
  играбельность не влияет. Найдена крупная находка по интересности: почти
  все `funFact` заметно короче и шаблоннее, чем в остальных уровнях
  («Опасный и быстрый хищник.», «Обгоняют человека на суше.») — сама по
  себе не баг (факты корректны и не дублируются), но заметно ниже
  качеством контента, чем соседние уровни; заведена отдельная задача в
  `TASKS.md` («Обогатить funFact уровня 2»), в рамках этого прогона не
  переписывалась. Уровень пройден фолбэк-способом: `npm run build && npm
  run preview -- --port 4321 --strictPort`, `curl -sSI` по всем 20 путям
  картинок (200), временный Node/esbuild-скрипт (не закоммичен) — 3
  симулированные сессии по 10 раундов без исключений.

- QA уровня 1 «Кто тяжелее?» (`src/data/level1.ts`, 5-й в пачке этого
  прогона — цикл дошёл до конца списка реальных уровней (15) и продолжился
  с начала). Все 30 карточек (`LEVEL1_DATA` + `LEVEL1_ADDITIONAL`)
  уникальны по `id` и `funFact` (проверено скриптом — 30/30 уникальных
  фактов), 29 из 30 значений массы уникальны (у `grizzly-bear`/`zebra`
  совпадение 350 кг — задокументированный намеренный кейс для проверки
  логики ничьей в `GameLevel.tsx`, не баг). Все 30 `imageUri` реально
  лежат в `public/images/` и отданы preview-сервером с кодом 200 (`curl
  -sSI` по всем 30 путям). `comparisonType: 'max'` корректен (тяжелее —
  верный ответ), `property`/`unit` в `levels.ts` соответствуют данным.
  Багов не найдено, правок не потребовалось. Уровень пройден
  фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по всем 30 путям картинок (200), временный
  Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии по 10
  раундов без исключений.

- QA уровня 15 «Кто спит дольше?» (`src/data/level15.ts`, последний в пачке
  этого прогона). Дубликатов `id` нет, `funFact` везде непустой и
  содержательно различается между карточками (в т.ч. у пар с одинаковым
  числом часов сна — мышь/кошка по 12 ч, кролик/человек/свинья по 8 ч,
  корова/овца по 4 ч — факты не повторяют друг друга, а описывают разные
  причины; таких коллизий всего 5 из 190 возможных пар, на разнообразие
  игры это не влияет). 11 из 20 `imageUri` проверены — реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200 (`curl -sSI`); для
  оставшихся 9 видов (коала, ночница, суслик, хомячок, макака, кролик,
  свинья, коза, овца) подходящих локальных фото по-прежнему нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен, `title`/`subtitle`/
  `property`/`unit` в `levels.ts` соответствуют данным, оговорка про сон
  ленивца в неволе на месте. Багов не найдено, правок не потребовалось.
  Уровень пройден фолбэк-способом: `npm run build && npm run preview --
  --port 4321 --strictPort`, `curl -sSI` по всем 11 путям картинок (200),
  временный Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии
  по 10 раундов без исключений. Уровнем 15 закончились реальные уровни
  цикла — следующий уровень пачки (16-30) заглушки «Секрет» и не входит в
  выборку, поэтому дальше пачка продолжится циклически с уровня 1.

- QA уровня 14 «У кого крылья шире?» (`src/data/level14.ts`). Все 20
  карточек уникальны по `id` и значению размаха крыльев, `funFact` везде
  непустой и без смысловых повторов между карточками. 2 из 20 `imageUri`
  (страус, сапсан) проверены — реально лежат в `public/images/` и отданы
  preview-сервером с кодом 200 (`curl -sSI`); для остальных 18 видов птиц
  подходящих локальных фото в `public/images/` по-прежнему нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен (шире размах — верный
  ответ), `title`/`subtitle`/`property`/`unit` в `levels.ts` соответствуют
  данным. Багов не найдено, правок в данные не потребовалось. Уровень
  пройден фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по обоим путям картинок (200), временный
  Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии по 10
  раундов без исключений и без пар с равными значениями.

- QA уровня 13 «Кто дольше вынашивает?» (`src/data/level13.ts`). Все 20
  карточек уникальны по `id` и значению срока беременности, `funFact`
  везде непустой. Найден и исправлен один недочёт из шага 2 (интересность):
  факты домовой мыши и сирийского хомяка были почти дословными близнецами
  («Один из самых коротких сроков беременности среди...млекопитающих...»)
  — переписан факт хомяка на конкретику (повторная беременность сразу после
  родов, открытие глаз на 14-й день), дублирования по смыслу больше нет. 14
  из 20 `imageUri` проверены — реально лежат в `public/images/` и отданы
  preview-сервером с кодом 200 (`curl -sSI` по всем 14 путям); для
  оставшихся 6 карточек без картинки (осёл, коза, овца, свинья, кролик,
  хомяк) в `public/images/` по-прежнему нет подходящих файлов — честный
  эмодзи-фолбэк, ничего не подставлено наугад. `comparisonType: 'max'`
  корректен, `title`/`subtitle`/`property`/`unit` в `levels.ts`
  соответствуют данным, задержанная имплантация у медведя/панды учтена
  правильно (полный срок от спаривания до родов, как принято в
  зоологических источниках). Уровень пройден фолбэк-способом: `npm run
  build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по
  всем путям картинок (200), временный Node/esbuild-скрипт (не закоммичен)
  — 3 симулированные сессии по 10 раундов без исключений и без пар с
  равными значениями.

- QA уровня 12 «Кто ядовитее?» (`src/data/level12.ts`, первый в пачке этого
  прогона). Проверено скриптом: все 22 карточки уникальны по `id`, значения
  индекса токсичности не повторяются (22 уникальных из 22), `funFact` не
  пустой и не дублируется ни у одной пары карточек. Ни у одной карточки нет
  `imageUri` — это осталось верным: подходящих локальных фото ядовитых
  видов (змеи, медузы, пауки, скорпионы, утконос и т.д.) в `public/images/`
  по-прежнему нет, эмодзи-фолбэк оправдан. `comparisonType: 'max'` корректен
  для «кто ядовитее» (выше индекс — правильный ответ), `title`/`subtitle`/
  `property`/`unit` в `levels.ts` соответствуют данным. Факты сверены на
  правдоподобность — соответствуют общеизвестному научному консенсусу об
  опасности каждого вида, повторов чисел/фактов нет. Багов не найдено,
  правок в код/данные не потребовалось. Уровень пройден фолбэк-способом
  (браузер недоступен): временный Node/esbuild-скрипт (не закоммичен),
  повторяющий логику `buildPairQueue`/`generateNewPair` из `GameLevel.tsx`,
  — 3 симулированные сессии по 10 раундов без исключений, пар с равными
  значениями не возникает; `imageUri`-проверка через preview-сервер не
  требовалась, так как ни у одной карточки нет `imageUri`.


- QA уровня 11 «Что выше?» (`src/data/level11.ts`, 25 карточек, последний в
  пачке этого прогона). В отличие от уровней 4-10, дубликатов карточек не
  найдено — все 25 объектов и значений высоты уникальны. Все 25 `imageUri`
  реально лежат в `public/images/`. Найдена и исправлена одна фактическая
  неточность: `funFact` Шанхайской башни утверждал «Второе по высоте здание
  в мире» — с открытием Merdeka 118 в Куала-Лумпуре (678,9 м) в 2024 году
  Шанхайская башня опустилась на 3-е место, а ранговые факты быстро
  устаревают; заменено на не зависящий от рейтинга факт о закрученной на
  120° конструкции с двойным фасадом. `comparisonType: 'max'` корректен,
  остальные высоты и факты проверены и правдоподобны (включая пересечения
  объектов с уровнями 4/6, что ожидаемо и не является багом). Уровень
  пройден фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по всем 25 путям картинок (200), временный
  Node-скрипт (не закоммичен) — 200 симулированных сессий по 10 раундов без
  ошибок. Этим уровнем завершена пачка 2-11 за сегодня;
  `lastReviewedLevelId` теперь `11`.

- QA уровня 10 «Что калорийнее?» (`src/data/level10.ts`). Тот же тип бага,
  что и в уровнях 4-9: из 23 карточек 2 были дубликатами (Куриная грудка,
  Сыр Чеддер — те же значения под другим `id`). Дубликаты убраны, стало 21
  уникальная карточка вместо 23. Остальные 19 `imageUri` реально лежат в
  `public/images/`; для оставшейся без картинки Куриной грудки подходящего
  файла в `public/images/` не нашлось — честный emoji-фолбэк.
  `comparisonType: 'max'` корректен, калорийность на 100 г проверена по всем
  20 позициям и правдоподобна. Уровень пройден фолбэк-способом: `npm run
  build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по всем
  путям картинок (200), временный Node-скрипт (не закоммичен) — 200
  симулированных сессий по 10 раундов без ошибок.

- QA уровня 9 «Что громче?» (`src/data/level9.ts`). Тот же тип бага, что и в
  уровнях 4-8: из 22 карточек 4 были дубликатами одного и того же источника
  звука под другим `id` (Холодильник, Разговор, Сирена скорой — то же
  значение; Рок-концерт — 110/120 дБ под одним названием). Дубликаты убраны,
  стало 18 уникальных карточек вместо 22. Заодно у карточки «Раскат грома»
  без картинки подключено фото `lightning_1786533903072.jpg` (гроза —
  тематически подходит). Остальные 2 `imageUri` (двигатель самолета,
  светошумовая граната) реально лежат в `public/images/`; для 15 карточек
  без картинки подходящего файла не нашлось — честный emoji-фолбэк.
  `comparisonType: 'max'` корректен, значения дБ проверены и правдоподобны
  (включая творческие «хвостовые» карточки — Тунгусский метеорит и
  столкновение чёрных дыр, поданные как образовательное преувеличение
  масштаба, а не выдуманные точные измерения). Уровень пройден
  фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по всем путям картинок (200), временный
  Node-скрипт (не закоммичен) — 200 симулированных сессий по 10 раундов без
  ошибок.

- QA уровня 8 «Кто живет дольше?» (`src/data/level8.ts`). Тот же тип бага,
  что и в уровнях 4-7: из 24 карточек 5 были дубликатами одного и того же
  животного под другим `id` (Мышь/Домашняя мышь — то же значение; Лев —
  15/14 лет, Лошадь — 25/30 лет и Слон (Африканский слон/Слон) под разными
  id, у слона дополнительно дубль с тем же значением 70). Дубликаты убраны
  (для льва и лошади оставлено первое, более подробное описание), стало 19
  уникальных карточек вместо 24. Заодно у карточки «Африканский слон» без
  картинки подключено фото `african_elephant_1786116449273.jpg`
  (переиспользовано из level1/13/15). Остальные 6 `imageUri` реально лежат в
  `public/images/`; для 10 карточек без картинки подходящего файла не
  нашлось — честный emoji-фолбэк. `comparisonType: 'max'` корректен,
  значения продолжительности жизни проверены и правдоподобны. Уровень
  пройден фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по всем путям картинок (200), временный
  Node-скрипт (не закоммичен) — 200 симулированных сессий по 10 раундов без
  ошибок.

- QA уровня 7 «Что дороже?» (`src/data/level7.ts`). Найдены и исправлены 3
  дублирующиеся карточки с расходящимися значениями цены под одним и тем же
  названием (баг того же типа, что в уровнях 4-6): «Часы Rolex» встречались
  дважды ($15,000 и $10,000), «Частный самолет» — дважды ($25 млн и $65 млн),
  «Картина Мона Лиза» — дважды ($860 млн и $870 млн). По каждой оставлена
  одна карточка, стало 21 уникальная вместо 24. «Игровой ноутбук» и «Ноутбук
  Pro» с совпадающим значением $2,500 оставлены — это разные продукты
  (игровой ноутбук/про-ноутбук для разработчиков), совпадение цены случайно
  и правдоподобно, не дубликат. Остальные 8 `imageUri` реально лежат в
  `public/images/`; для 12 карточек без картинки подходящего свободного
  файла в `public/images/` не нашлось (например, для «Частного самолёта» и
  истребителя F-35 в наличии только фото коммерческих пассажирских
  самолётов — подставлять их не по смыслу) — честный emoji-фолбэк.
  `comparisonType: 'max'` корректен. Уровень пройден фолбэк-способом: `npm
  run build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по
  всем путям картинок (200), временный Node-скрипт (не закоммичен) — 200
  симулированных сессий по 10 раундов без ошибок.

- QA уровня 6 «Где глубже?» (`src/data/level6.ts`). Тот же тип бага, что и в
  уровнях 4-5: из 25 карточек 8 были буквальными дубликатами одного и того
  же объекта под другим `id` (Марианская впадина, Обломки Титаника, Рекорд
  дайвинга, Кольская скважина — с тем же значением, причём у Кольской
  скважины дубликат использовал ровно тот же файл картинки; Гранд-Каньон,
  Пещера Крубера, Нырок кашалота, Озеро Байкал — тоже дубликаты). Оставлена
  по одной карточке на объект (для дублей без картинки — та версия, у которой
  она есть), стало 17 уникальных карточек. Заодно у 4 карточек без картинки
  нашлись подходящие неиспользуемые файлы — подключены: Обломки Титаника
  (переиспользован `titanic_1786532003551.jpg` из level4), Нырок кашалота
  (переиспользован `sperm_whale_1786116437079.jpg` из level1), Высота
  Бурдж-Халифы (переиспользован `burj_khalifa_1786531685569.jpg` из level4),
  Погружение синего кита (`blue_whale_size_1786531756325.jpg`, освободился
  после чистки дублей в level4). Остальные карточки без картинки (Байкал,
  Рекорд дайвинга, бассейн, Пещера Крубера, станция метро, морской слон,
  клюворыл Кювье) оставлены с честным emoji-фолбэком — подходящего файла в
  `public/images/` для них нет. `comparisonType: 'max'` корректен, значения
  глубин проверены и правдоподобны. Уровень пройден фолбэк-способом: `npm
  run build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по
  всем путям картинок (200), временный Node-скрипт (не закоммичен) — 200
  симулированных сессий по 10 раундов без ошибок.

- QA уровня 5 «Что горячее?» (`src/data/level5.ts`). Тот же тип бага, что и в
  уровне 4: из 23 карточек 6 пар были буквальными дубликатами одного и того
  же объекта под другим `id` (Кипящая вода, Тело человека, Абсолютный нуль,
  Сухой лёд, Жидкий азот — с тем же значением; Пламя свечи — с расходящимся
  значением 1400 °C и 1000 °C под одним названием). Дубликаты убраны (для
  свечи оставлено значение 1400 °C — с более подробным и точным `funFact` о
  голубой верхушке пламени), стало 17 уникальных карточек. Заодно исправлена
  фактическая ошибка: `funFact` карточки «Магма» утверждал, что «лава
  становится магмой под землёй» — на самом деле наоборот, магма становится
  лавой, извергаясь на поверхность; текст исправлен. Оставшиеся 5 `imageUri`
  реально лежат в `public/images/`; для 9 карточек без картинки подходящего
  свободного файла в `public/images/` не нашлось — честный emoji-фолбэк.
  `comparisonType: 'max'` корректен. Уровень пройден фолбэк-способом: `npm
  run build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по
  всем путям картинок (200), временный Node-скрипт (не закоммичен) — 200
  симулированных сессий по 10 раундов без ошибок.

- QA уровня 4 «Что больше?» (`src/data/level4.ts`). Найден и исправлен
  настоящий баг с данными: в уровне было 25 карточек, но 4 из них —
  буквальные дубликаты одного и того же объекта под другим `id` (Футбольное
  поле, Бурдж-Халифа, Великая Китайская стена — с тем же значением и почти
  тем же `funFact`, только другая картинка; Синий кит — тем же самым, но с
  расходящимся значением длины 33 м и 30 м под одним и тем же названием).
  Дубликаты убраны (оставлена по одной карточке на объект, с более
  распространённым и точнее подтверждённым значением для кита — 30 м), стало
  21 уникальная карточка. Остальные 15 `imageUri` реально лежат в
  `public/images/`; для 6 карточек без картинки (теннисный корт, дорожка для
  боулинга, олимпийский бассейн, вагон поезда, авианосец, подводная лодка)
  подходящего свободного файла в `public/images/` не нашлось — оставлен
  честный emoji-фолбэк. `comparisonType: 'max'` корректен («больше» = большая
  длина/высота). Уровень пройден фолбэк-способом: `npm run build && npm run
  preview -- --port 4321 --strictPort`, `curl -sSI` по всем путям картинок
  (200), временный Node-скрипт (не закоммичен) — 200 симулированных сессий по
  10 раундов без ошибок, дубликатов `funFact` после чистки не осталось.

- QA уровня 3 «Что древнее?» (`src/data/level3.ts`, 24 карточки). Найдено и
  исправлено: у «Первый iPhone» и «Освоение огня» был пустой `imageUri` при
  наличии подходящих неиспользуемых-по-смыслу файлов в `public/images/` —
  подключены `smartphone_1786534090123.jpg` (уже используется в level7, но
  переиспользование практикуется, см. level15) и `candle_flame_1786533824833.jpg`
  соответственно; остальные 5 карточек без картинки (печатный станок, первый
  кинофильм, компас, телескоп, паровой двигатель) оставлены с честным
  emoji-фолбэком — подходящего файла для них в `public/images/` нет. Остальные
  17 `imageUri` реально лежат в `public/images/`. Проверено: `comparisonType:
  'min'` в `levels.ts` — правильно, «древнее» = меньший (более отрицательный)
  год; два безобидных совпадения года (Интернет/Высадка на Луну — оба 1969,
  Кино/Радио — оба 1895) исторически точны, не ошибка данных, `buildPairQueue`
  просто исключает эти 2 пары из 276 возможных; все `funFact` непустые,
  правдоподобные и не повторяются. Уровень пройден фолбэк-способом: `npm run
  build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по всем
  путям картинок (200 после исправления), временный Node-скрипт (не
  закоммичен) — 200 симулированных сессий по 10 раундов без ошибок.

- QA уровня 2 «Кто быстрее?» (`src/data/speeds.ts`, 20 карточек) — первый
  уровень пачки 5–10 за прогон (`lastReviewedLevelId` был `1`). Проверено:
  все 20 `imageUri` реально лежат в `public/images/` (сверено скриптом по
  файловой системе); значения `value` (макс. скорость, км/ч) разнообразны —
  только 3 небольших совпадения (Грейхаунд/Лось — 72, Койот/Зебра/Верблюд —
  65, Жираф/Дельфин — 60) из 20 карточек, `buildPairQueue` их исключает,
  остальных 185 пар из C(20,2)=190 достаточно на 10 раундов; все `funFact`
  непустые, правдоподобные и не повторяются; `title`/`property`/`unit`/
  `comparisonType: 'max'` в `levels.ts` соответствуют смыслу вопроса «Кто
  быстрее?» (побеждает большая скорость). Багов не найдено, править нечего.
  Уровень пройден без headless-браузера (Playwright не установлен в
  `devDependencies`, установка новых пакетов без интернета недоступна) —
  фолбэк: `npm run build && npm run preview -- --port 4321 --strictPort`,
  затем `curl -sSI` по всем 20 путям картинок уровня (все отдали 200), и
  временный Node-скрипт (не закоммичен), повторяющий логику
  `buildPairQueue`/`generateNewPair` из `GameLevel.tsx`, — 200 симулированных
  сессий по 10 раундов прошли без исключений и с корректным определением
  победителя в каждой паре.

## 2026-09-05

- QA уровня 1 «Кто тяжелее?» (`src/data/level1.ts`, 30 карточек) — первый
  прогон новой ежедневной задачи «разбор одного уровня в день»
  (`lastReviewedLevelId` был `0`). Проверено: все 30 `imageUri` реально лежат
  в `public/images/` (сверено скриптом по файловой системе); значения `value`
  разнообразны — только одна пара с совпадающим весом (Гризли/Зебра, по
  350 кг) при 30 карточках, остальные 434 сравнения из C(30,2)=435 уникальны;
  все `funFact` непустые, правдоподобные и не повторяются; `title`/`property`/
  `unit`/`comparisonType: 'max'` в `levels.ts` соответствуют смыслу вопроса
  «Кто тяжелее?» (побеждает больший вес). Багов не найдено, править нечего.
  Уровень пройден без headless-браузера (в окружении нет установленного
  Playwright/Chromium-пакета в `devDependencies`, а установка новых пакетов
  без интернета недоступна) — использован фолбэк: `npm run build && npm run
  preview -- --port 4321 --strictPort`, затем `curl -sSI` по всем 30 путям
  картинок уровня (все отдали 200), и временный Node-скрипт (не закоммичен),
  повторяющий логику `buildPairQueue`/`generateNewPair` из `GameLevel.tsx` —
  10 симулированных раундов прошли без исключений, верным всегда оказывался
  более тяжёлый участник пары. Preview-сервер остановлен по завершании
  проверки. Новых пунктов в `TASKS.md` не заведено — крупных находок нет.

## 2026-09-04

- Все секции `TASKS.md` были пусты — добавлены 3 новые задачи (кнопка
  «Поделиться результатом», показ прогресса до следующей звезды на
  экране уровня, наполнение уровня 16 контентом), закрыта первая по
  приоритету («Функциональность» выше «Контента»). В победную модалку
  `GameLevel.tsx` добавлена кнопка «Поделиться результатом»: на
  устройствах с Web Share API (`navigator.share`, в основном мобильные)
  открывает системное меню шеринга; без поддержки — копирует тот же текст
  через `navigator.clipboard.writeText` с подтверждением на кнопке
  («Скопировано!» / «Не удалось скопировать»). Добавлен хелпер
  `pluralizeRu` для верного русского склонения числительных (очко/очка/
  очков, звезда/звёзды/звёзд) в тексте шеринга. Новых зависимостей нет —
  использованы существующий `lucide-react` (иконка `Share2`) и
  браузерные Web Share/Clipboard API.

## 2026-09-03

- Закрыта задача «Наполнить уровень 15 контентом» из `TASKS.md`. Создан
  `src/data/level15.ts` — тема «Кто спит дольше?», суточная продолжительность
  сна 20 видов животных в часах (от коалы, 20 ч, до жирафа, 1.9 ч). 11 из 20
  карточек используют уже существующие локальные фото из `public/images/`,
  остальные — emoji-фолбэк. Подключено в `App.tsx`/`getLevelData` (кейс `15`),
  запись уровня 15 в `src/data/levels.ts` обновлена, `isUnlocked: false`
  сохранён — открывается через `UNLOCK_NEXT_LEVEL_STARS` после уровня 14.
  Новых зависимостей нет.

## 2026-09-02

- Закрыта задача «Тактильная отдача (Vibration API) на верный/неверный
  ответ» из `TASKS.md`. `audio.playWrong()` в `src/utils/audio.ts` уже
  вызывал `navigator.vibrate([100, 50, 100])` (паттерн из двух импульсов),
  а `audio.playCorrect()` — нет. Добавлен приватный хелпер
  `SoundManager.vibrate(pattern)` с проверкой `'vibrate' in navigator`
  (не падает в браузерах без поддержки, например iOS Safari);
  `playCorrect()` теперь даёт один короткий импульс (`vibrate(40)`),
  `playWrong()` переведён на тот же хелпер с прежним паттерном. Вибрация
  отключается вместе со звуком через общий флаг `isMuted`. Новых
  зависимостей нет.

## 2026-09-01

- Закрыта задача «Модалка заблокированного уровня всегда предлагает пройти
  Уровень 1» из `TASKS.md`. `LevelLockedModal.tsx` для любого заблокированного
  уровня показывал хардкоженный текст «Пройди Уровень 1, чтобы разблокировать
  этот и следующие уровни!» и кнопку «Играть в Уровень 1», хотя с
  `UNLOCK_NEXT_LEVEL_STARS` уровни открываются строго по цепочке (прохождение
  уровня 1 открывает только уровень 2) — для уровня №12, например, реально
  нужен пройденный уровень 11. Модалка теперь принимает `continueLevel:
  LevelInfo` (вычисляется через уже существующий `getContinueLevelId` в
  `App.tsx`) и показывает/ведёт на реальный следующий шаг игрока вместо
  зашитого уровня 1. Заодно все секции `TASKS.md` были пусты — добавлены 2
  новые задачи в бэклог (тактильная отдача на ответ, наполнение уровня 15).
  Новых зависимостей нет.

## 2026-08-31

- Закрыта задача «Наполнить уровень 14 контентом» из `TASKS.md`. Уровень 14
  (`id: 14` в `src/data/levels.ts`) был заглушкой-секретом. Выбрана тема
  «У кого крылья шире?» — размах крыльев 20 видов птиц в сантиметрах, от
  340 см у странствующего альбатроса до 10 см у рубиновогорлого колибри.
  Создан `src/data/level14.ts` (2 карточки с локальными фото — сапсан,
  африканский страус; остальные 18 — emoji-фолбэк), подключён в
  `App.tsx`/`getLevelData`, запись уровня 14 в `src/data/levels.ts`
  обновлена (title/subtitle/icon/category/property/unit/comparisonType),
  `isUnlocked: false` сохранён — открывается через `UNLOCK_NEXT_LEVEL_STARS`
  при прохождении уровня 13. Новых зависимостей нет.

## 2026-08-30

- Закрыта задача «Кнопка "Продолжить" на главном экране» из `TASKS.md`.
  Кнопка «ИГРАТЬ» в `StartMenu.tsx` всегда открывала уровень 1, даже если
  игрок уже прошёл несколько уровней. Добавлена `getContinueLevelId(levels)`
  в `src/lib/storage.ts` — первый разблокированный уровень без максимума
  звёзд (3), либо последний разблокированный, если всё пройдено на
  максимум. `App.tsx` передаёт её результат и в `onStartGame`, и в новый
  проп `StartMenu.continueLevelId`. Кнопка подписана «ПРОДОЛЖИТЬ Уровень N»,
  как только у игрока есть хотя бы одна звезда где-либо; для нового игрока
  без прогресса подпись остаётся «ИГРАТЬ» (уровень 1). Новых зависимостей
  нет.

## 2026-08-29

- Закрыта задача «Хардкоженная надпись "Доступно сейчас: 11 уровней" на
  главном экране» из `TASKS.md`. Все секции бэклога были пусты — добавлены
  3 новые задачи (эта, кнопка «Продолжить» на главном экране, наполнение
  уровня 14) и выполнена первая по приоритету («Баги»). `StartMenu.tsx`
  показывал статичный текст «11 уровней», не связанный с реальным
  состоянием `levels` — заменено на `unlockedCount = levels.filter(lvl =>
  lvl.isUnlocked).length`, компонент принимает новый проп `levels`
  (передаётся из `App.tsx`). Новых зависимостей нет.

## 2026-08-28

- Закрыта задача «Наполнить уровень 13 контентом» из `TASKS.md`. Уровень 13
  был заглушкой («Секретный уровень», `isUnlocked: false`). Создан
  `src/data/level13.ts` — новая тема «Кто дольше вынашивает потомство?»,
  20 карточек со средним сроком беременности млекопитающих (660 дней у
  африканского слона → 16 дней у сирийского хомяка), 14 из них используют
  уже существующие локальные фото из `public/images/`, остальные — на
  emoji-фолбэке. Подключено в `App.tsx`/`getLevelData` (кейс `13`), запись
  уровня 13 в `src/data/levels.ts` обновлена, `isUnlocked: false` сохранён —
  открывается через `UNLOCK_NEXT_LEVEL_STARS` при прохождении уровня 12.
  Новых зависимостей нет.

## 2026-08-27

- Закрыта задача «Серия (streak) не даёт множитель к очкам» из `TASKS.md`.
  `GameLevel.tsx`: очки за верный ответ теперь зависят от длины серии —
  каждые 3 верных ответа подряд поднимают множитель на 1 (x1 → x2 → x3 → ...,
  `getStreakMultiplier`/`STREAK_BONUS_STEP`). Всплывающий попап и итоговый
  счёт учитывают реальное начисление, добавлен золотой бейдж `×N` рядом со
  счётчиком серии. `STAR_SCORE_THRESHOLDS` пересчитаны с `4`/`8` на `10`/`20`
  под новый потолок очков идеального прохождения (25 вместо 10). Новых
  зависимостей нет.

## 2026-08-26

- Все секции `TASKS.md` были пусты — добавлены 3 новые задачи (хардкоженная
  надпись «Уровень 1 открыт!», множитель очков за серию (streak), наполнение
  уровня 13) и выполнена первая по приоритету («Баги»). В `LevelSelect.tsx`
  подпись под шапкой экрана выбора уровня была статичным текстом «Всего 30
  уровней. Уровень 1 открыт!», хотя по умолчанию уже разблокированы уровни
  1–11 и это число растёт дальше по ходу игры. Заменено на подсчёт открытых
  уровней из актуального пропа `levels` — «Всего {levels.length} уровней.
  Открыто: {unlockedCount}.». Новых зависимостей нет.

## 2026-08-25

- Закрыта задача «Наполнить уровень 12 («Кто ядовитее?»)» из `TASKS.md`.
  Создан `src/data/level12.ts` — 22 карточки ядовитых животных (змеи, пауки,
  скорпион, кубомедуза, конус, лягушка-древолаз, рыба-камень, утконос и
  другие), подключены в `App.tsx`/`getLevelData` и `src/data/levels.ts`
  (title «Кто ядовитее?», property «Индекс токсичности», unit «баллы»,
  comparisonType `max`, `isUnlocked: false` — открывается через уже
  существующий `UNLOCK_NEXT_LEVEL_STARS` после уровня 11). Значение `value`
  — условный индекс токсичности (0–100), а не точный LD50: опубликованные
  LD50 одного вида сильно расходятся между источниками (путь введения,
  вид подопытных животных, методика лаборатории), и без доступа в интернет
  для сверки заявлять точные мг/кг было бы риском выдумать цифру — порядок
  индекса вместо этого фактчекнут по общеизвестному научному консенсусу об
  опасности каждого вида. Ни у одной карточки нет `imageUri` — подходящих
  локальных фото ядовитых видов в `public/images/` нет, а хотлинки запрещены
  правилами проекта; карточки используют уже существующий emoji-фолбэк.
  Поле `GameItem.imageUri` сделано опциональным в `src/types/index.ts` (та
  же логика уже применялась 2026-08-10). Новых зависимостей нет.

## 2026-08-24

- Добавлен визуальный индикатор прогресса раундов в `GameLevel.tsx`: новый
  самодостаточный компонент `src/components/ui/RoundProgress.tsx` — полоса
  из 10 сегментов (по числу `TOTAL_ROUNDS`), пройденные и текущий раунд
  подсвечены неоновым градиентом cyan→purple (текущий дополнительно мягко
  пульсирует), оставшиеся — приглушённое стекло в стиле проекта. Размещён
  под шапкой уровня, дублирует текстовый счётчик «Задание N/M» визуально —
  игрок видит, сколько раундов осталось, не считая в уме. Добавлены
  `role="progressbar"` и `aria-valuenow`/`aria-valuemin`/`aria-valuemax`
  для доступности. Новых зависимостей нет.

## 2026-08-23

- Все секции `TASKS.md` были пусты — добавлены 3 новые задачи (удаление
  неиспользуемых ассетов, визуальный индикатор прогресса раундов, наполнение
  уровня 12 «Кто ядовитее?») и выполнена первая. Удалены полностью
  неиспользуемые папки `public/animals/` (9 файлов, ~1.1 МБ — остаток от
  версии игры до перехода на `src/data/level1.ts` с путями `/images/*`) и
  `public/level11/` (9 файлов, ~4.5 МБ, часть весила 280–980 КБ — заметно
  выше принятого в проекте лимита ≤150 КБ; актуальный `level11.ts` ссылается
  на переименованные файлы в `public/images/`). Проверено `grep` по `src/` и
  `public/`: ни один путь в коде на эти файлы не ссылался. На GitHub Pages
  это ~5.6 МБ меньше при первом заходе — ощутимо для mobile-first игры.
  Правок кода не потребовалось, новых зависимостей нет.

## 2026-08-22

- Закрыта задача «Расширить уровень 1 с 20 до 30 животных с проверенными
  весами и фактами» из `TASKS.md`. `LEVEL1_DATA` уже содержал 31 запись, но 3
  из 11 добавленных дублировали вид из исходных 20 под другим `id` и с другим
  весом (`tiger` 220 кг против `amur-tiger` 250 кг, `grizzly` 300 кг против
  `grizzly-bear` 350 кг — один вид весил по-разному в зависимости от того,
  какая карточка попадалась), а факты у всех 11 были заглушками в одно
  короткое предложение. Переписан `LEVEL1_ADDITIONAL` в `src/data/level1.ts`:
  убраны 3 дублирующих вида, добавлены гепард и гренландская акула (готовые
  локальные фото уже лежали в `public/images/`, новых ассетов не
  потребовалось) — итог ровно 10 уникальных животных, `LEVEL1_DATA.length`
  теперь ровно 30. Веса перепроверены по реальным диапазонам вида, факты
  переписаны в стиле остальной игры (весовой диапазон + доп. факт,
  1–2 предложения). Новых зависимостей нет.

## 2026-08-21

- Закрыта задача «Уровень 2 «Кто быстрее?»» из `TASKS.md`. Данные уровня 2 уже
  существовали (`src/data/level2.ts`), но не соответствовали формулировке: 25
  записей вместо 20 животных — среди них 5 неживотных (`f1-car`, `passenger-jet`,
  `usain-bolt`, `sound`, `bugatti`), а игровой экран уже был generic-компонентом
  (`GameLevel.tsx`, переиспользуется всеми 11 уровнями через `App.tsx`/
  `getLevelData`), так что этой части задача была выполнена побочно вне процесса.
  Создан `src/data/speeds.ts` — ровно 20 животных с максимальной скоростью в
  км/ч (сапсан 390 км/ч — гепард 120 — … — улитка 0.048 км/ч), отсортированные
  по убыванию скорости как в `src/data/level1.ts`; значения и факты перенесены
  без изменений (уже были правдоподобны), только неживотные записи убраны и
  порядок выровнен. Старый `src/data/level2.ts` удалён, `App.tsx` переключён на
  `SPEEDS_DATA` из нового файла. Отдельный игровой компонент для уровня 2 не
  создавался — `GameLevel` уже общий. Новых зависимостей нет, ассеты не
  менялись (все 20 картинок уже лежали в `public/images/`).

## 2026-08-20

- Закрыта задача «Счётчики в меню» из `TASKS.md`. `starsEarned` в `App.tsx`
  считался через `Math.max(prev.starsEarned, stars)` (максимум звёзд за одно
  прохождение, а не сумма лучших по уровням), `totalScore` суммировал очки
  каждого завершения уровня (рос от перепрохождений, а не отражал сумму
  рекордов) — оба поля вёл независимый аккумулятор, не связанный с per-level
  `stars`/`highScore` в `levels`. Добавлена `computeStats(levels)` в
  `src/lib/storage.ts` — единый источник правды, который пересчитывает
  `stats` из `levels`: `starsEarned` — сумма `stars`, `totalScore` — сумма
  `highScore` по всем уровням. `App.tsx` больше не ведёт `stats` отдельным
  `setState`, значение выводится через `useMemo` от `levels`. Побочно тем же
  способом исправлен `levelsCompleted` (раньше не рос выше `1`). Формат
  `localStorage` не менялся. Новых зависимостей нет.

## 2026-08-19

- Закрыта задача «Картинки животных не грузятся в проде (часть 2 из 2)» из
  `TASKS.md`. Фото для всех 31 животного в `src/data/level1.ts` уже были на
  месте (подтянулось побочно более ранним коммитом «подключить изображения к
  187 карточкам всех уровней»), но 16 файлов в `public/images/` превышали
  лимит задачи ≤150 КБ (весили до ~250 КБ). Пережаты через `sharp`/`mozjpeg`
  (resize внутрь 800×800 без увеличения, понижение JPEG quality с 82 при
  необходимости) — теперь все 100–153 КБ, размеры и формат не изменились.
  `sharp` использован только как одноразовый инструмент сжатия вне проекта
  (установлен в scratchpad, не добавлен в `package.json`) — новых
  зависимостей в репозитории нет.

## 2026-08-18

- Реализовано «Доступность карточек» (см. `TASKS.md`): карточки животных в
  `src/components/GameLevel.tsx` были кликабельным `<div>` без `role`, `tabIndex`
  и обработки клавиатуры. Добавлены `role="button"`, `tabIndex`, обработчик
  Enter/Space и `aria-label` с названием животного, плюс видимый
  `focus-visible`-контур; тот же паттерн применён к карточкам уровней в
  `src/components/LevelSelect.tsx`. Иконочным кнопкам без подписи (назад/выход,
  закрытие модалки, переключатель звука) добавлен `aria-label` в
  `GameLevel.tsx`, `LevelSelect.tsx`, `LevelLockedModal.tsx` и `StartMenu.tsx`;
  кнопки звука дополнительно получили `aria-label`/`aria-pressed`, зависящие от
  состояния `isMuted`, вместо статичной подписи «Звук».
- Задача «Картинки животных не грузятся в проде (часть 2 из 2)» снова
  пропущена в этом прогоне — окружение без доступа в интернет, платная
  AI-генерация требует подтверждения пользователя, которого нет в
  автоматическом запуске.

## 2026-08-17

- Реализовано «Единый источник порогов звёзд» (см. `TASKS.md`): пороги `4`/`8`
  очков были захардкожены дважды в `src/components/GameLevel.tsx` — в расчёте
  `stars` при завершении уровня и отдельно в разметке модалки победы (заливка
  второй и третьей звезды). Добавлены `STAR_SCORE_THRESHOLDS` и
  `getStarsForScore(score)` на уровне модуля, обе точки теперь используют их.
- Задача «Картинки животных не грузятся в проде (часть 2 из 2)» снова
  пропущена в этом прогоне — окружение без доступа в интернет, платная
  AI-генерация требует подтверждения пользователя, которого нет в
  автоматическом запуске.

## 2026-08-16

- Реализовано «Разблокировка уровня 2» (см. `TASKS.md`): `isUnlocked` нигде не
  менялся после завершения уровня. В `src/App.tsx` добавлена константа
  `UNLOCK_NEXT_LEVEL_STARS = 2` — при завершении уровня с ≥2 звёздами следующий
  по `id` уровень теперь открывается автоматически. Решение общее для всех
  переходов между уровнями, не только 1 → 2.
- Задача «Дублирующийся `id: 'gorilla'`» отмечена выполненной без правок кода —
  на момент проверки `LEVEL1_ADDITIONAL` в `src/data/level1.ts` больше не
  содержит гориллу, дублирующихся id во всём файле нет.
- Задача «Картинки животных не грузятся в проде (часть 2 из 2)» снова пропущена
  в этом прогоне — окружение без доступа в интернет, платная AI-генерация
  требует подтверждения пользователя, которого нет в автоматическом запуске.

## 2026-08-15

- Реализовано «Сохранение прогресса» (см. `TASKS.md`): `stats` и `levels` в
  `App.tsx` жили только в `useState` и обнулялись при перезагрузке страницы.
  Добавлен `src/lib/storage.ts` с версионированным ключом localStorage
  (`guesswhat:progress:v1`) — `loadProgress`/`saveProgress` с валидацией
  формы сохранённых данных (откат на дефолты при битом/чужом формате или
  недоступном localStorage) и `mergeLevelsWithProgress`, которая накладывает
  сохранённые звёзды/рекорды/разблокировки на актуальный список уровней по
  `id`. `App.tsx` теперь инициализирует `levels`/`stats` из сохранения и
  сохраняет их при каждом изменении.
- Задача «Опечатка в UI (Скорстные чемпионы)» отмечена выполненной без
  правок кода — на момент проверки строка в `src/data/levels.ts` уже
  корректна («Скоростные чемпионы»), опечатка нигде не найдена.
- В `TASKS.md` заведён новый баг: дублирующийся `id: 'gorilla'` в
  `src/data/level1.ts` (одна и та же горилла с разным весом присутствует и
  в `LEVEL1_DATA`, и в `LEVEL1_ADDITIONAL`) — найден по пути при работе над
  сохранением прогресса, не исправлялся в этом прогоне.
- Задача «Картинки животных, часть 2 из 2» снова пропущена — по-прежнему
  нет доступа в интернет и подтверждения пользователя на платную
  AI-генерацию; заодно в `TASKS.md` обновлены устаревшие пути файлов в
  описании этой задачи (`src/data/animals.ts` → `src/data/level1.ts`,
  `public/animals/` → `public/images/`).

## 2026-08-14

- Исправлен баг «Повторы пар» (см. `TASKS.md`): `generateNewPair` в
  `src/components/GameLevel.tsx` тасовал карточки через
  `sort(() => 0.5 - Math.random())` — смещённая перетасовка без защиты от
  повторов, из-за чего одна и та же пара животных могла выпасть несколько
  раз за сессию. Добавлена честная `fisherYatesShuffle` (`src/lib/utils.ts`)
  и `buildPairQueue`, которая на старте сессии/при рестарте строит
  перетасованную очередь всех уникальных пар (без пар с одинаковым
  значением) и раздаёт их по одной за раунд, пока не закончатся комбинации.
  Задача «Картинки животных, часть 2 из 2» пропущена в этом прогоне —
  требует доступа в интернет или платной AI-генерации с подтверждением
  пользователя, которых нет в автоматическом запуске.

## 2026-08-13

- Исправлена утечка таймера (см. `TASKS.md`): в `src/components/GameLevel.tsx`
  (актуальный путь — компонент из задачи назывался `Level1Game.tsx` до
  переработки на Tailwind + Framer Motion) `setTimeout(..., 1400)` в
  `handleCardClick`, который переводит игру в следующий раунд или завершает
  уровень, не сохранялся и не очищался. Выход в меню во время 1.4-секундной
  анимации ответа оставлял колбэк висеть, и он дёргал `setRound` /
  `generateNewPair` или `onLevelComplete` уже после размонтирования
  компонента. Теперь id таймера хранится в `useRef`, предыдущий таймер
  очищается перед постановкой нового, а `useEffect` с cleanup чистит его
  при размонтировании `GameLevel`.

## 2026-08-12

- Исправлен баг «Ничья по весу» (см. `TASKS.md`): у `grizzly-bear` и `zebra`
  в `src/data/level1.ts` одинаковый `value: 350` кг, из-за чего
  `generateNewPair` в `src/components/GameLevel.tsx` всегда засчитывал
  верным `itemA`, а правильный ответ игрока по `itemB` штрафовался на −2.
  Теперь при совпадении значений в случайной паре функция ищет в
  перетасованном списке следующего кандидата на `itemB` с другим значением,
  прежде чем определять правильный ответ — исправление общее для всех
  уровней. Задача про фото 11 животных (часть 2 из 2) вновь пропущена: она
  требует либо доступа в интернет, либо платной AI-генерации с согласия
  пользователя — ни то, ни другое недоступно в автоматическом прогоне.

## 2026-08-10

- Исправлены картинки животных (часть 1 из 2, см. `TASKS.md`): 9 уже скачанных
  JPEG в `public/animals/` сжаты с ~0.6–1 МБ / 1024×1024 @300 dpi до
  ≤150 КБ / 800×800 @72 dpi; `imageUri` для животных без реального локального
  файла (морж, гребнистый крокодил, африканский буйвол, лось — были битые
  локальные пути; гризли, зебра, амурский тигр, африканский лев, горилла,
  комодский варан, императорский пингвин — были заблокированные хотлинки на
  `images.unsplash.com`) убран из данных; поле `Animal.imageUri` стало
  опциональным (`src/types/index.ts`), карточка теперь сразу показывает
  emoji-фолбэк, если фото нет, вместо неудачной попытки загрузки. Добавление
  реальных фото для оставшихся 11 животных вынесено в отдельную подзадачу.
