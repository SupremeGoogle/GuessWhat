# Changelog

## 2026-09-20

- QA уровня 8 «Кто живет дольше?» (`src/data/level8.ts`, пятый в пачке
  этого прогона 4-5-6-7-8-9-10-11). Картинки: 9 из 19 карточек с
  непустым `imageUri` реально лежат в `public/images/` и отдаются 200;
  для остальных 10 (муха-подёнка, беркут, галапагосская черепаха,
  кролик, шимпанзе, попугай ара, карп кои, гренландский кит,
  океанический куахог, остистая сосна) в `public/images/` подходящих
  файлов не нашлось, оставлен честный emoji-фолбэк. Все 19 значений
  `value` уникальны (от 0.003 у мухи-подёнки до 9999 у «бессмертной»
  медузы — специально завышенное значение-сентинел, чтобы она всегда
  выигрывала сравнение как формально самый долгоживущий организм, при
  `displayValue: 'Бессмертна'` для игрока). `comparisonType: 'max'`
  верен для «кто живёт дольше». Прочитал все 19 `funFact` — правдоподобны
  и без дублей; сверил конкретные цифры (черепаха Мин — 507 лет, карп
  Ханако — 226 лет как факт-курьёз при типичном значении 40 лет для
  вида) — везде корректно. Багов не найдено. Уровень пройден
  по-настоящему в headless Chromium: собран прод-бандл, поднят `npm run
  preview -- --port 4321 --strictPort`, пройдены все 10 раундов, победная
  модалка появилась корректно. Ноль неудачных запросов по картинкам
  уровня; единственная ошибка консоли — заблокированные офлайн-песочницей
  шрифты Google, к уровню не относится.

- QA уровня 7 «Что дороже?» (`src/data/level7.ts`, четвёртый в пачке
  этого прогона 4-5-6-7-8-9-10-11). Картинки: 9 из 20 карточек с
  непустым `imageUri` реально лежат в `public/images/` и отдаются 200;
  для остальных 11 (кофе, ноутбук, Rolex, слиток золота, частный
  самолёт, бутылка воды, билет в кино, видеоигра, новый автомобиль, дом
  в США, F-35) поискал в `public/images/` — единственные похожие по
  смыслу файлы (`passenger_jet_*.jpg`) изображают пассажирский лайнер, а
  не частный самолёт и не истребитель F-35, подставлять их значило бы
  ввести игрока в заблуждение, оставлен честный emoji-фолбэк. Все 20
  значений `value` уникальны (от $0.01 за спичку до $150 млрд за МКС).
  `comparisonType: 'max'` верен для «что дороже». Прочитал все 20
  `funFact` — правдоподобны и без дублей; отдельно сверил неочевидный
  факт про спичку («Спички были изобретены после зажигалок») —
  фактически верно: водородная зажигалка Дёберайнера (1823) появилась
  раньше фрикционных спичек Уокера (1826). Багов не найдено. Уровень
  пройден по-настоящему в headless Chromium: собран прод-бандл, поднят
  `npm run preview -- --port 4321 --strictPort`, пройдены все 10
  раундов, победная модалка появилась корректно. Ноль неудачных
  запросов по картинкам уровня; единственная ошибка консоли —
  заблокированные офлайн-песочницей шрифты Google, к уровню не
  относится.

- QA уровня 6 «Где глубже?» (`src/data/level6.ts`, третий в пачке этого
  прогона 4-5-6-7-8-9-10-11). Картинки: 10 из 17 карточек с непустым
  `imageUri` реально лежат в `public/images/` и отдаются 200; для
  оставшихся 7 (Байкал, рекорд дайвинга, бассейн, пещера Крубера,
  станция «Арсенальная», морской слон, клюворыл Кювье) повторно
  проверил `public/images/` — подходящих неиспользуемых файлов по-прежнему
  нет, оставлен честный emoji-фолбэк (то же покрытие, что и на прошлых
  QA-проходах этого уровня). Все 17 значений `value` уникальны (от 2.5 м
  бассейна до 12,262 м Кольской скважины). `comparisonType: 'max'` верен
  для «где глубже». `displayValue` у всех карточек уже с разделителем
  тысяч (баг с этим был найден и исправлен на предыдущем проходе).
  Прочитал все 17 `funFact` — правдоподобны, без дублей. Багов не
  найдено. Уровень пройден по-настоящему в headless Chromium: собран
  прод-бандл, поднят `npm run preview -- --port 4321 --strictPort`,
  пройдены все 10 раундов, победная модалка появилась корректно. Ноль
  неудачных запросов по картинкам уровня; единственная ошибка консоли —
  заблокированные офлайн-песочницей шрифты Google, к уровню не
  относится.

- QA уровня 5 «Что горячее?» (`src/data/level5.ts`, второй в пачке этого
  прогона 4-5-6-7-8-9-10-11). Картинки: 9 из 17 карточек с непустым
  `imageUri` реально лежат в `public/images/` и отдаются 200; для
  оставшихся 8 (поверхность Солнца, лава, кипящая вода, жидкий азот,
  печь для пиццы, нить накаливания, ядро Солнца, плавление железа) в
  `public/images/` по-прежнему нет подходящих файлов — повторно
  поискал по ключевым словам (sun/lava/boil/nitrogen/pizza/bulb/
  tungsten/iron), совпадений нет, оставлен честный emoji-фолбэк; это уже
  известная крупная находка, заведённая в `TASKS.md` («📚 Контент») при
  QA-проходе 2026-09-15, повторно не дублирую пункт. Все 17 значений
  `value` уникальны (от -273.15 °C у абсолютного нуля до 15 000 000 °C у
  ядра Солнца), `comparisonType: 'max'` верен для «что горячее».
  Прочитал все 17 `funFact` — правдоподобны и без дублей (в т.ч.
  сверил цифры: точка кипения воды на Эвересте ≈68 °C, молния примерно
  в 5 раз горячее фотосферы Солнца — оба утверждения корректны). Багов
  не найдено. Уровень пройден по-настоящему в headless Chromium: собран
  прод-бандл, поднят `npm run preview -- --port 4321 --strictPort`,
  пройдены все 10 раундов, победная модалка появилась корректно. Ноль
  неудачных запросов по картинкам уровня; единственная ошибка консоли —
  заблокированные офлайн-песочницей шрифты Google, к уровню не
  относится.

- QA уровня 4 «Что больше?» (`src/data/level4.ts`, первый в пачке этого
  прогона: `lastReviewedLevelId` был 3, случайный размер пачки — 8,
  берутся уровни 4-5-6-7-8-9-10-11). Картинки: все 15 карточек с
  непустым `imageUri` реально лежат в `public/images/` и отдаются 200 (не
  только по факту наличия файла — проверено ещё раз headless-браузером,
  ноль 404 по сети); оставшиеся 6 карточек (теннисный корт, дорожка для
  боулинга, олимпийский бассейн, вагон поезда, авианосец, подводная
  лодка) намеренно без картинки — в `public/images/` подходящих по
  смыслу файлов не нашлось, оставлен честный emoji-фолбэк. Значения
  `value` разнообразны (от 14 м у школьного автобуса до 21196000 м —
  21196 км — у Великой Китайской стены), пар с совпадающим `value` нет.
  Прочитал все 20 `funFact` — правдоподобны, без дублей и выдуманных
  цифр; `title`/`subtitle`/`property`/`unit`/`comparisonType: 'max'` в
  `levels.ts` соответствуют смешанному набору «высота/длина». Багов не
  найдено. Уровень пройден по-настоящему в headless Chromium (глобально
  установленный Playwright 1.56.1, без интернета): собран прод-бандл,
  поднят `npm run preview -- --port 4321 --strictPort`, через уровень
  пройдены все 10 раундов кликами по карточкам, победная модалка
  появилась корректно. Ноль неудачных запросов по картинкам уровня;
  единственная ошибка консоли — заблокированные офлайн-песочницей шрифты
  Google, к уровню не относится.

## 2026-09-19

- QA уровня 3 «Что древнее?» (`src/data/level3.ts`, шестой и последний в
  пачке этого прогона: пачка 13-14-15-1-2-3, размер выпал случайно — 6).
  Картинки: 19 из 24 карточек с непустым `imageUri` реально лежат в
  `public/images/` и отдаются 200; для 5 карточек без картинки (печатный
  станок, первый кинофильм, компас, телескоп, паровой двигатель) в
  `public/images/` подходящих файлов не нашлось — единственная похожая
  находка `jet_engine_*.jpg` оказалась современным реактивным двигателем,
  а не паровым 1712 года, подставлять её значило бы ввести игрока в
  заблуждение, оставлен честный emoji-фолбэк. Все 24 значения `value`
  уникальны, включая умную дробную часть у событий одного года (Интернет
  1969.83 против высадки на Луну 1969.55 — октябрь позже июля; первый
  кинофильм 1895.99 против радио 1895.66 — декабрь позже лета), что
  корректно определяет верный ответ даже при совпадающем отображаемом
  годе. `comparisonType: 'min'` верен для «что древнее» (более
  отрицательный/меньший год — древнее событие). Прочитал все 24
  `funFact` — правдоподобны, дублей нет. Багов не найдено. Уровень
  пройден по-настоящему в headless Chromium (глобально установленный
  Playwright 1.56.1, без интернета): собран прод-бандл, поднят `npm run
  preview -- --port 4321 --strictPort`, пройдены все 10 раундов кликами
  по объективно более древнему событию в каждой паре (клик сверялся с
  реальными `value` уровня, включая дробную часть, а после — с тем, что
  игра подсветила именно эту карточку как верную), финальный счёт на
  финише — 25 очков (10/10 верных ответов), победная модалка появилась
  корректно. Ноль неудачных запросов по картинкам уровня; единственные
  ошибки консоли — те же заблокированные офлайн-песочницей шрифты
  Google, к уровню не относятся. Этим уровнем пачка 13-14-15-1-2-3 этого
  прогона завершена.

- QA уровня 2 «Кто быстрее?» (`src/data/speeds.ts`, пятый в пачке этого
  прогона: пачка 13-14-15-1-2-3, размер — 6). Уровень уже проверялся
  ранее (2026-09-07) — известная находка про однословные `funFact`
  («Быстро бросаются на добычу.» и т.п., заметно беднее остальных
  уровней) уже заведена отдельным пунктом в `TASKS.md` («📚 Контент»),
  за этот прогон её закрывать не пытался, как и требуют правила для
  крупных находок. Картинки: все 20 карточек с непустым `imageUri`
  реально лежат в `public/images/` — уровень полностью проиллюстрирован.
  Разброс `value`: 16 уникальных значений из 20 (пары с равным значением
  — грейхаунд/лось по 72, койот/зебра/верблюд по 65, жираф/дельфин по
  60 — исключаются из игры автоматически, но из 190 возможных пар
  остаётся 185 различимых, заметного обеднения нет). Дублей `funFact` не
  найдено. `comparisonType: 'max'` верен для «кто быстрее». Новых багов
  не найдено. Уровень пройден по-настоящему в headless Chromium
  (глобально установленный Playwright 1.56.1, без интернета): собран
  прод-бандл, поднят `npm run preview -- --port 4321 --strictPort`,
  пройдены все 10 раундов кликами по объективно более быстрому животному
  в каждой паре (клик сверялся с реальными `value` уровня, а после — с
  тем, что игра подсветила именно эту карточку как верную), финальный
  счёт на финише — 25 очков (10/10 верных ответов), победная модалка
  появилась корректно. Ноль неудачных запросов по картинкам уровня;
  единственные ошибки консоли — те же заблокированные офлайн-песочницей
  шрифты Google, к уровню не относятся.

- QA уровня 1 «Кто тяжелее?» (`src/data/level1.ts` + `LEVEL1_ADDITIONAL`,
  четвёртый в пачке этого прогона: пачка 13-14-15-1-2-3, размер — 6).
  Уровень уже неоднократно проверялся в предыдущих прогонах (см. более
  ранние записи этого файла) — очередная сверка не нашла новых проблем.
  Картинки: все 30 карточек с непустым `imageUri` реально лежат в
  `public/images/` — уровень полностью проиллюстрирован, чинить нечего.
  Разброс `value` отличный (4 кг домашняя кошка — 150000 кг синий кит),
  все 30 значений уникальны. Прочитал все 30 `funFact` — правдоподобны,
  дублей нет. `comparisonType: 'max'` верен для «кто тяжелее». Багов не
  найдено. Уровень пройден по-настоящему в headless Chromium (глобально
  установленный Playwright 1.56.1, без интернета): собран прод-бандл,
  поднят `npm run preview -- --port 4321 --strictPort`, пройдены все 10
  раундов кликами по объективно более тяжёлому животному в каждой паре
  (клик сверялся с реальными `value` уровня, а после — с тем, что игра
  подсветила именно эту карточку как верную), финальный счёт на финише —
  25 очков (10/10 верных ответов), победная модалка появилась корректно.
  Ноль неудачных запросов по картинкам уровня; единственные ошибки
  консоли — те же заблокированные офлайн-песочницей шрифты Google, к
  уровню не относятся.

- QA уровня 15 «Кто спит дольше?» (`src/data/level15.ts`, третий в пачке
  этого прогона: пачка 13-14-15-1-2-3, размер — 6). Картинки: 11 из 20
  карточек с непустым `imageUri` реально лежат в `public/images/` и
  отдаются 200; для 9 карточек без картинки (коала, бурая ночница, суслик,
  золотистый хомячок, макака-резус, кролик, свинья, коза, овца) в
  `public/images/` подходящих файлов не нашлось (проверил по ключевым
  словам koala/bat/squirrel/hamster/macaque/monkey/rabbit/pig/goat/sheep)
  — оставлен честный emoji-фолбэк. Разброс `value` отличный (1.9 ч жираф —
  20 ч коала), все 20 значений уникальны. `comparisonType: 'max'` верен
  для «кто спит дольше». Прочитал все 20 `funFact` — правдоподобны, дублей
  нет; для ленивца отдельно оговорено в комментарии файла и в самом факте,
  что цифра — по наблюдениям в неволе, а не универсальная. Багов не
  найдено. Уровень пройден по-настоящему в headless Chromium (глобально
  установленный Playwright 1.56.1, без интернета): собран прод-бандл,
  поднят `npm run preview -- --port 4321 --strictPort`, прогресс в
  `localStorage` предзаполнен, чтобы разблокировать уровень 15, пройдены
  все 10 раундов кликами по объективно дольше спящему виду в каждой паре
  (клик сверялся с реальными `value` уровня, а после — с тем, что игра
  подсветила именно эту карточку как верную), финальный счёт на финише —
  25 очков (10/10 верных ответов), победная модалка появилась корректно.
  Ноль неудачных запросов по картинкам уровня; единственные ошибки
  консоли — те же заблокированные офлайн-песочницей шрифты Google, к
  уровню не относятся.

- QA уровня 14 «У кого крылья шире?» (`src/data/level14.ts`, второй в
  пачке этого прогона: пачка 13-14-15-1-2-3, размер — 6). Картинки: 2 из
  20 карточек (сапсан, африканский страус) переиспользуют уже загруженные
  фото уровней 2 и 11 и реально отдаются 200; для остальных 18 птиц в
  `public/images/` подходящих по смыслу файлов нет (проверил по ключевым
  словам — eagle/owl/swan/condor/vulture/stork/pelican/albatross/heron/
  crane/raven/sparrow/pigeon и т.д., совпадений не нашлось) — оставлен
  честный emoji-фолбэк. Разброс `value` отличный (10 см колибри — 340 см
  странствующий альбатрос), все 20 значений уникальны.
  `comparisonType: 'max'` верен для «у кого крылья шире». Прочитал все 20
  `funFact` — правдоподобны, дублей нет. Багов не найдено. Уровень пройден
  по-настоящему в headless Chromium (глобально установленный Playwright
  1.56.1, без интернета): собран прод-бандл, поднят `npm run preview --
  --port 4321 --strictPort`, прогресс в `localStorage` предзаполнен, чтобы
  разблокировать уровень 14, пройдены все 10 раундов кликами по
  объективно более широкому размаху крыльев в каждой паре (клик
  автоматически сверялся с реальными `value` уровня, а после — с тем, что
  игра подсветила именно эту карточку как верную), финальный счёт на
  финише — 25 очков (10/10 верных ответов), победная модалка появилась
  корректно. Ноль неудачных запросов по картинкам уровня; единственные
  ошибки консоли — те же заблокированные офлайн-песочницей шрифты Google,
  к уровню не относятся.

- QA уровня 13 «Кто дольше вынашивает?» (`src/data/level13.ts`, первый в
  пачке этого прогона: следующий после уровня 12, размер пачки выпал
  случайно — 6, пачка 13-14-15-1-2-3). Картинки: 14 из 20 карточек с
  непустым `imageUri` реально лежат в `public/images/` и отдаются 200 (файл
  проверен и на диске, и через `requestfailed`/`response`-хуки headless
  Chromium — ни одного 404 по картинкам уровня). Оставшиеся 6 карточек без
  картинки (осёл, коза, овца, свинья, кролик, сирийский хомяк) — честно
  проверил `public/images/` на предмет подходящих по смыслу файлов, ни
  одного не нашлось (только `mouse_...jpg`, но это домовая мышь другого
  вида, уже занятая под `house_mouse` — подставлять её под хомяка значило
  бы ввести игрока в заблуждение), оставлен emoji-фолбэк. Разброс `value`
  отличный (16 дней сирийский хомяк — 660 дней африканский слон), все 20
  значений уникальны. Прочитал все 20 `funFact` — правдоподобны, дублей
  нет, числа (например, «три месяца, три недели и три дня» у свиньи)
  совпадают с `value`. `comparisonType: 'max'` верен для «кто дольше
  вынашивает». Багов не найдено. Уровень пройден по-настоящему в headless
  Chromium (глобально установленный Playwright 1.56.1 из `/opt/pw-browsers`,
  без интернета): собран прод-бандл, поднят `npm run preview -- --port 4321
  --strictPort` (сайт раздаётся с базового пути `/GuessWhat/`, а не с корня
  — корень отдаёт 302 на `/GuessWhat/`), прогресс в `localStorage`
  предзаполнен скриптом инициализации страницы, чтобы разблокировать
  уровень 13 (по умолчанию открывается только после уровня 12), пройдены
  все 10 раундов кликами по объективно более длительному сроку
  беременности в каждой паре (клик сверялся с реальными `value` уровня, а
  не наугад — после каждого клика скрипт проверял, что именно эта карточка
  подсвечена игрой как верная), финальный счёт на финише — 25 очков, что
  точно совпадает с формулой стриков при 10/10 верных ответов, победная
  модалка появилась корректно. Ноль неудачных запросов по картинкам
  уровня; единственные ошибки консоли — `ERR_CERT_AUTHORITY_INVALID` на
  `fonts.googleapis.com`/`fonts.gstatic.com`, заблокированных политикой
  офлайн-песочницы, к уровню не относятся.

## 2026-09-18

- QA уровня 12 «Кто ядовитее?» (`src/data/level12.ts`, седьмой и
  последний в пачке этого прогона: пачка 6-7-8-9-10-11-12, размер
  пачки — 7). Уровень уже неоднократно проверялся в предыдущих прогонах
  (см. более ранние записи этого файла) — очередная сверка не нашла
  новых проблем. Картинок нет ни у одной из 22 карточек — это
  осознанное решение, задокументированное прямо в файле (блок-комментарий
  в начале `level12.ts`): подходящих локальных фото ядовитых видов в
  `public/images/` по-прежнему нет, внешние хотлинки запрещены. Заново
  проверил `public/images/` на предмет новых подходящих файлов — из
  относящихся к теме нашёлся только `immortal_jellyfish_*.jpg`, но это
  другой вид (бессмертная медуза Turritopsis, уже используется в
  уровне 8), а не морская оса (Chironex fleckeri) из этой карточки —
  подставлять фото не того вида было бы введением игрока в заблуждение,
  оставлен честный emoji-фолбэк. Все 22 значения индекса токсичности
  уникальны (6–99 баллов), `comparisonType: 'max'` верен для «кто
  ядовитее», факты сверены на правдоподобность и дублей не найдено.
  Багов не найдено. Уровень пройден по-настоящему в headless Chromium
  (глобально установленный Playwright 1.56.1, без интернета): прогресс
  в `localStorage` временно предзаполнен скриптом инициализации
  страницы, чтобы разблокировать уровень для QA (по умолчанию открывается
  только после уровня 11), собран прод-бандл, поднят `npm run preview --
  --port 4321 --strictPort`, пройдены все 10 раундов кликами по
  фактически более ядовитому виду в каждой паре (сверено с данными
  уровня), финальный счёт на финише — 25 очков, что точно совпадает с
  формулой стриков при 10/10 верных ответов, победная модалка появилась
  корректно. Ноль ошибок консоли и ноль неудачных сетевых запросов
  (единственный неудачный запрос — блокировка `fonts.googleapis.com`
  политикой офлайн-песочницы, не связана с уровнем). Этим уровнем пачка
  6-7-8-9-10-11-12 этого прогона завершена.

- QA уровня 11 «Что выше?» (`src/data/level11.ts`, шестой в пачке этого
  прогона: пачка 6-7-8-9-10-11-12, размер выпал случайно — 7). Картинки:
  все 25 карточек с непустым `imageUri` реально лежат в
  `public/images/` и отдаются 200 через preview-сервер — уровень
  полностью проиллюстрирован, чинить нечего. Разброс `value` отличный
  (38 м статуя Христа-Искупителя — 21900 м марсианский Олимп), все 25
  значений уникальны. Факты сверены на правдоподобность, дублей не
  найдено. `comparisonType: 'max'` верен для «что выше». Багов не
  найдено. Уровень пройден по-настоящему в headless Chromium (глобально
  установленный Playwright 1.56.1, без интернета): собран прод-бандл,
  поднят `npm run preview -- --port 4321 --strictPort`, пройдены все 10
  раундов кликами по фактически более высокому объекту в каждой паре
  (сверено с данными уровня), финальный счёт на финише — 25 очков, что
  точно совпадает с формулой стриков при 10/10 верных ответов, победная
  модалка появилась корректно. Ноль ошибок консоли и ноль неудачных
  сетевых запросов по картинкам уровня (единственный неудачный запрос —
  блокировка `fonts.googleapis.com` политикой офлайн-песочницы, не
  связана с уровнем).

- QA уровня 10 «Что калорийнее?» (`src/data/level10.ts`, пятый в пачке
  этого прогона: пачка 6-7-8-9-10-11-12, размер выпал случайно — 7).
  Картинки: 20 из 21 карточки с непустым `imageUri` реально лежат в
  `public/images/` и отдаются 200 через preview-сервер — уровень уже
  почти полностью проиллюстрирован. Единственная карточка без картинки
  (куриная грудка) — проверил `public/images/` целиком, файла под курицу
  нет, оставлен честный emoji-фолбэк. Разброс `value` отличный (15–900
  ккал), все 21 значение уникальны. Факты сверены на правдоподобность,
  дублей не найдено. `comparisonType: 'max'` верен для «что
  калорийнее». Багов не найдено. Уровень пройден по-настоящему в
  headless Chromium (глобально установленный Playwright 1.56.1, без
  интернета): собран прод-бандл, поднят `npm run preview -- --port 4321
  --strictPort`, пройдены все 10 раундов кликами по фактически более
  калорийному продукту в каждой паре (сверено с данными уровня),
  финальный счёт на финише — 25 очков, что точно совпадает с формулой
  стриков при 10/10 верных ответов, победная модалка появилась
  корректно. Ноль ошибок консоли и ноль неудачных сетевых запросов по
  картинкам уровня (единственный неудачный запрос — блокировка
  `fonts.googleapis.com` политикой офлайн-песочницы, не связана с
  уровнем).

- QA уровня 9 «Что громче?» (`src/data/level9.ts`, четвёртый в пачке
  этого прогона: пачка 6-7-8-9-10-11-12, размер выпал случайно — 7).
  Картинки: 3 из 19 карточек с непустым `imageUri` реально лежат в
  `public/images/` и отдаются 200 через preview-сервер (двигатель
  самолёта, раскат грома, светошумовая граната). Для 16 карточек без
  картинки проверил `public/images/` целиком — нашёлся неиспользуемый
  файл `sound_1786533603314.jpg` (самолёт, преодолевающий звуковой
  барьер), но он не соответствует ни одному из оставшихся пунктов уровня
  (нет отдельной карточки про звуковой удар/сверхзвук), поэтому не
  подставлен — оставлен честный emoji-фолбэк для всех 16. Разброс
  `value` отличный (20–500 дБ), все 19 значений уникальны. Факты сверены
  на правдоподобность, дублей не найдено (гром, светошумовая граната,
  Тунгусский метеорит и т.д.) — значения 300 дБ у Тунгусского метеорита и
  условное 500 у столкновения чёрных дыр не заявлены как измеренные
  децибелы в самих фактах, а служат игровой шкалой сравнения (аналогично
  «Бессмертна» у медузы в уровне 8), это в порядке вещей для уровня.
  `comparisonType: 'max'` верен для «что громче». Багов не найдено.
  Уровень пройден по-настоящему в headless Chromium (глобально
  установленный Playwright 1.56.1, без интернета): собран прод-бандл,
  поднят `npm run preview -- --port 4321 --strictPort`, пройдены все 10
  раундов кликами по фактически более громкому источнику звука в каждой
  паре (сверено с данными уровня), финальный счёт на финише — 25 очков,
  что точно совпадает с формулой стриков при 10/10 верных ответов,
  победная модалка появилась корректно. Ноль ошибок консоли и ноль
  неудачных сетевых запросов по картинкам уровня (единственный
  неудачный запрос — блокировка `fonts.googleapis.com` политикой
  офлайн-песочницы, не связана с уровнем).

- QA уровня 8 «Кто живет дольше?» (`src/data/level8.ts`, третий в пачке
  этого прогона: пачка 6-7-8-9-10-11-12, размер выпал случайно — 7).
  Картинки: 9 из 19 карточек с непустым `imageUri` реально лежат в
  `public/images/` и отдаются 200 через preview-сервер (домашняя мышь,
  собака, лев, лошадь, африканский слон, человек, гренландская акула,
  секвойя, бессмертная медуза). Для 10 карточек без картинки (муха-
  подёнка, беркут, галапагосская черепаха, кролик, шимпанзе, попугай ара,
  карп кои, гренландский кит, океанический куахог, остистая сосна)
  проверил `public/images/` целиком — подходящих неиспользуемых файлов не
  нашлось, оставлен честный emoji-фолбэк. Разброс `value` отличный — от
  0.003 года (муха-подёнка, ~1 день) до 9999 (условное значение для
  «бессмертной» медузы, специально выше всех остальных, чтобы она
  логично выигрывала в сравнении «кто живёт дольше»), все 19 значений
  уникальны. Факты сверены на правдоподобность (карп Ханако — 226 лет,
  моллюск Мин — 507 лет и т.д.), дублей не найдено. `comparisonType:
  'max'` верен для «кто живёт дольше». Багов не найдено. Уровень пройден
  по-настоящему в headless Chromium (глобально установленный Playwright
  1.56.1, без интернета): собран прод-бандл, поднят `npm run preview --
  --port 4321 --strictPort`, пройдены все 10 раундов кликами по
  фактически более долгоживущему существу в каждой паре (сверено с
  данными уровня), финальный счёт на финише — 25 очков, что точно
  совпадает с формулой стриков при 10/10 верных ответов, победная
  модалка появилась корректно. Ноль ошибок консоли и ноль неудачных
  сетевых запросов по картинкам уровня (единственный неудачный запрос —
  блокировка `fonts.googleapis.com` политикой офлайн-песочницы, не
  связана с уровнем).

- QA уровня 7 «Что дороже?» (`src/data/level7.ts`, второй в пачке этого
  прогона: пачка 6-7-8-9-10-11-12, размер выпал случайно — 7). Картинки: 9
  из 20 карточек с непустым `imageUri` реально лежат в `public/images/` и
  отдаются 200 через preview-сервер (коробок спичек, флагманский
  смартфон, Ferrari, Мона Лиза, МКС, одна спичка, Бугатти Широн, Бурдж
  Халифа, телескоп Хаббл). Для 11 карточек без картинки (кофе, ноутбук,
  Rolex, слиток золота, частный самолёт, бутылка воды, билет в кино,
  видеоигра, новый автомобиль, дом в США, истребитель F-35) проверил
  `public/images/` целиком — подходящих неиспользуемых файлов не нашлось
  (например, `vodka_*.jpg` не подходит для «бутылки воды» — это другой
  напиток, подставлять не по теме нельзя), оставлен честный
  emoji-фолбэк. Разброс `value` отличный — от $0.01 (спичка) до $150 млрд
  (МКС), все 20 значений уникальны. Факты сверены на правдоподобность,
  дублей не найдено. `comparisonType: 'max'` верен для «что дороже».
  Нашёл и исправил мелкий баг форматирования: у карточки «Новый
  автомобиль» `displayValue` был без разделителя тысяч («$40000») в
  отличие от остальных карточек уровня — приведено к «$40,000». Уровень
  пройден по-настоящему в headless Chromium (глобально установленный
  Playwright 1.56.1, без интернета): собран прод-бандл, поднят `npm run
  preview -- --port 4321 --strictPort`, пройдены все 10 раундов кликами
  по фактически более дорогому объекту в каждой паре (сверено с данными
  уровня), финальный счёт на финише — 25 очков, что точно совпадает с
  формулой стриков при 10/10 верных ответов, победная модалка появилась
  корректно. Ноль ошибок консоли и ноль неудачных сетевых запросов по
  картинкам уровня (единственный неудачный запрос — блокировка
  `fonts.googleapis.com` политикой офлайн-песочницы, не связана с уровнем).

- QA уровня 6 «Где глубже?» (`src/data/level6.ts`, первый в пачке этого
  прогона: пачка 6-7-8-9-10-11-12, размер выпал случайно — 7). Картинки: 10
  из 17 карточек с непустым `imageUri` реально лежат в `public/images/` и
  отдаются 200 через preview-сервер (Марианская впадина, обломки Титаника,
  Кольская скважина, Гранд-Каньон, нырок кашалота, высота Бурдж-Халифы для
  масштаба, погружение синего кита, императорский пингвин, кожистая
  черепаха, шахта Мпоненг). Для 7 карточек без картинки (Байкал, рекорд
  дайвинга, бассейн, пещера Крубера, станция «Арсенальная», морской слон,
  клюворыл Кювье) проверил `public/images/` целиком — подходящих
  неиспользуемых файлов не нашлось, оставлен честный emoji-фолбэк. Разброс
  `value` хороший (от 2.5 м бассейна до 12,262 м Кольской скважины), все 17
  значений уникальны — пар для исключения из-за совпадения нет. Факты
  сверены на правдоподобность, дублей не найдено. `comparisonType: 'max'`
  верен для «где глубже». Нашёл и исправил мелкий баг форматирования: у
  4 карточек (кожистая черепаха, морской слон, клюворыл Кювье, шахта
  Мпоненг) `displayValue` был без разделителя тысяч («1280 м», «2388 м»,
  «2992 м», «4000 м»), в отличие от остальных карточек уровня — приведено к
  единому виду («1,280 м», «2,388 м», «2,992 м», «4,000 м»). Уровень
  пройден по-настоящему в headless Chromium (глобально установленный
  Playwright 1.56.1, без интернета): собран прод-бандл, поднят `npm run
  preview -- --port 4321 --strictPort`, пройдены все 10 раундов кликами по
  фактически более глубокому объекту в каждой паре (сверено с данными
  уровня), финальный счёт на финише — 25 очков, что точно совпадает с
  формулой стриков при 10/10 верных ответов, победная модалка появилась
  корректно. Ноль ошибок консоли и ноль неудачных сетевых запросов по
  картинкам уровня (единственный неудачный запрос — блокировка
  `fonts.googleapis.com` политикой офлайн-песочницы, не связана с уровнем).

## 2026-09-17

- QA уровня 5 «Что горячее?» (`src/data/level5.ts`, седьмой и последний в
  пачке этого прогона: уровни 14-15-1-2-3-4-5, размер пачки — 7). Картинки:
  9 из 17 карточек с непустым `imageUri` по-прежнему реально лежат в
  `public/images/` и отдаются 200 через preview-сервер. Для 8 карточек без
  картинки (поверхность Солнца, вулканическая лава, кипящая вода, жидкий
  азот, печь для пиццы, нить накаливания, ядро Солнца, плавление железа)
  заново просмотрел весь `public/images/` — подходящих файлов по-прежнему
  нет, найденное ранее (QA 2026-09-15) уже отражено открытым пунктом в
  `TASKS.md` («Найти или подготовить картинки для уровня 5»), повторно не
  дублирую. Все 17 значений температуры уникальны (-273.15 °C абсолютный
  нуль — 15 млн °C ядро Солнца), пар для сравнения хватает.
  `comparisonType: 'max'` верен для «что горячее». Багов не найдено.
  Уровень пройден по-настоящему в headless Chromium (глобально
  установленный Playwright 1.56.1, без интернета): собран прод-бандл,
  поднят `npm run preview -- --port 4321 --strictPort`, прогресс в
  `localStorage` предзаполнен скриптом инициализации страницы; на каждом
  из 10 раундов имя и число карточек считаны из DOM и сверены с `value`
  из `level5.ts` перед кликом — пройдены все 10 раундов, счёт на финише
  25 очков (идеальные 10/10), победная модалка появилась корректно. Ноль
  ошибок консоли и ноль неудачных сетевых запросов по картинкам уровня
  (единственный неудачный запрос — блокировка `fonts.googleapis.com`
  политикой офлайн-песочницы, не связана с уровнем). Этим уровнем пачка
  14-15-1-2-3-4-5 этого прогона завершена.

- QA уровня 4 «Что больше?» (`src/data/level4.ts`, шестой в пачке этого
  прогона: уровни 14-15-1-2-3-4-5, размер пачки — 7). Картинки: 15 из 21
  карточек с непустым `imageUri` реально лежат в `public/images/` и
  отдаются 200 через preview-сервер. Для 6 карточек без картинки
  (теннисный корт, дорожка для боулинга, олимпийский бассейн, вагон
  поезда, авианосец, подводная лодка) просмотрел весь `public/images/` —
  подходящих неиспользуемых файлов не нашлось, оставлен честный
  emoji-фолбэк. Все 21 значение длины/высоты уникальны (14 м школьный
  автобус — 21 196 км Великая Китайская стена), пар для сравнения хватает.
  Факты сверены на правдоподобность, дублей нет, `comparisonType: 'max'`
  верен для «что больше». Багов не найдено. Уровень пройден по-настоящему
  в headless Chromium (глобально установленный Playwright 1.56.1, без
  интернета): собран прод-бандл, поднят `npm run preview -- --port 4321
  --strictPort`, прогресс в `localStorage` предзаполнен скриптом
  инициализации страницы; на каждом из 10 раундов имя и число карточек
  считаны из DOM и сверены с `value` из `level4.ts` перед кликом — пройдены
  все 10 раундов, счёт на финише 25 очков (идеальные 10/10), победная
  модалка появилась корректно. Ноль ошибок консоли и ноль неудачных
  сетевых запросов по картинкам уровня (единственный неудачный запрос —
  блокировка `fonts.googleapis.com` политикой офлайн-песочницы, не связана
  с уровнем).

- QA уровня 3 «Что древнее?» (`src/data/level3.ts`, пятый в пачке этого
  прогона: уровни 14-15-1-2-3-4-5, размер пачки — 7). Картинки: 19 из 24
  карточек с непустым `imageUri` по-прежнему реально лежат в
  `public/images/` и отдаются 200 через preview-сервер. Для 5 карточек без
  картинки (печатный станок, первый кинофильм, компас, телескоп, паровой
  двигатель) заново просмотрел весь `public/images/` — файлов под
  гутенберговский станок, синематограф, компас, телескоп или паровоз по-
  прежнему нет, оставлен честный emoji-фолбэк. Все 24 значения года/эпохи
  уникальны (в т.ч. дробная часть года у Луны/интернета/кино/радио — сверил,
  соответствует реальным датам в пределах года), `comparisonType: 'min'`
  верен для «что древнее». Багов не найдено. Уровень пройден по-настоящему
  в headless Chromium (глобально установленный Playwright 1.56.1, без
  интернета): собран прод-бандл, поднят `npm run preview -- --port 4321
  --strictPort`, прогресс в `localStorage` предзаполнен скриптом
  инициализации страницы; на каждом из 10 раундов имя и число карточек
  считаны из DOM и сверены с `value` из `level3.ts` перед кликом — пройдены
  все 10 раундов, счёт на финише 25 очков (идеальные 10/10), победная
  модалка появилась корректно. Ноль ошибок консоли и ноль неудачных сетевых
  запросов по картинкам уровня (единственный неудачный запрос — блокировка
  `fonts.googleapis.com` политикой офлайн-песочницы, не связана с уровнем).

- QA уровня 2 «Кто быстрее?» (`src/data/speeds.ts`, четвёртый в пачке
  этого прогона: уровни 14-15-1-2-3-4-5, размер пачки — 7; уровень уже
  проверялся в прошлых прогонах, но каждый цикл перепроверяет данные
  заново, чтобы ловить регрессии). Картинки: все 20 карточек с непустым
  `imageUri` по-прежнему реально лежат в `public/images/` и отдаются 200
  через preview-сервер. Разброс `value` тот же, что и раньше (390 км/ч
  сапсан — 0.048 км/ч улитка), с теми же 5 исключёнными из 190 парами
  из-за совпадений (грейхаунд/лось 72, койот/зебра/верблюд 65, жираф/
  дельфин 60) — на разнообразие раундов не влияет, отдельного пункта в
  TASKS.md не заводил (как и в прошлых QA этого уровня). Факты и
  `comparisonType: 'max'` по-прежнему верны. Багов не найдено. Уровень
  пройден по-настоящему в headless Chromium (глобально установленный
  Playwright 1.56.1, без интернета): собран прод-бандл, поднят `npm run
  preview -- --port 4321 --strictPort`, прогресс в `localStorage`
  предзаполнен скриптом инициализации страницы; на каждом из 10 раундов
  имя и число карточек считаны из DOM и сверены с `value` из `speeds.ts`
  перед кликом — пройдены все 10 раундов, счёт на финише 25 очков
  (идеальные 10/10), победная модалка появилась корректно. Ноль ошибок
  консоли и ноль неудачных сетевых запросов по картинкам уровня
  (единственный неудачный запрос — блокировка `fonts.googleapis.com`
  политикой офлайн-песочницы, не связана с уровнем).

- QA уровня 1 «Кто тяжелее?» (`src/data/level1.ts` + `LEVEL1_ADDITIONAL`,
  третий в пачке этого прогона: уровни 14-15-1-2-3-4-5, размер пачки — 7,
  цикл дошёл до конца списка уровней и продолжился с начала). Картинки:
  все 30 карточек с непустым `imageUri` (от синего кита до домашней кошки
  и гренландской акулы) реально лежат в `public/images/` и раздаются
  `npm run preview` с кодом 200 — пустых `imageUri` на уровне нет. Все 30
  значений массы уникальны (4 кг кошка — 150 000 кг синий кит), пар для
  сравнения более чем достаточно. Факты сверены на правдоподобность,
  дублей между карточками нет (ранее устранённые дубли видов «lion»/
  «african-lion», «tiger»/«amur-tiger», «grizzly»/«grizzly-bear» через
  `LEVEL1_ADDITIONAL` уже не повторяются), `comparisonType: 'max'` верен
  для «кто тяжелее». Багов не найдено. Уровень пройден по-настоящему в
  headless Chromium (глобально установленный Playwright 1.56.1, без
  интернета): собран прод-бандл, поднят `npm run preview -- --port 4321
  --strictPort`, прогресс в `localStorage` предзаполнен скриптом
  инициализации страницы; на каждом из 10 раундов имя и число карточек
  считаны из DOM и сверены с `value` из `level1.ts` перед кликом — пройдены
  все 10 раундов, счёт на финише 25 очков (идеальные 10/10), победная
  модалка появилась корректно. Ноль ошибок консоли и ноль неудачных
  сетевых запросов по картинкам уровня (единственный неудачный запрос —
  блокировка `fonts.googleapis.com` политикой офлайн-песочницы, не связана
  с уровнем).

- QA уровня 15 «Кто спит дольше?» (`src/data/level15.ts`, второй в пачке
  этого прогона: уровни 14-15-1-2-3-4-5, размер пачки — 7). Картинки: все
  11 карточек с непустым `imageUri` (тигр, лев, ленивец, домовая мышь,
  кошка, собака, человек, корова, лошадь, африканский слон, жираф —
  переиспользуют ассеты уровней 1/8/10/12/13) реально лежат в
  `public/images/` и раздаются `npm run preview` с кодом 200. Для
  остальных 9 карточек (коала, бурая ночница, суслик, золотистый хомячок,
  макака-резус, кролик, свинья, коза, овца) весь `public/images/`
  просмотрен целиком — подходящих неиспользуемых фото не нашлось, оставлен
  честный emoji-фолбэк. Все 20 значений продолжительности сна уникальны
  (1.9 ч жираф — 20 ч коала), пар для сравнения хватает. Факты сверены на
  правдоподобность (в т.ч. честно помечено в `funFact` ленивца, что оценка
  сна — по наблюдениям в неволе, а не в дикой природе), дублей между
  карточками нет, `comparisonType: 'max'` верен для «кто спит дольше».
  Багов не найдено. Уровень пройден по-настоящему в headless Chromium
  (глобально установленный Playwright 1.56.1, без интернета): собран
  прод-бандл, поднят `npm run preview -- --port 4321 --strictPort`,
  прогресс в `localStorage` предзаполнен скриптом инициализации страницы,
  чтобы открыть уровень напрямую через кнопку «Продолжить»; на каждом из
  10 раундов имя и число карточек считаны из DOM и сверены с `value` из
  `level15.ts` перед кликом, чтобы кликать действительно верную по данным
  карточку — пройдены все 10 раундов, счёт на финише 25 очков (идеальные
  10/10), победная модалка появилась корректно. Ноль ошибок консоли и
  ноль неудачных сетевых запросов по картинкам уровня (единственный
  неудачный запрос — блокировка `fonts.googleapis.com` политикой
  офлайн-песочницы, не связана с уровнем).

- QA уровня 14 «У кого крылья шире?» (`src/data/level14.ts`, первый в пачке
  этого прогона: `lastReviewedLevelId` был 13, случайный размер пачки —
  7, то есть уровни 14-15-1-2-3-4-5). Картинки: 2 карточки с непустым
  `imageUri` (сапсан — переиспользует ассет уровня 2, африканский страус —
  переиспользует ассет уровня 11) реально лежат в `public/images/` и
  раздаются `npm run preview` с кодом 200. Для остальных 18 карточек (все
  прочие птицы) весь `public/images/` просмотрен целиком — подходящих
  неиспользуемых фотографий птиц (альбатрос, кондор, пеликан, сип, марабу,
  лебедь, беркут, журавль, цапля, скопа, филин, ворон, голубь, иволга,
  ласточка, воробей, королёк, колибри) не нашлось, оставлен честный
  emoji-фолбэк. Все 20 значений размаха крыльев уникальны (10 см колибри —
  340 см странствующий альбатрос), пар для сравнения хватает. Факты
  сверены на правдоподобность по общеизвестным диапазонам вида, дублей
  между карточками нет, `comparisonType: 'max'` верен для «у кого крылья
  шире». Багов не найдено. Уровень пройден по-настоящему в headless
  Chromium (глобально установленный Playwright 1.56.1, без интернета):
  собран прод-бандл, поднят `npm run preview -- --port 4321 --strictPort`,
  прогресс в `localStorage` предзаполнен скриптом инициализации страницы,
  чтобы открыть уровень напрямую через кнопку «Продолжить» без прохождения
  предыдущих; на каждом из 10 раундов имя и число карточек считаны из DOM
  и сверены с `value` из `level14.ts` перед кликом, чтобы кликать
  действительно верную по данным карточку — пройдены все 10 раундов,
  счёт на финише 25 очков (идеальные 10/10), победная модалка появилась
  корректно. Ноль ошибок консоли и ноль неудачных сетевых запросов по
  картинкам уровня (единственный неудачный запрос — блокировка
  `fonts.googleapis.com` политикой офлайн-песочницы, не связана с уровнем).

## 2026-09-16

- QA уровня 6 «Где глубже?» (`src/data/level6.ts`, первый в пачке этого
  прогона: пачка 6-7-8-9-10-11-12-13, размер выпал случайно — 8). Картинки:
  все 10 карточек с непустым `imageUri` (Марианская впадина, обломки
  Титаника, Кольская скважина, Гранд-Каньон, нырок кашалота, Бурдж-Халифа,
  синий кит, императорский пингвин, кожистая черепаха, шахта Мпоненг)
  реально лежат в `public/images/`. Для 6 карточек с пустым `imageUri`
  (Байкал, рекорд дайвинга, бассейн, метро «Арсенальная», морской слон,
  клюворыл Кювье) целиком проверил `public/images/` — подходящих
  неиспользуемых файлов под озеро, дайвинг, бассейн, метро, морского слона
  или клюворыла не нашлось, оставлен честный emoji-фолбэк. Все 17 значений
  глубины уникальны (2.5 м бассейн — 12 262 м Кольская скважина), пар для
  сравнения хватает. Факты сверены на правдоподобность, дублей нет,
  `comparisonType: 'max'` верен для «где глубже». Багов не найдено. Уровень
  пройден по-настоящему в headless Chromium (глобально установленный
  Playwright 1.56.1, без интернета): собран прод-бандл, поднят `npm run
  preview -- --port 4321 --strictPort`, прогресс в `localStorage`
  предзаполнен скриптом инициализации страницы, чтобы открыть уровень
  напрямую через «Выбор уровней» без прохождения предыдущих; пройдены все 10
  раундов кликами по карточке с фактически большей глубиной (сверено с
  данными уровня через тот же `comparisonType`), счёт на финише — 25 очков
  (идеальные 10/10), победная модалка появилась корректно. Ноль ошибок
  консоли и ноль неудачных сетевых запросов по картинкам уровня (единственный
  неудачный запрос — блокировка `fonts.googleapis.com` политикой офлайн-
  песочницы, не связана с уровнем).

- QA уровня 7 «Что дороже?» (`src/data/level7.ts`, второй в пачке этого
  прогона: пачка 6-7-8-9-10-11-12-13, размер выпал случайно — 8). Картинки:
  все 9 карточек с непустым `imageUri` (коробок спичек, флагманский
  смартфон, Ferrari, одна спичка, Мона Лиза, МКС, Бугатти Широн, Бурдж
  Халифа, телескоп Хаббл) реально лежат в `public/images/`. Для 11 карточек
  с пустым `imageUri` (кофе, ноутбук, часы Rolex, слиток золота, частный
  самолет, бутылка воды, билет в кино, видеоигра, новый автомобиль, дом в
  США, истребитель F-35) проверил `public/images/` целиком — подходящих
  неиспользуемых файлов под эти предметы не нашлось, оставлен честный
  emoji-фолбэк. Все 20 значений цены уникальны ($0.01 спичка — $150 млрд
  МКС), пар для сравнения хватает. Факты сверены на правдоподобность, дублей
  нет, `comparisonType: 'max'` верен для «что дороже». Багов не найдено.
  Уровень пройден по-настоящему в headless Chromium (глобально установленный
  Playwright 1.56.1, без интернета) тем же способом, что и уровень 6 этой
  пачки (прод-бандл + `npm run preview -- --port 4321 --strictPort`,
  прогресс в `localStorage` предзаполнен для прямого перехода к уровню):
  пройдены все 10 раундов кликами по фактически более дорогой карточке,
  счёт на финише — 25 очков (идеальные 10/10), победная модалка появилась
  корректно. Ноль ошибок консоли и ноль неудачных сетевых запросов по
  картинкам уровня (единственный неудачный запрос — блокировка
  `fonts.googleapis.com` политикой офлайн-песочницы, не связана с уровнем).

- QA уровня 8 «Кто живет дольше?» (`src/data/level8.ts`, третий в пачке этого
  прогона: пачка 6-7-8-9-10-11-12-13, размер выпал случайно — 8). Картинки:
  все 9 карточек с непустым `imageUri` (домашняя мышь, собака, лев, лошадь,
  африканский слон, человек, гренландская акула, секвойя, бессмертная
  медуза) реально лежат в `public/images/`. Для 10 карточек с пустым
  `imageUri` (муха-поденка, беркут, галапагосская черепаха, кролик,
  шимпанзе, попугай ара, карп кои, гренландский кит, океанический куахог,
  остистая сосна) проверил `public/images/` целиком — подходящих
  неиспользуемых файлов под этих животных/растения не нашлось, оставлен
  честный emoji-фолбэк. Все 19 значений срока жизни уникальны (0.003 года
  муха-поденка — условные 9999 «бессмертна» медуза), пар для сравнения
  хватает. Факты сверены на правдоподобность, дублей нет,
  `comparisonType: 'max'` верен для «кто живет дольше». Багов не найдено.
  Уровень пройден по-настоящему в headless Chromium (глобально
  установленный Playwright 1.56.1, без интернета) тем же способом, что и
  предыдущие уровни этой пачки: пройдены все 10 раундов кликами по
  фактически более долгоживущей карточке, счёт на финише — 25 очков
  (идеальные 10/10), победная модалка появилась корректно. Ноль ошибок
  консоли и ноль неудачных сетевых запросов по картинкам уровня
  (единственный неудачный запрос — блокировка `fonts.googleapis.com`
  политикой офлайн-песочницы, не связана с уровнем).

- QA уровня 9 «Что громче?» (`src/data/level9.ts`, четвёртый в пачке этого
  прогона: пачка 6-7-8-9-10-11-12-13, размер выпал случайно — 8). Картинки:
  все 3 карточки с непустым `imageUri` (двигатель самолета, раскат грома —
  переиспользует `lightning_*.jpg`, светошумовая граната) реально лежат в
  `public/images/`. Для 16 карточек с пустым `imageUri` (шепот, холодильник,
  разговор, пылесос, газонокосилка, рок-концерт, сирена скорой, взрыв
  петарды, старт ракеты, шелест листьев, городской трафик, мотоцикл,
  бензопила, выстрел из пистолета, Тунгусский метеорит, столкновение чёрных
  дыр) проверил `public/images/` целиком — подходящих неиспользуемых файлов
  под эти звуки/предметы не нашлось, оставлен честный emoji-фолбэк. Все 19
  значений громкости уникальны (20 дБ шелест листьев — условные 500
  «неизмеримо» столкновение чёрных дыр), пар для сравнения хватает. Факты
  сверены на правдоподобность, дублей нет, `comparisonType: 'max'` верен
  для «что громче». Багов не найдено. Уровень пройден по-настоящему в
  headless Chromium (глобально установленный Playwright 1.56.1, без
  интернета) тем же способом, что и предыдущие уровни этой пачки: пройдены
  все 10 раундов кликами по фактически более громкой карточке, счёт на
  финише — 25 очков (идеальные 10/10), победная модалка появилась
  корректно. Ноль ошибок консоли и ноль неудачных сетевых запросов по
  картинкам уровня (единственный неудачный запрос — блокировка
  `fonts.googleapis.com` политикой офлайн-песочницы, не связана с уровнем).

- QA уровня 10 «Что калорийнее?» (`src/data/level10.ts`, пятый в пачке
  этого прогона: пачка 6-7-8-9-10-11-12-13, размер выпал случайно — 8).
  Картинки: 20 из 21 карточки с непустым `imageUri` реально лежат в
  `public/images/` (огурец, яблоко, картофель, говядина, хлеб, сыр, шоколад,
  грецкие орехи, масло, оливковое масло, брокколи, клубника, рис,
  арахисовая паста, миндаль, майонез, топленое масло, сало, кока-кола,
  водка). Единственная карточка с пустым `imageUri` — куриная грудка;
  проверил `public/images/` целиком, файла под курицу не нашлось, оставлен
  честный emoji-фолбэк. Все 21 значение калорийности уникальны (15 ккал
  огурец — 900 ккал сало), пар для сравнения хватает. Факты сверены на
  правдоподобность, дублей нет, `comparisonType: 'max'` верен для «что
  калорийнее». Багов не найдено. Уровень пройден по-настоящему в headless
  Chromium (глобально установленный Playwright 1.56.1, без интернета) тем
  же способом, что и предыдущие уровни этой пачки: пройдены все 10 раундов
  кликами по фактически более калорийной карточке, счёт на финише — 25
  очков (идеальные 10/10), победная модалка появилась корректно. Ноль
  ошибок консоли и ноль неудачных сетевых запросов по картинкам уровня
  (единственный неудачный запрос — блокировка `fonts.googleapis.com`
  политикой офлайн-песочницы, не связана с уровнем).

- QA уровня 11 «Что выше?» (`src/data/level11.ts`, шестой в пачке этого
  прогона: пачка 6-7-8-9-10-11-12-13, размер выпал случайно — 8). Картинки:
  все 25 карточек с непустым `imageUri` реально лежат в `public/images/` —
  уровень полностью проиллюстрирован, чинить нечего. Все 25 значений высоты
  уникальны (38 м статуя Христа-Искупителя — 21 900 м марсианский Олимп),
  пар для сравнения хватает. Факты сверены на правдоподобность, дублей нет,
  `title`/`subtitle`/`comparisonType: 'max'` в `levels.ts` соответствуют
  содержимому (здания, башни, горы, статуи). Багов не найдено. Уровень
  пройден по-настоящему в headless Chromium (глобально установленный
  Playwright 1.56.1, без интернета) тем же способом, что и предыдущие
  уровни этой пачки: пройдены все 10 раундов кликами по фактически более
  высокой карточке, счёт на финише — 25 очков (идеальные 10/10), победная
  модалка появилась корректно. Ноль ошибок консоли и ноль неудачных сетевых
  запросов по картинкам уровня (единственный неудачный запрос — блокировка
  `fonts.googleapis.com` политикой офлайн-песочницы, не связана с уровнем).

- QA уровня 12 «Кто ядовитее?» (`src/data/level12.ts`, седьмой в пачке
  этого прогона: пачка 6-7-8-9-10-11-12-13, размер выпал случайно — 8).
  Картинки: ни у одной из 22 карточек нет `imageUri` — как и объяснено в
  комментарии в начале файла, подходящих локальных фото ядовитых видов в
  `public/images/` нет, а хотлинки запрещены правилами проекта; заново
  проверил `public/images/` целиком — по-прежнему ничего подходящего (змеи,
  пауки, медузы, скорпионы и т.д.), честный emoji-фолбэк остаётся
  единственным вариантом. Все 22 значения индекса токсичности уникальны
  (99 баллов тайпан Маклея — 6 баллов утконос), пар для сравнения хватает.
  Факты сверены на правдоподобность — используется условный сравнительный
  индекс токсичности вместо точных LD50 (обоснование в комментарии файла),
  дублей фактов нет, `comparisonType: 'max'` верен для «кто ядовитее».
  Багов не найдено. Уровень пройден по-настоящему в headless Chromium
  (глобально установленный Playwright 1.56.1, без интернета) тем же
  способом, что и предыдущие уровни этой пачки (уровень изначально
  заблокирован для нового игрока — прогресс в `localStorage` предзаполнен
  так, чтобы разблокировать все уровни для перехода напрямую к уровню 12):
  пройдены все 10 раундов кликами по фактически более токсичной карточке,
  счёт на финише — 25 очков (идеальные 10/10), победная модалка появилась
  корректно. Ноль ошибок консоли и ноль неудачных сетевых запросов
  (единственный неудачный запрос — блокировка `fonts.googleapis.com`
  политикой офлайн-песочницы, не связана с уровнем; картинок у уровня нет,
  поэтому проверять по сети нечего).

- QA уровня 13 «Кто дольше вынашивает?» (`src/data/level13.ts`, восьмой и
  последний в пачке этого прогона: пачка 6-7-8-9-10-11-12-13, размер выпал
  случайно — 8). Картинки: все 14 карточек с непустым `imageUri`
  (африканский слон, белый носорог, жираф, верблюд, лошадь, корова,
  человек, бурый медведь — переиспользует `grizzly_bear_*.jpg`, панда, лев,
  тигр, кошка, кенгуру, домовая мышь) реально лежат в `public/images/`. Для
  6 карточек с пустым `imageUri` (осёл, коза, овца, свинья, кролик,
  сирийский хомяк) проверил `public/images/` целиком — подходящих
  неиспользуемых файлов под этих животных не нашлось, оставлен честный
  emoji-фолбэк. Все 20 значений срока беременности уникальны (660 дней
  африканский слон — 16 дней сирийский хомяк), пар для сравнения хватает.
  Факты сверены на правдоподобность — используются общепринятые средние
  значения из зоологии, а не точечные измерения (обоснование в комментарии
  файла), дублей нет, `comparisonType: 'max'` верен для «кто дольше
  вынашивает». Багов не найдено. Уровень пройден по-настоящему в headless
  Chromium (глобально установленный Playwright 1.56.1, без интернета) тем
  же способом, что и предыдущие уровни этой пачки (уровень изначально
  заблокирован для нового игрока — прогресс в `localStorage` предзаполнен
  так, чтобы разблокировать все уровни для перехода напрямую к уровню 13):
  пройдены все 10 раундов кликами по карточке с фактически более долгой
  беременностью, счёт на финише — 25 очков (идеальные 10/10), победная
  модалка появилась корректно. Ноль ошибок консоли и ноль неудачных сетевых
  запросов по картинкам уровня (единственный неудачный запрос — блокировка
  `fonts.googleapis.com` политикой офлайн-песочницы, не связана с уровнем).
  Этим уровнем пачка 6-7-8-9-10-11-12-13 этого прогона завершена.

## 2026-09-15

- QA уровня 2 «Кто быстрее?» (`src/data/speeds.ts`, первый в пачке этого
  прогона: пачка 2-3-4-5, размер выпал случайно — 4). Картинки: все 20
  карточек с непустым `imageUri` реально лежат в `public/images/` и
  отдаются 200 через preview-сервер (сокол-сапсан, гепард, лев, грейхаунд,
  лось, кенгуру, лошадь, койот, зебра, верблюд, жираф, дельфин, гризли,
  носорог, кошка, слон, крокодил, бегемот, ленивец, улитка) — уровень
  полностью проиллюстрирован, чинить нечего. Разброс `value` в целом
  хороший (390 км/ч сапсан — 0.048 км/ч улитка), есть несколько совпадающих
  значений (грейхаунд/лось 72, койот/зебра/верблюд 65, жираф/дельфин 60), но
  это лишь 5 исключённых пар из 190 возможных — на разнообразие раундов не
  влияет, отдельного пункта в TASKS.md не заводил. Факты сверены на
  правдоподобность и дублей не найдено, `comparisonType: 'max'` верен для
  «кто быстрее». Багов не найдено. Уровень пройден по-настоящему в headless
  Chromium (глобально установленный Playwright 1.56.1, без интернета):
  собран прод-бандл, поднят `npm run preview -- --port 4321 --strictPort`,
  открыт `/GuessWhat/` (базовый путь preview теперь совпадает с прод-базой),
  пройдены все 10 раундов кликами по фактически более быстрому животному в
  каждой паре (сверено с данными уровня), счёт на финише — 25 очков, что
  точно совпадает с формулой стриков при 10/10 верных ответов, победная
  модалка появилась корректно. Ноль ошибок консоли и ноль неудачных сетевых
  запросов по картинкам уровня (единственный неудачный запрос — блокировка
  `fonts.googleapis.com` политикой офлайн-песочницы, не связана с уровнем).

- QA уровня 3 «Что древнее?» (`src/data/level3.ts`, второй в пачке этого
  прогона: пачка 2-3-4-5, размер выпал случайно — 4). Картинки: 19 из 24
  карточек с непустым `imageUri` реально лежат в `public/images/` и
  отдаются 200 через preview-сервер (колесо, интернет/ARPANET, пирамида
  Хеопса, первый iPhone — переиспользует `smartphone_*.jpg`, тираннозавр,
  освоение огня, биткоин, высадка на Луну, бумага, порох, микроскоп,
  фотография, радио, телевидение, ЭВМ, лазер, email, ПК, соцсети). Для 5
  карточек без картинки (печатный станок, первый кинофильм, компас,
  телескоп, паровой двигатель) проверил `public/images/` целиком — файлов
  под гутенберговский станок, синематограф, компас, телескоп или паровоз не
  нашлось, оставлен честный emoji-фолбэк. Все 24 значения года/эпохи
  уникальны (кодировка дробной частью года для случаев с известной датой в
  пределах года — например, высадка на Луну 1969.55 ≈ 20 июля, интернет
  1969.83 ≈ 29 октября, сверил обе доли с реальными датами — совпадают),
  `comparisonType: 'min'` верен для «что древнее» (уровень уже упомянут как
  пример правильной настройки в самой инструкции QA). Факты сверены на
  правдоподобность и дублей не найдено (колесо — Месопотамия ~3500 до н.э.,
  первое фото экспонировалось 8 часов, Rэй Томлинсон выбрал @ для email и
  т.д.). Багов не найдено. Уровень пройден по-настоящему в headless Chromium
  (Playwright 1.56.1, без интернета): 10/10 верных ответов (в т.ч. пары с
  экстремальным разбросом вроде T-Rex 68 млн лет назад против колеса 3500
  до н.э.), финальный счёт 25 совпал с формулой стриков, победная модалка
  корректна, ноль ошибок консоли и ноль неудачных сетевых запросов по
  картинкам уровня (не считая офлайн-блокировки Google Fonts).

- QA уровня 4 «Что больше?» (`src/data/level4.ts`, третий в пачке этого
  прогона: пачка 2-3-4-5, размер выпал случайно — 4). Картинки: 15 из 21
  карточки с непустым `imageUri` реально лежат в `public/images/` и
  отдаются 200 через preview-сервер (Эйфелева башня, Бурдж-Халифа, Эверест,
  Титаник, Великая Китайская стена, Статуя Свободы, синий кит, футбольное
  поле, Эмпайр-стейт-билдинг, МКС, школьный автобус, баскетбольная
  площадка, фура, круизный лайнер, мост Золотые Ворота). Для 6 карточек без
  картинки (теннисный корт, дорожка для боулинга, олимпийский бассейн,
  вагон поезда, авианосец, подводная лодка) проверил `public/images/`
  целиком — подходящих файлов не нашлось, оставлен честный emoji-фолбэк.
  Все 21 значение длины/высоты в метрах уникальны, разброс огромный (от
  14 м школьного автобуса до 21 196 000 м Великой Китайской стены со всеми
  ответвлениями) — пар для сравнения с запасом, `comparisonType: 'max'`
  верен для «что больше». Факты сверены на правдоподобность и дублей не
  найдено (Бурдж-Халифа — самое высокое когда-либо построенное сооружение,
  подлодки класса «Акула»/Typhoon — крупнейшие в мире, круизники длиннее
  Эйфелевой башни в горизонтали и т.д.). Багов не найдено. Уровень пройден
  по-настоящему в headless Chromium (Playwright 1.56.1, без интернета):
  10/10 верных ответов, финальный счёт 25 совпал с формулой стриков,
  победная модалка корректна, ноль ошибок консоли и ноль неудачных сетевых
  запросов по картинкам уровня (не считая офлайн-блокировки Google Fonts).

- QA уровня 5 «Что горячее?» (`src/data/level5.ts`, четвёртый и последний в
  пачке этого прогона: пачка 2-3-4-5, размер выпал случайно — 4). Картинки:
  только 9 из 17 карточек с непустым `imageUri` реально лежат в
  `public/images/` и отдаются 200 через preview-сервер (тело человека,
  молния, сухой лёд, духовка, абсолютный нуль, пламя свечи, лёд, горение
  бумаги, магма). У 8 карточек (поверхность Солнца, вулканическая лава,
  кипящая вода, жидкий азот, печь для пиццы, нить накаливания, ядро Солнца,
  плавление железа) `imageUri` пуст — проверил `public/images/` целиком,
  подходящих файлов не нашлось, оставлен честный emoji-фолбэк. Покрытие
  картинками (9/17 ≈ 53%) заметно ниже, чем у остальных уровней пачки
  (уровни 2–4 — от 71% до 100%) — завёл отдельный пункт в TASKS.md
  («📚 Контент») на будущее наполнение ассетами, за один прогон это не
  чиню. Все 17 значений температуры уникальны (от -273.15 °C абсолютного
  нуля до 15 млн °C ядра Солнца), `comparisonType: 'max'` верен для «что
  горячее». Факты сверены на правдоподобность и дублей не найдено (молния
  в ~5 раз горячее поверхности Солнца — 30000/5500 ≈ 5.45, сходится; вода
  на вершине Эвереста кипит при ~68 °C из-за давления;
  температура самовоспламенения бумаги по Брэдбери соответствует 451 °F из
  «451 градус по Фаренгейту» и т.д.) — багов в данных не найдено. Уровень
  пройден по-настоящему в headless Chromium (Playwright 1.56.1, без
  интернета): 10/10 верных ответов, финальный счёт 25 совпал с формулой
  стриков, победная модалка корректна, ноль ошибок консоли и ноль неудачных
  сетевых запросов по картинкам уровня (не считая офлайн-блокировки Google
  Fonts).

## 2026-09-14

- QA уровня 11 «Что выше?» (`src/data/level11.ts`, первый в пачке этого
  прогона: пачка 11-1 циклически, размер выпал случайно — 6). Картинки: все
  22 карточки с непустым `imageUri` реально лежат в `public/images/` и
  отдаются 200 через preview-сервер — уровень полностью проиллюстрирован,
  чинить нечего. Все 22 значения высоты уникальны (38 м статуя Христа —
  21900 м марсианский Олимп), пар для сравнения хватает с запасом. Факты
  правдоподобны и без дублей (Эйфелева башня 330 м, Бурдж-Халифа 828 м, гора
  Олимп на Марсе — самый высокий вулкан Солнечной системы и т.д.),
  `comparisonType: 'max'` верен для «что выше». Найдена и исправлена мелкая
  неточность: `subtitle` уровня был «Высота строений», хотя среди карточек
  есть природные объекты (Эверест, K2, Килиманджаро, марсианский Олимп) —
  заменён на «Здания, горы и вершины», честно описывающий фактический набор
  карточек. Уровень пройден по-настоящему в headless Chromium (глобально
  установленный Playwright 1.56.1, `NODE_PATH=/opt/node22/lib/node_modules`,
  без интернета): прогресс временно разблокирован через сид
  `localStorage`, чтобы дойти до уровня в один прогон, пройдены все 10
  раундов кликами по карточкам, победная модалка появилась корректно, счёт
  и звёзды посчитаны по формуле стриков. Ноль ошибок консоли и ноль
  неудачных сетевых запросов уровня (не считая уже известного офлайн
  `ERR_CONNECTION_RESET` на Google Fonts). Временный `play_level.cjs` жил в
  скретчпаде, в репозиторий не коммитился.

- QA уровня 12 «Кто ядовитее?» (`src/data/level12.ts`, второй в пачке этого
  прогона: пачка 11-1 циклически, размер выпал случайно — 6). Картинки: ни у
  одной из 22 карточек нет `imageUri` — сознательное решение, задокументировано
  комментарием в самом файле (нет подходящих локальных фото ядовитых видов в
  `public/images/`, внешние хотлинки запрещены). Перепроверил `public/images/`
  на случай, если с прошлых QA появились подходящие файлы (змеи, пауки,
  медузы, скорпионы, шершни, утконос и т.д.) — ничего по смыслу не нашлось,
  emoji-фолбэк остаётся честным выбором. `value` (условный индекс токсичности
  0–100) уникален у всех 22 карточек, `comparisonType: 'max'` верен для «кто
  ядовитее». Факты сверены на правдоподобность (тайпан Маклея — самый
  токсичный яд среди сухопутных змей, кубомедуза Chironex fleckeri способна
  остановить сердце за минуты, лягушка-листолаз — самое токсичное позвоночное,
  утконос — редкий пример ядовитого млекопитающего и т.д.) — дублей и
  выдуманных цифр не найдено. Багов не найдено. Уровень пройден по-настоящему
  в headless Chromium (глобальный Playwright 1.56.1, без интернета,
  прогресс временно разблокирован сидом `localStorage`): 10/10 раундов,
  победная модалка появилась корректно, ноль ошибок консоли и ноль неудачных
  сетевых запросов уровня (не считая уже известного офлайн
  `ERR_CONNECTION_RESET` на Google Fonts).

- QA уровня 13 «Кто дольше вынашивает?» (`src/data/level13.ts`, третий в
  пачке этого прогона: пачка 11-1 циклически, размер выпал случайно — 6).
  Картинки: 13 из 19 карточек с непустым `imageUri` (слон, носорог, жираф,
  верблюд, лошадь, корова, человек, медведь, панда, лев, тигр, кошка,
  кенгуру) реально лежат в `public/images/` и отдаются 200 через
  preview-сервер. Для 6 карточек без картинки (осёл, коза, овца, свинья,
  кролик, сирийский хомяк) проверил `public/images/` — подходящих по виду
  файлов не нашлось, оставлен честный emoji-фолбэк, как и указано в
  комментарии самого файла. Все 19 значений срока беременности в днях
  уникальны (16 дней хомяк — 660 дней слон), `comparisonType: 'max'` верен
  для «кто дольше вынашивает». Факты сверены на правдоподобность (слон —
  самая долгая беременность среди наземных млекопитающих ~22 месяца, свиной
  мнемонический «три месяца, три недели, три дня», задержанная имплантация у
  бурого медведя и панды и т.д.) — дублей и выдуманных цифр не найдено.
  Багов не найдено. Уровень пройден по-настоящему в headless Chromium
  (глобальный Playwright 1.56.1, без интернета, прогресс временно
  разблокирован сидом `localStorage`): 10/10 раундов, победная модалка
  появилась корректно, ноль ошибок консоли и ноль неудачных сетевых
  запросов уровня (не считая уже известного офлайн `ERR_CONNECTION_RESET`
  на Google Fonts).

- QA уровня 14 «У кого крылья шире?» (`src/data/level14.ts`, четвёртый в
  пачке этого прогона: пачка 11-1 циклически, размер выпал случайно — 6).
  Картинки: 2 из 20 карточек с непустым `imageUri` (африканский страус,
  сапсан — переиспользуют ассеты уровней 11 и 2) реально лежат в
  `public/images/` и отдаются 200 через preview-сервер. Для остальных 18
  карточек без картинки локальных фото птиц в `public/images/` нет —
  оставлен честный emoji-фолбэк, как и указано в комментарии самого файла.
  Все 20 значений размаха крыльев уникальны (10 см колибри — 340 см
  странствующий альбатрос), `comparisonType: 'max'` верен для «у кого шире».
  Факты сверены на правдоподобность (альбатрос — самый большой размах крыльев
  среди живых птиц, сапсан — самое быстрое живое существо в пикировании,
  страус — самая крупная нелетающая птица и т.д.) — дублей и выдуманных цифр
  не найдено. При настоящем прохождении в браузере нашёлся баг **движка**
  (не уровня): победная модалка в `GameLevel.tsx` подписывала итоговый счёт
  захардкоженным «X очков» без учёта склонения — «1 очков», «2 очков», «-2
  очков» вместо грамматически верных «1 очко», «2 очка», «-2 очка». В том же
  файле уже была верная функция `pluralizeRu` (использовалась в тексте
  шеринга результата) — теперь и подпись счёта в модалке использует её.
  Починено в общем коде `src/components/GameLevel.tsx`, затрагивает победную
  модалку всех уровней, а не только этого. Уровень пройден по-настоящему в
  headless Chromium (глобальный Playwright 1.56.1, без интернета, прогресс
  временно разблокирован сидом `localStorage`): 10/10 раундов, победная
  модалка появилась корректно (после фикса — с верным склонением), ноль
  ошибок консоли и ноль неудачных сетевых запросов уровня (не считая уже
  известного офлайн `ERR_CONNECTION_RESET` на Google Fonts).

- QA уровня 15 «Кто спит дольше?» (`src/data/level15.ts`, пятый в пачке
  этого прогона: пачка 11-1 циклически, размер выпал случайно — 6).
  Картинки: 11 из 20 карточек с непустым `imageUri` (тигр, лев, ленивец,
  домовая мышь, кошка, собака, человек, корова, лошадь, слон, жираф) реально
  лежат в `public/images/` и отдаются 200 через preview-сервер. Для 9
  карточек без картинки (коала, ночница, суслик, хомячок, макака, кролик,
  свинья, коза, овца) проверил `public/images/` — подходящих по виду файлов
  не нашлось, оставлен честный emoji-фолбэк, как и указано в комментарии
  самого файла. Все 20 значений продолжительности сна в часах уникальны
  (1.9 ч жираф — 20 ч коала), `comparisonType: 'max'` верен для «кто спит
  дольше». Факты и цифры сверены на правдоподобность — значения заметно
  совпадают с классическим сравнительным набором данных о сне млекопитающих
  (Allison & Cicchetti, 1976: кролик 8.4 ч, свинья 7.8 ч, корова 3.9 ч, овца
  3.8 ч и т.д.) и более поздними наблюдениями о коротком сне слонов и
  жирафов в дикой природе; для ленивца честно отмечено в `funFact`, что 13 ч
  — оценка по неволе, а не по дикой природе. Дублей и выдуманных цифр не
  найдено. Багов не найдено. Уровень пройден по-настоящему в headless
  Chromium (глобальный Playwright 1.56.1, без интернета, прогресс временно
  разблокирован сидом `localStorage`): 10/10 раундов, победная модалка
  появилась корректно (со склонением очков, исправленным на этом же уровне
  пачки), ноль ошибок консоли и ноль неудачных сетевых запросов уровня (не
  считая уже известного офлайн `ERR_CONNECTION_RESET` на Google Fonts).

- QA уровня 1 «Кто тяжелее?» (`src/data/level1.ts`, шестой и последний в
  пачке этого прогона: пачка 11-12-13-14-15-1 циклически — список из 15
  реальных уровней закончился на 15, поэтому пачка на 6 уровней замкнулась
  на уровень 1; размер выпал случайно — 6). Картинки: все 30 карточек
  (20 из `LEVEL1_DATA` + 10 из `LEVEL1_ADDITIONAL`) с непустым `imageUri`
  реально лежат в `public/images/` и отдаются 200 через preview-сервер —
  уровень полностью проиллюстрирован. Все 30 значений массы уникальны
  (4 кг кошка — 150 000 кг синий кит), пар для сравнения хватает с большим
  запасом. `comparisonType: 'max'` верен для «кто тяжелее», факты
  правдоподобны и без дублей (синий кит — крупнейшее животное в истории
  Земли, слоновий хобот из 40 000 мышц, жирафье сердце 11 кг и т.д.).
  Уровень уже содержит собственный комментарий о более раннем фиксе
  дублей-видов (`lion`/`african-lion`, `tiger`/`amur-tiger`,
  `grizzly`/`grizzly-bear`) — новых дублей не нашлось. Багов не найдено.
  Уровень пройден по-настоящему в headless Chromium (глобальный Playwright
  1.56.1, без интернета): 10/10 раундов, победная модалка появилась
  корректно (со склонением очков, исправленным на уровне 14 этой же пачки),
  ноль ошибок консоли и ноль неудачных сетевых запросов уровня (не считая
  уже известного офлайн `ERR_CONNECTION_RESET` на Google Fonts). Этим
  уровнем пачка прогона (11, 12, 13, 14, 15, 1) завершена.

## 2026-09-13

- QA уровня 8 «Кто живет дольше?» (`src/data/level8.ts`, первый в пачке этого
  прогона: пачка 8-10, размер выпал случайно — 3). Картинки: 9 из 19 карточек
  с непустым `imageUri` (мышь, собака, лев, лошадь, слон, человек, акула,
  секвойя, медуза) реально лежат в `public/images/` и отдаются 200 через
  preview-сервер. Для 10 карточек без картинки (муха-подёнка, орёл,
  галапагосская черепаха, кролик, шимпанзе, попугай ара, карп кои,
  гренландский кит, океанический куахог, остистая сосна) осмотрел
  `public/images/` на совпадения по смыслу — нашлись только неточные по виду
  файлы (сокол вместо орла, горилла вместо шимпанзе), подставлять их не стал,
  оставлен честный эмодзи-фолбэк. Все 19 значений срока жизни уникальны (0.003
  года у подёнки до условных 9999 у «бессмертной» медузы), `comparisonType:
  'max'` верен для «кто живёт дольше», факты правдоподобны (в т.ч. реальные
  рекорды — лошадь Old Billy 62 года, гренландская акула ~400 лет, моллюск
  Мин 507 лет, дерево Мафусаил старше пирамид) и без дублей. Багов не найдено.
  Уровень пройден по-настоящему в headless Chromium (Playwright, установлен
  временно через `npm install --no-save` — не сохранён в `package.json`):
  10/10 раундов, победная модалка появилась корректно, ноль ошибок консоли и
  ноль неудачных сетевых запросов, относящихся к уровню (единственный
  `net::ERR_CONNECTION_RESET` — офлайн-попытка подключить Google Fonts, не в
  счёт, как и в прошлых QA-проходах). Временный `qa_level8_tmp.mjs` — в
  корне репозитория при запуске, удалён по завершении, не коммитился.

- QA уровня 9 «Что громче?» (`src/data/level9.ts`, второй в пачке этого
  прогона: пачка 8-10, размер выпал случайно — 3). Картинки: 3 из 19
  карточек с непустым `imageUri` (двигатель самолёта, раскат грома,
  светошумовая граната) реально лежат в `public/images/` и отдаются 200
  через preview-сервер. Для 16 карточек без картинки (шёпот, холодильник,
  разговор, пылесос, газонокосилка, рок-концерт, сирена скорой, взрыв
  петарды, старт ракеты, шелест листьев, городской трафик, мотоцикл,
  бензопила, выстрел из пистолета, Тунгусский метеорит, столкновение
  чёрных дыр) осмотрел `public/images/` — подходящих по смыслу файлов не
  нашлось, оставлен честный эмодзи-фолбэк. Все 19 значений громкости в дБ
  уникальны (20 дБ шелест листьев — 500 дБ гравитационные волны
  столкновения чёрных дыр), `comparisonType: 'max'` верен для «что
  громче», факты правдоподобны и без дублей. Багов не найдено. Уровень
  пройден по-настоящему в headless Chromium (Playwright, снова временно
  через `npm install --no-save`): 10/10 раундов, победная модалка
  появилась корректно, ноль ошибок консоли и ноль неудачных сетевых
  запросов уровня (не считая уже известного офлайн `ERR_CONNECTION_RESET`
  на Google Fonts). Временный `qa_level9_tmp.mjs` удалён по завершении, не
  коммитился.

- QA уровня 10 «Что калорийнее?» (`src/data/level10.ts`, третий и последний в
  пачке этого прогона: пачка 8-10, размер выпал случайно — 3). Картинки: 20
  из 21 карточки с непустым `imageUri` реально лежат в `public/images/` и
  отдаются 200 через preview-сервер — почти весь уровень уже проиллюстрирован.
  Единственная карточка без картинки — куриная грудка: подходящего файла с
  курицей/мясом птицы в `public/images/` нет, оставлен честный
  эмодзи-фолбэк. Все 21 значение калорийности уникальны (15 ккал огурец —
  900 ккал сало), `comparisonType: 'max'` верен для «что калорийнее», факты
  правдоподобны и без дублей. Багов не найдено. Уровень пройден
  по-настоящему в headless Chromium (Playwright, снова временно через `npm
  install --no-save`): 10/10 раундов, победная модалка появилась корректно,
  ноль ошибок консоли и ноль неудачных сетевых запросов уровня (не считая
  уже известного офлайн `ERR_CONNECTION_RESET` на Google Fonts). Временный
  `qa_level10_tmp.mjs` удалён по завершении, не коммитился. Пачка этого
  прогона (уровни 8-10, размер 3) полностью проверена.

## 2026-09-12

- QA уровня 7 «Что дороже?» (`src/data/level7.ts`, четвёртый и последний
  в пачке этого прогона: пачка 4-7, размер выпал случайно — 4). Картинки:
  9 из 20 непустых `imageUri` реально лежат в `public/images/`; для 11
  карточек без картинки (кофе, ноутбук, Rolex, слиток золота, частный
  самолёт, бутылка воды, билет в кино, видеоигра, новый автомобиль, дом в
  США, F-35) осмотрел `public/images/` на совпадения по смыслу — нашлись
  только тематически неточные файлы (пассажирский авиалайнер вместо
  частного самолёта, мост «Золотые Ворота» вместо слитка золота, мышь
  вместо дома) — подставлять их не стал, оставлен честный эмодзи-фолбэк
  для всех 11. Все 20 цен уникальны, `comparisonType: 'max'` верен для
  «что дороже», факты правдоподобны и без дублей (ранее уже была
  устранена дублирующая карточка ноутбука). Багов не найдено. Уровень
  пройден по-настоящему в headless Chromium (Playwright): 10/10 раундов,
  25 очков, 3 звезды, ноль ошибок консоли и ноль неудачных сетевых
  запросов (офлайн Google Fonts не в счёт). Временный `qa_level.mjs` — в
  скретчпаде, не коммитился.

  Пачка этого прогона (уровни 4-7) завершена — все 4 уровня проверены,
  каждый со своим коммитом.

- QA уровня 6 «Где глубже?» (`src/data/level6.ts`, третий в пачке этого
  прогона, размер пачки — 4, берём уровни 4-7). Картинки: 10 из 17
  непустых `imageUri` реально лежат в `public/images/`; для 7 карточек
  без картинки (Байкал, рекорд дайвинга, обычный бассейн, пещера Крубера,
  станция метро, морской слон, клюворыл Кювье) подходящего неиспользуемого
  файла не нашлось — нашлись только фотографии обычных (сухопутных)
  слонов (`african_elephant_*`, `asian_elephant_*`, `elephant_*`), но
  «Морской слон» (ластоногое) — не то же животное, что слон, поэтому
  подставлять их не стал (это было бы «картинка не по теме») — оставлен
  честный эмодзи-фолбэк для всех 7. Все 17 значений глубины уникальны,
  `comparisonType: 'max'` верен для «где глубже», факты правдоподобны и
  без дублей. Багов не найдено. Уровень пройден по-настоящему в headless
  Chromium (Playwright): 10/10 раундов, 25 очков, 3 звезды, ноль ошибок
  консоли и ноль неудачных сетевых запросов (офлайн Google Fonts не в
  счёт). Временный `qa_level.mjs` — в скретчпаде, не коммитился.

- QA уровня 5 «Что горячее?» (`src/data/level5.ts`, второй в пачке этого
  прогона, размер пачки — 4, берём уровни 4-7). Картинки: 9 из 16
  непустых `imageUri` реально лежат в `public/images/`; для оставшихся 7
  карточек без картинки (поверхность Солнца, лава, кипящая вода, жидкий
  азот, печь для пиццы, нить накаливания, ядро Солнца) подходящего
  неиспользуемого файла в `public/images/` не нашлось — оставлен честный
  эмодзи-фолбэк. Все 16 значений температуры уникальны, `comparisonType:
  'max'` верен для «что горячее». Нашёл и исправил опечатку в `funFact`
  карточки «Горение бумаги»: было «по Редбери», должно быть «по Брэдбери»
  (отсылка к роману Рэя Брэдбери «451 градус по Фаренгейту» — температуре
  самовоспламенения бумаги). Уровень пройден по-настоящему в headless
  Chromium (Playwright): 10/10 раундов, 25 очков, 3 звезды, ноль ошибок
  консоли и ноль неудачных сетевых запросов (офлайн Google Fonts не в
  счёт). Временный `qa_level.mjs` — в скретчпаде, не коммитился.

- QA уровня 4 «Что больше?» (`src/data/level4.ts`, первый в пачке этого
  прогона: `lastReviewedLevelId` был 3, случайный размер пачки — 4, берём
  уровни 4-7). Картинки: все 15 непустых `imageUri` реально лежат в
  `public/images/` (проверено на диске); для оставшихся 6 карточек без
  картинки (теннисный корт, дорожка для боулинга, олимпийский бассейн,
  вагон поезда, авианосец, подводная лодка) подходящего неиспользуемого
  файла в `public/images/` не нашлось — оставлен честный эмодзи-фолбэк. Все
  21 значение длины/высоты уникальны — пар для сравнения хватает с запасом,
  `funFact` у каждой карточки свой и правдоподобен, `comparisonType: 'max'`
  верен для «что больше». Багов в данных не найдено.
  По ходу проверки в окружении обнаружился и оказался доступен Playwright
  (глобальный пакет + предустановленный Chromium) — впервые в этой серии
  прогонов появилась возможность реально пройти уровень в headless-браузере,
  а не только фолбэк-способом. При первой попытке страница оставалась
  пустой (бандл не грузился): `vite.config.ts` выбирал `base` только по
  `command === 'build'`, а `vite preview` тоже приходит с `command ===
  'serve'`, поэтому раздавал уже собранный `dist` (ассеты зашиты под
  `/GuessWhat/assets/*`) с корня `/` — это баг общего билд-конфига, а не
  уровня 4, чинил в `vite.config.ts` (учёл `isPreview` из `ConfigEnv`).
  После фикса уровень пройден по-настоящему: 10/10 раундов кликами по
  карточке с верным ответом (правильность вычислялась из `value` в данных
  уровня, а не подсматривалась в DOM), итог — 25 очков, 3 звезды, ноль
  ошибок в консоли и ноль неудачных сетевых запросов (кроме ожидаемо
  недоступных офлайн Google Fonts, отфильтрованы отдельно и багом не
  считаются). Временный скрипт `qa_level.mjs` жил в скретчпаде, в репозиторий
  не коммитился.

## 2026-09-11

- QA уровня 3 «Что древнее?» (`src/data/level3.ts`, седьмой и последний в
  пачке этого прогона: пачка 12-15, 1-3, размер выпал случайно — 7).
  Повторная проверка (уже проверялся и дважды правился ранее) — багов не
  найдено, все прошлые правки на месте: все 24 карточки уникальны по
  `id`, `value` и `funFact` (проверено скриптом), включая дробные
  значения года у Интернета/Высадки на Луну (1969.83/1969.55) и
  Кино/Радио (1895.99/1895.66) — сохраняют верный исторический порядок
  для `comparisonType: 'min'`. 19 из 24 `imageUri` реально лежат в
  `public/images/` (проверено скриптом); для оставшихся 5 карточек
  (печатный станок, кинофильм, компас, телескоп, паровой двигатель)
  подходящего неиспользуемого файла по-прежнему нет — честный
  эмодзи-фолбэк, новых подходящих ассетов не появилось. Playwright не
  установлен как зависимость проекта — уровень пройден
  фолбэк-способом: `npm run build` зелёный, все 19 путей к картинкам
  подтверждены на диске, временный Node/esbuild-скрипт
  `dist/qa-sim-level.mjs` повторил логику
  `buildPairQueue`/`generateNewPair` — все 10 раундов без исключений, с
  верно определяемым победителем (включая корректный порядок «огонь
  древнее телевидения» и «соцсети древнее iPhone»). `lastReviewedLevelId`
  теперь `3` — этим уровнем завершена пачка из 7 уровней (12-15, 1-3) за
  сегодня.

- QA уровня 2 «Кто быстрее?» (`src/data/speeds.ts`, шестой в пачке этого
  прогона: пачка 12-15, 1-3). Повторная проверка (уже проверялся
  2026-09-07) — багов не найдено, состояние не изменилось: все 20 `id` и
  `funFact` уникальны, все 20 `imageUri` реально лежат в
  `public/images/` (проверено скриптом). 16 из 20 значений скорости
  уникальны (совпадения: грейхаунд/лось по 72 км/ч, койот/зебра/верблюд
  по 65 км/ч, жираф/дельфин по 60 км/ч) — как и зафиксировано
  2026-09-07, это реально сходящиеся общеизвестные цифры максимальной
  скорости для разных видов, а не ошибка ввода; придумывать
  искусственную точность ради различия не стал — исключает 5 пар
  сравнения из 190 возможных, на играбельность не влияет.
  `comparisonType: 'max'` корректен. Задача «Обогатить funFact уровня 2»
  в `TASKS.md` остаётся открытой (не дублирую пункт). Playwright не
  установлен как зависимость проекта — уровень пройден
  фолбэк-способом: `npm run build` зелёный, все 20 путей к картинкам
  подтверждены на диске, временный Node/esbuild-скрипт
  `dist/qa-sim-level.mjs` повторил логику
  `buildPairQueue`/`generateNewPair` — все 10 раундов без исключений и
  с верно определяемым победителем. `lastReviewedLevelId` теперь `2`.

- QA уровня 1 «Кто тяжелее?» (`src/data/level1.ts`, пятый в пачке этого
  прогона: пачка 12-15, 1-3 — пачка идёт по кругу, вернулись к началу
  списка после 15-го уровня). Багов не найдено: все 30 карточек
  (LEVEL1_DATA + LEVEL1_ADDITIONAL) уникальны по `id`, `value` и
  `funFact`, все 30 непустых `imageUri` реально лежат в
  `public/images/` (проверено скриптом) — пустых `imageUri` в этом
  уровне нет. Масса в кг у всех различна и правдоподобна (от синего
  кита 150 000 кг до кошки 4 кг), `comparisonType: 'max'` корректен
  (тяжелее = больше кг). Ранее (см. коммит 1092627 от 2026-09-09) уже
  была устранена дублирующаяся масса медведя и зебры — сейчас повторно
  подтверждено, что дублей значений больше нет. Playwright не
  установлен как зависимость проекта — уровень пройден
  фолбэк-способом: `npm run build` зелёный, все 30 путей к картинкам
  подтверждены на диске, временный Node/esbuild-скрипт
  `dist/qa-sim-level.mjs` повторил логику
  `buildPairQueue`/`generateNewPair` — все 10 раундов без исключений и
  с верно определяемым победителем. `lastReviewedLevelId` теперь `1`.

- QA уровня 15 «Кто спит дольше?» (`src/data/level15.ts`, четвёртый в
  пачке этого прогона: пачка 12-15, 1-3). Багов не найдено: все 20
  карточек уникальны по `id`, `value` и `funFact`, 11 непустых
  `imageUri` (переиспользуют ассеты уровней 1/8/9/13 — тигр, лев,
  ленивец, мышь, кошка, собака, человек, корова, лошадь, слон, жираф)
  реально лежат в `public/images/` (проверено); для 9 карточек без
  картинки (коала, ночница, суслик, хомячок, макака, кролик, свинья,
  коза, овца) подходящего неиспользуемого файла не нашлось — оставлен
  честный эмодзи-фолбэк. Часы сна в сутки все различны и правдоподобны
  (коала лидирует, жираф и слон спят меньше всех — соответствует
  общеизвестным фактам), `comparisonType: 'max'` корректен (спит дольше
  = больше часов). Playwright не установлен как зависимость проекта —
  уровень пройден фолбэк-способом: `npm run build` зелёный, все 11
  путей к картинкам подтверждены на диске, временный Node/esbuild-скрипт
  `dist/qa-sim-level.mjs` повторил логику
  `buildPairQueue`/`generateNewPair` — все 10 раундов без исключений и
  с верно определяемым победителем. `lastReviewedLevelId` теперь `15`.

- QA уровня 14 «У кого крылья шире?» (`src/data/level14.ts`, третий в
  пачке этого прогона: пачка 12-15, 1-3). Багов не найдено: все 20
  карточек уникальны по `id`, `value` и `funFact`, 2 непустых
  `imageUri` (страус, сапсан) реально лежат в `public/images/`
  (проверено); для остальных 18 карточек-птиц подходящих
  неиспользуемых фото в `public/images/` не нашлось — оставлен честный
  эмодзи-фолбэк. Размах крыльев в см все различны и правдоподобны
  (странствующий альбатрос лидирует, колибри замыкает — соответствует
  общеизвестным фактам), `comparisonType: 'max'` корректен (шире =
  больше см). Playwright не установлен как зависимость проекта —
  уровень пройден фолбэк-способом: `npm run build` зелёный, оба пути к
  картинкам подтверждены на диске, временный Node/esbuild-скрипт
  `dist/qa-sim-level.mjs` повторил логику
  `buildPairQueue`/`generateNewPair` — все 10 раундов без исключений и
  с верно определяемым победителем. `lastReviewedLevelId` теперь `14`.

- QA уровня 13 «Кто дольше вынашивает?» (`src/data/level13.ts`, второй в
  пачке этого прогона: пачка 12-15, 1-3). Багов не найдено: все 20
  карточек уникальны по `id`, `value` и `funFact`, 14 непустых
  `imageUri` реально лежат в `public/images/` (проверено); для 6
  карточек без картинки (осёл, коза, овца, свинья, кролик, сирийский
  хомяк) подходящего неиспользуемого файла в `public/images/` не
  нашлось — оставлен честный эмодзи-фолбэк. Сроки беременности в днях
  все различны и правдоподобны, `comparisonType: 'max'` корректен
  (дольше вынашивает = больше дней). Playwright не установлен как
  зависимость проекта — уровень пройден фолбэк-способом: `npm run
  build` зелёный, все 14 путей к картинкам подтверждены на диске,
  временный Node/esbuild-скрипт `dist/qa-sim-level.mjs` повторил логику
  `buildPairQueue`/`generateNewPair` — все 10 раундов без исключений и
  с верно определяемым победителем. `lastReviewedLevelId` теперь `13`.

- QA уровня 12 «Кто ядовитее?» (`src/data/level12.ts`, первый в пачке
  этого прогона: пачка 12-15, 1-3, размер выпал случайно — 7). Багов не
  найдено: все 22 карточки уникальны по `id`, `value` и `funFact`,
  `imageUri` ни у одной не указан — подходящих локальных фото для
  ядовитых видов (змеи, пауки, медузы, скорпионы и т.п.) в
  `public/images/` нет, оставлен честный эмодзи-фолбэк у всех карточек.
  Значения индекса токсичности (0-100) все различны, порядок
  фактчекнут по общеизвестному консенсусу об опасности каждого вида,
  `comparisonType: 'max'` корректен (ядовитее = выше индекс).
  Playwright не установлен как зависимость проекта — уровень пройден
  фолбэк-способом: `npm run build` зелёный, картинок для curl-проверки
  нет, временный Node/esbuild-скрипт `dist/qa-sim-level.mjs` повторил
  логику `buildPairQueue`/`generateNewPair` из `GameLevel.tsx` — все 10
  раундов прошли без исключений и с верно определяемым победителем.
  `lastReviewedLevelId` теперь `12`.

## 2026-09-10

- QA уровня 11 «Что выше?» (`src/data/level11.ts`, шестой и последний в
  пачке этого прогона: уровни 6-11, размер пачки выпал случайно — 6).
  Багов не найдено: все 25 карточек уникальны по `id`, `value` и
  `funFact`, у всех 25 непустых `imageUri` файлы реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200 — пустых
  `imageUri` в этом уровне нет. Высоты правдоподобны (в т.ч. смешение
  зданий, статуй, гор и вулкана Марса для контраста масштабов —
  намеренный приём уровня), `comparisonType: 'max'` корректен (выше =
  больше метров). Playwright недоступен — уровень пройден
  фолбэк-способом: сборка + preview, `curl -sSI` по всем 25 картинкам
  (200 у каждой) и временный Node-скрипт `scratchpad/sim-level.mjs` — все
  10 раундов без исключений. `lastReviewedLevelId` теперь `11`. Этим
  уровнем завершена пачка из 6 уровней (6-11) за сегодня.

- QA уровня 10 «Что калорийнее?» (`src/data/level10.ts`, пятый в пачке
  этого прогона: уровни 6-11). Багов не найдено: все 21 карточка
  уникальны по `id`, `value` и `funFact`, 20 непустых `imageUri` реально
  лежат в `public/images/` и отданы preview-сервером с кодом 200; для
  единственной карточки без картинки (куриная грудка) подходящего
  неиспользуемого файла в `public/images/` не нашлось — оставлен честный
  эмодзи-фолбэк. Значения ккал на 100 г правдоподобны, `comparisonType:
  'max'` корректен (калорийнее = больше ккал). Playwright недоступен —
  уровень пройден фолбэк-способом: сборка + preview, `curl -sSI` по всем
  20 картинкам (200 у каждой) и временный Node-скрипт
  `scratchpad/sim-level.mjs` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `10`.

- QA уровня 9 «Что громче?» (`src/data/level9.ts`, четвёртый в пачке
  этого прогона: уровни 6-11). Багов не найдено: все 19 карточек
  уникальны по `id`, `value` и `funFact`, 3 непустых `imageUri` реально
  лежат в `public/images/` и отданы preview-сервером с кодом 200; для 16
  карточек без картинки (шепот, холодильник, разговор, пылесос,
  газонокосилка, рок-концерт, сирена, петарда, старт ракеты, шелест
  листьев, городской трафик, мотоцикл, бензопила, выстрел из пистолета,
  Тунгусский метеорит, столкновение черных дыр) подходящего
  неиспользуемого файла в `public/images/` не нашлось — оставлен честный
  эмодзи-фолбэк. Значения дБ и факты правдоподобны; экстремальные
  карточки «Тунгусский метеорит» (300 дБ) и «Столкновение черных дыр»
  (Неизмеримо, value 500) — намеренные шуточные карточки вне шкалы, как
  и аналогичные приёмы в других уровнях (например, «Бессмертная медуза» в
  8-м), не баг. `comparisonType: 'max'` корректен (громче = больше дБ).
  Playwright недоступен — уровень пройден фолбэк-способом: сборка +
  preview, `curl -sSI` по всем 3 картинкам (200 у каждой) и временный
  Node-скрипт `scratchpad/sim-level.mjs` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `9`.

- QA уровня 8 «Кто живет дольше?» (`src/data/level8.ts`, третий в пачке
  этого прогона: уровни 6-11). Багов не найдено: все 19 карточек
  уникальны по `id`, `value` и `funFact`, 9 непустых `imageUri` реально
  лежат в `public/images/` и отданы preview-сервером с кодом 200; для 10
  карточек без картинки (муха-поденка, беркут, галапагосская черепаха,
  кролик, шимпанзе, попугай ара, карп кои, гренландский кит,
  океанический куахог, остистая сосна) подходящего неиспользуемого файла
  в `public/images/` не нашлось (близкие по теме файлы — `falcon_...jpg`
  для беркута, `gorilla_...jpg` для шимпанзе, `blue_whale_...jpg` для
  гренландского кита — визуально принадлежат другим видам, подставлять
  не стал) — оставлен честный эмодзи-фолбэк. Значения продолжительности
  жизни и факты правдоподобны (в т.ч. про моллюска Мин — 507 лет,
  реальный рекордсмен), `comparisonType: 'max'` корректен (живёт дольше =
  больше лет). Playwright недоступен — уровень пройден фолбэк-способом:
  сборка + preview, `curl -sSI` по всем 9 картинкам (200 у каждой) и
  временный Node-скрипт `scratchpad/sim-level.mjs` — все 10 раундов без
  исключений. `lastReviewedLevelId` теперь `8`.

- QA уровня 7 «Что дороже?» (`src/data/level7.ts`, второй в пачке этого
  прогона: уровни 6-11). Багов не найдено: все 20 карточек уникальны по
  `id`, `value` и `funFact`, 9 непустых `imageUri` реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200; для 11 карточек
  без картинки (кофе, ноутбук, часы Rolex, слиток золота, частный
  самолет, бутылка воды, билет в кино, видеоигра, новый автомобиль, дом в
  США, истребитель F-35) подходящего неиспользуемого файла в
  `public/images/` не нашлось (единственные формально близкие по теме
  файлы — старые компьютеры `computer_...jpg`/`pc_...jpg` и
  `passenger_jet_...jpg` — визуально не соответствуют современному
  игровому ноутбуку и частному/военному самолету, подставлять не стал) —
  оставлен честный эмодзи-фолбэк. Цены правдоподобны, `comparisonType:
  'max'` корректен (дороже = больше $). Playwright недоступен — уровень
  пройден фолбэк-способом: сборка + preview, `curl -sSI` по всем 9
  картинкам (200 у каждой) и временный Node-скрипт
  `scratchpad/sim-level.mjs` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `7`.

- QA уровня 6 «Где глубже?» (`src/data/level6.ts`, первый в пачке этого
  прогона: размер пачки выпал случайно — 6, уровни 6-11). Багов не
  найдено: все 17 карточек уникальны по `id`, `value` и `funFact`, 10
  непустых `imageUri` реально лежат в `public/images/` и отданы
  preview-сервером с кодом 200; для 7 карточек без картинки (озеро
  Байкал, рекорд дайвинга, бассейн, пещера Крубера, станция метро,
  морской слон, клюворыл Кювье) подходящего неиспользуемого файла в
  `public/images/` не нашлось — оставлен честный эмодзи-фолбэк (единственный
  формально похожий файл, `dolphin_...jpg`, уже занят другим уровнем и
  визуально не соответствует клюворылу — подставлять не стал).
  `comparisonType: 'max'` корректен (глубже = больше метров). Playwright
  недоступен (пакет отсутствует в `devDependencies`, установка без
  интернета не гарантирована) — уровень пройден фолбэк-способом: сборка +
  preview, `curl -sSI` по всем 10 картинкам (200 у каждой) и временный
  Node-скрипт `scratchpad/sim-level.mjs` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `6`.

## 2026-09-09

- QA уровня 5 «Что горячее?» (`src/data/level5.ts`, четвёртый и последний в
  пачке этого прогона: 2-5, размер пачки в этом прогоне выпал случайно —
  4). Найден и исправлен неточный `funFact`: карточка «Поверхность
  Солнца» (5 500 °C) описывала температуру **ядра** Солнца («15
  миллионов градусов») — чужой факт, к тому же дублирующий тему отдельной
  карточки «Ядро Солнца» (`sun_core`, 15 000 000 °C) того же уровня.
  Заменён на факт именно о поверхности (фотосфере) — что это видимый нами
  слой звезды и самый холодный из её слоёв. Все 17 карточек уникальны по
  `id`, `value` и `funFact`. 9 непустых `imageUri` реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200; для 8 карточек
  без картинки (поверхность Солнца, лава, кипящая вода, жидкий азот,
  печь для пиццы, вольфрамовая нить, ядро Солнца, плавление железа)
  подходящего неиспользуемого файла в `public/images/` не нашлось —
  оставлен честный эмодзи-фолбэк. `comparisonType: 'max'` корректен
  (горячее = больше °C). Playwright недоступен — уровень пройден
  фолбэк-способом: сборка + preview, `curl -sSI` по всем 9 картинкам (200
  у каждой) и временный Node-скрипт `scratchpad/sim-level.mjs` — все 10
  раундов без исключений. `lastReviewedLevelId` теперь `5`. Этим уровнем
  завершена пачка из 4 уровней (2-5) за сегодня.

- QA уровня 4 «Что больше?» (`src/data/level4.ts`, третий в пачке этого
  прогона: 2-5). Багов не найдено: все 21 карточка уникальны по `id`,
  `value` и `funFact`, 15 непустых `imageUri` реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200; для 6 карточек
  без картинки (теннисный корт, дорожка боулинга, олимпийский бассейн,
  вагон поезда, авианосец, подводная лодка) подходящего неиспользуемого
  файла в `public/images/` не нашлось — оставлен честный эмодзи-фолбэк.
  Значения длины/высоты в метрах правдоподобны (Великая Китайская стена
  специально хранит суммарную длину в метрах — 21 196 000 м — это
  реальная справочная цифра с учётом всех ответвлений, не опечатка).
  `comparisonType: 'max'` корректен. Playwright недоступен — уровень
  пройден фолбэк-способом: сборка + preview, `curl -sSI` по всем 15
  картинкам (200 у каждой) и временный Node-скрипт
  `scratchpad/sim-level.mjs` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `4`.

- QA уровня 3 «Что древнее?» (`src/data/level3.ts`, второй в пачке этого
  прогона: 2-5). Найдены и исправлены 2 пары карточек с совпадающим
  `value` (пары с равным `value` не участвуют в сравнениях — см.
  `buildPairQueue`): «Интернет (ARPANET)» и «Высадка на Луну» стояли на
  одном 1969 годе, «Первый кинофильм» и «Радио» — на 1895. Вместо
  выдумки значения уточнены до реальных дат тех же событий: высадка на
  Луну — 20 июля 1969 (0.55 доли года), сообщение ARPANET — 29 октября
  1969 (0.83) — Луна раньше по факту, что и сохраняет верный порядок для
  `comparisonType: 'min'`; демонстрации радиосвязи Маркони — лето 1895
  (0.66), киносеанс братьев Люмьер — 28 декабря 1895 (0.99). `value`
  теперь дробный только для этих 4 карточек, `displayValue` не менялся
  (по-прежнему показывает только год), `funFact` у них дополнен точной
  датой. Все 24 карточки уникальны по `id`, `value` и `funFact`
  (проверено Node-скриптом). Непустые `imageUri` (19 из 24) реально лежат
  в `public/images/` и отданы preview-сервером с кодом 200; для 5 карточек
  без картинки (печатный станок, кинофильм, компас, телескоп, паровой
  двигатель) в `public/images/` не нашлось подходящего неиспользуемого
  файла — оставлен честный эмодзи-фолбэк. `comparisonType: 'min'`
  корректен («древнее» = меньшее/более отрицательное значение года).
  Playwright недоступен — уровень пройден фолбэк-способом: сборка +
  preview, `curl -sSI` по всем 19 картинкам (200 у каждой) и временный
  Node-скрипт `scratchpad/sim-level.mjs`, повторяющий
  `buildPairQueue`/`generateNewPair` — все 10 раундов без исключений.
  `lastReviewedLevelId` теперь `3`.

- QA уровня 2 «Кто быстрее?» (`src/data/speeds.ts`, первый в пачке этого
  прогона из 4 уровней: 2-5, размер пачки в этом прогоне выбран случайно —
  выпало 4). Багов не найдено: все 20 `imageUri` реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200 (проверено
  `curl -sSI` по каждому пути), эмодзи-фолбэк на уровне не требуется.
  Значения максимальной скорости (км/ч) правдоподобны и соответствуют
  справочным данным, `funFact` у каждой карточки не пустой и без дублей
  между карточками. Совпадающие `value` у части пар (72, 65, 60 км/ч у
  разных животных) — реальные справочные цифры, а не баг: пары с равным
  `value` в игру всё равно не попадают (см. `buildPairQueue`), а из 20
  карточек остаётся более 180 уникальных пар для сравнения, чего с запасом
  хватает на 10 раундов. `comparisonType: 'max'` в `levels.ts` корректен
  (выше скорость — вернее ответ). Playwright/Chromium в окружении не
  установлен (нет `@playwright/test` в зависимостях и сети для его
  установки нет) — уровень пройден фолбэк-способом: `npm run build && npm
  run preview -- --port 4321 --strictPort`, затем `curl -sSI` по всем 20
  картинкам (200 у каждой) и временный Node-скрипт
  (`scratchpad/sim-level.mjs`, в репозиторий не коммитился), повторяющий
  логику `buildPairQueue`/`generateNewPair` из `GameLevel.tsx` — все 10
  раундов построены без исключений, ни одна пара с одинаковым `value` в
  игру не попала. `lastReviewedLevelId` теперь `2`.

## 2026-09-08

- QA уровня 1 «Кто тяжелее?» (`src/data/level1.ts`, последний в пачке
  этого прогона — 10 уровней: 7-15, 1, циклически). Найдена и исправлена
  1 пара карточек с совпадающим значением массы (пара с равным `value` не
  участвует в сравнениях — см. `buildPairQueue`): «Медведь Гризли» и
  «Зебра» обе стояли на 350 кг — вес гризли уточнён до 300 кг (типичный
  средний вес взрослого гризли по справочным данным лежит в широком
  диапазоне 180–360 кг, 300 — реалистичное среднее, отличное от веса
  зебры). Теперь все 30 карточек (20 из `LEVEL1_DATA` + 10 из
  `LEVEL1_ADDITIONAL`) уникальны по `id`, `value` и `funFact` (проверено
  скриптом `tsx` — 30/30, дублей после правки не осталось). Все 30
  `imageUri` реально лежат в `public/images/` и отданы preview-сервером с
  кодом 200 — картинок, требующих эмодзи-фолбэка, на этом уровне нет.
  `comparisonType: 'max'` корректен. Уровень пройден headless-браузером
  (Playwright/Chromium): `npm run build && npm run preview -- --port 4321
  --strictPort --base /GuessWhat/`, пройдены реальным кликом все 10
  раундов, появилась победная модалка, консоль чистая (кроме ожидаемого
  недоступного в песочнице Google Fonts CDN), ни одного 404 на картинки
  уровня. `lastReviewedLevelId` теперь `1`. Этим уровнем завершена пачка
  из 10 уровней (7-15, 1) за сегодня.

- QA уровня 15 «Кто спит дольше?» (`src/data/level15.ts`, девятый из 10
  уровней пачки этого прогона: 7-15, 1). Найдены и исправлены
  4 пары карточек с совпадающим значением сна (пары с равным `value` не
  участвуют в сравнениях — см. `buildPairQueue`), из 20 карточек это
  задевало сразу 8: «Лев» и «Золотистый хомячок» (обе 14 ч), «Домашняя
  кошка» и «Домовая мышь» (обе 12 ч), «Кролик», «Человек» и «Свинья» (все
  три 8 ч), «Корова» и «Овца» (обе 4 ч). Вместо удаления карточек значения
  уточнены до более точных десятичных цифр из того же источника, на
  который уже ссылается комментарий к файлу (сравнительные исследования
  сна млекопитающих Allison & Cicchetti, 1976): лев 13.5 ч, кошка 12.1 ч,
  кролик 8.4 ч, свинья 7.8 ч, корова 3.9 ч, овца 3.8 ч; хомячок (14 ч) и
  человек (8 ч) и мышь (12 ч) оставлены без изменений как более округлые
  общепринятые цифры. Теперь все 20 карточек уникальны по `id`, `value` и
  `funFact` (проверено скриптом `tsx` — 20/20). 8 из 20 `imageUri`
  реально лежат в `public/images/` и отданы preview-сервером с кодом 200;
  для оставшихся 12 карточек подходящих локальных фото нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен. Уровень заблокирован
  по умолчанию (открывается после уровня 14) — для QA прогресс временно
  разблокирован через localStorage, без изменений в самой игре. Пройден
  headless-браузером (Playwright/Chromium): `npm run build && npm run
  preview -- --port 4321 --strictPort --base /GuessWhat/`, пройдены
  реальным кликом все 10 раундов, появилась победная модалка, консоль
  чистая (кроме ожидаемого недоступного в песочнице Google Fonts CDN), ни
  одного 404 на картинки уровня. `lastReviewedLevelId` теперь `15`.

- QA уровня 14 «У кого крылья шире?» (`src/data/level14.ts`) — багов не
  найдено. Все 20 карточек уникальны по `id`, `value` и `funFact`
  (проверено скриптом `tsx` — 20/20). Размах крыльев сверен на
  правдоподобность: странствующий альбатрос 340 см (крупнейший размах
  среди живых птиц), сапсан 100 см при скорости пикирования свыше 300
  км/ч (самое быстрое живое существо), колибри 10 см с частотой взмахов
  до 50/с — расхождений не найдено. 2 из 20 `imageUri` (сапсан, страус)
  переиспользуют ассеты уровней 2 и 11 и реально отданы preview-сервером
  с кодом 200; для остальных 18 карточек подходящих локальных фото птиц в
  репозитории нет — честный эмодзи-фолбэк, как и задокументировано в
  комментарии к файлу. `comparisonType: 'max'` корректен. Уровень
  заблокирован по умолчанию (открывается после уровня 13) — для QA
  прогресс временно разблокирован через localStorage, без изменений в
  самой игре. Пройден headless-браузером (Playwright/Chromium): `npm run
  build && npm run preview -- --port 4321 --strictPort --base
  /GuessWhat/`, пройдены реальным кликом все 10 раундов, появилась
  победная модалка, консоль чистая (кроме ожидаемого недоступного в
  песочнице Google Fonts CDN), ни одного 404 на картинки уровня.
  `lastReviewedLevelId` теперь `14`.

- QA уровня 13 «Кто дольше вынашивает?» (`src/data/level13.ts`) — багов не
  найдено. Все 20 карточек уникальны по `id`, `value` и `funFact`
  (проверено скриптом `tsx` — 20/20). Сроки беременности сверены на
  правдоподобность: слон 660 дней (самый долгий срок среди наземных
  млекопитающих), свинья 114 дней (в точности «три месяца, три недели, три
  дня» — 90+21+3, как и говорит `funFact`), хомяк 16 дней (один из самых
  коротких сроков среди млекопитающих) — расхождений не найдено; для
  медведя и панды `value` намеренно учитывает весь срок при задержанной
  имплантации, как и задокументировано в комментарии к файлу. 14 из 20
  `imageUri` реально лежат в `public/images/` и отданы preview-сервером с
  кодом 200; для оставшихся 6 карточек (осёл, коза, овца, свинья, кролик,
  сирийский хомяк) подходящих локальных фото нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен. Уровень заблокирован
  по умолчанию (открывается после уровня 12) — для QA прогресс временно
  разблокирован через localStorage, без изменений в самой игре. Пройден
  headless-браузером (Playwright/Chromium): `npm run build && npm run
  preview -- --port 4321 --strictPort --base /GuessWhat/`, пройдены
  реальным кликом все 10 раундов, появилась победная модалка, консоль
  чистая (кроме ожидаемого недоступного в песочнице Google Fonts CDN), ни
  одного 404 на картинки уровня. `lastReviewedLevelId` теперь `13`.

- QA уровня 12 «Кто ядовитее?» (`src/data/level12.ts`) — багов не найдено.
  Все 22 карточки уникальны по `id`, `value` и `funFact` (проверено
  скриптом `tsx` — 22/22). `value` здесь — условный индекс токсичности
  (0–100), а не точное LD50 — намеренное решение, задокументированное в
  комментарии к файлу, чтобы не выдумывать точные мг/кг без доступа к
  первоисточникам; порядок значений сверен с общеизвестным консенсусом
  (тайпан Маклея, кубомедуза, ужасный листолаз — в топе; медоносная пчела,
  утконос — внизу) и расхождений не найдено. `imageUri` намеренно пуст у
  всех карточек (подходящих локальных фото ядовитых видов в
  `public/images/` нет) — честный эмодзи-фолбэк, тоже задокументировано в
  файле. `comparisonType: 'max'` корректен. Уровень по умолчанию
  заблокирован (открывается после уровня 11) — для QA прогресс временно
  разблокирован через localStorage (`guesswhat:progress:v1`), без изменений
  в самой игре. Пройден headless-браузером (Playwright/Chromium): `npm run
  build && npm run preview -- --port 4321 --strictPort --base
  /GuessWhat/`, пройдены реальным кликом все 10 раундов, появилась
  победная модалка, консоль чистая (кроме ожидаемого недоступного в
  песочнице Google Fonts CDN), ни одного 404 (картинок для запроса и нет —
  эмодзи не грузятся по сети). `lastReviewedLevelId` теперь `12`.

- QA уровня 11 «Что выше?» (`src/data/level11.ts`) — багов не найдено. Все
  25 карточек уникальны по `id`, `value` и `funFact` (проверено скриптом
  `tsx` — 25/25), и у всех 25 есть рабочий `imageUri` (реально лежат в
  `public/images/`, отданы preview-сервером с кодом 200) — картинок,
  требующих эмодзи-фолбэка, на этом уровне нет. Высоты сверены на
  правдоподобность: Бурдж-Халифа 828 м, Эверест 8848 м, K2 8611 м,
  Олимп на Марсе ~21900 м (высочайший вулкан Солнечной системы), Статуя
  Единства 182 м (самая высокая статуя в мире) — расхождений не найдено;
  факт про Шанхайскую башню (закручена на 120°, двойной фасад экономит
  энергию) уже актуален после правки в предыдущем прогоне QA.
  `comparisonType: 'max'` корректен. Уровень пройден headless-браузером
  (Playwright/Chromium): `npm run build && npm run preview -- --port 4321
  --strictPort --base /GuessWhat/`, пройдены реальным кликом все 10
  раундов, появилась победная модалка, консоль чистая (кроме ожидаемого
  недоступного в песочнице Google Fonts CDN), ни одного 404 на картинки
  уровня. `lastReviewedLevelId` теперь `11`.

- QA уровня 10 «Что калорийнее?» (`src/data/level10.ts`) — багов не
  найдено. Все 21 карточек уникальны по `id`, `value` и `funFact`
  (проверено скриптом `tsx` — 21/21). Калорийность на 100 г/мл сверена с
  типичными справочными значениями: огурец 15, брокколи 34, картофель 77,
  куриная грудка 165, говядина 250, сыр чеддер 402, шоколад 535, грецкий
  орех 654, сливочное масло 717, оливковое масло 884, сало 900, водка 231
  (за счёт спирта) — расхождений не найдено. 20 из 21 `imageUri` реально
  лежат в `public/images/` и отданы preview-сервером с кодом 200; для
  «Куриной грудки» подходящего локального фото нет — честный
  эмодзи-фолбэк (среди неиспользуемых файлов `public/images/` куриного
  мяса тоже не нашлось). `comparisonType: 'max'` корректен. Уровень
  пройден headless-браузером (Playwright/Chromium): `npm run build && npm
  run preview -- --port 4321 --strictPort --base /GuessWhat/`, пройдены
  реальным кликом все 10 раундов, появилась победная модалка, консоль
  чистая (кроме ожидаемого недоступного в песочнице Google Fonts CDN), ни
  одного 404 на картинки уровня. `lastReviewedLevelId` теперь `10`.

- QA уровня 9 «Что громче?» (`src/data/level9.ts`). Найдены и исправлены 2
  пары карточек с совпадающим значением громкости (пары с равным `value`
  не попадают в игру — см. `buildPairQueue`): «Рок-концерт» и «Бензопила»
  обе стояли на 110 дБ — концерт уточнён до 115 дБ (источники обычно дают
  диапазон 100–120 дБ у сцены, бензопила остаётся на типичных 110 дБ);
  «Сирена скорой» и «Раскат грома» обе стояли на 120 дБ — гром уточнён до
  130 дБ (ближний раскат грома нередко указывают в диапазоне 120–140 дБ),
  `funFact` грома заодно уточнён (добавлена причина — резкое расширение
  воздуха от жара молнии). Также переписан почти неинформативный и
  дублирующий по смыслу с «Газонокосилкой» факт «Городского трафика»
  (был: «Длительное воздействие может портить слух.» — то же самое уже
  говорит газонокосилка) на более конкретный: связь постоянного шума
  мегаполиса со стрессом и нарушениями сна (по данным ВОЗ). Все 19
  карточек теперь уникальны по `id`, `value` и `funFact` (проверено
  скриптом `tsx` — 19/19). 3 из 19 `imageUri` проверены — реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200; для оставшихся
  16 карточек подходящих локальных фото нет (единственный неиспользуемый
  файл `sound_...jpg` слишком общий, чтобы уверенно привязать к
  конкретному звуку — оставлен честный эмодзи-фолбэк, а не угадывание).
  `comparisonType: 'max'` корректен. Уровень пройден headless-браузером
  (Playwright/Chromium): `npm run build && npm run preview -- --port 4321
  --strictPort --base /GuessWhat/`, пройдены реальным кликом все 10
  раундов, появилась победная модалка, консоль чистая (кроме ожидаемого
  недоступного в песочнице Google Fonts CDN), ни одного 404 на картинки
  уровня. `lastReviewedLevelId` теперь `9`.

- QA уровня 8 «Кто живет дольше?» (`src/data/level8.ts`). Найден и
  исправлен 1 неточный факт: у карточки «Собака» было указано, что самая
  старая собака в мире прожила 31 год — по данным Книги рекордов Гиннесса
  рекорд принадлежит австралийской пастушьей собаке Bluey (1910–1939) и
  составляет 29 лет 5 месяцев; текст факта исправлен на «29 лет» со
  ссылкой на источник. Остальные 18 карточек уникальны по `id`, `value` и
  `funFact` (проверено скриптом `tsx` — 19/19 по всем трём).
  Продолжительности жизни сверены на правдоподобность: гренландская акула
  ~400 лет и рост ~1 см/год (исследование 2016 года), моллюск-океанический
  куахог 507 лет (знаменитый «Мин»), сосна долговечная 5000 лет (дерево
  Мафусаил, старше пирамид), секвойя 3000 лет — расхождений больше не
  найдено. 9 из 19 `imageUri` проверены — реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200; для оставшихся
  10 карточек (подёнка, беркут, галапагосская черепаха, кролик, шимпанзе,
  ара, карп кои, гренландский кит, куахог, сосна долговечная) подходящих
  локальных фото нет — честный эмодзи-фолбэк. `comparisonType: 'max'`
  корректен. Уровень пройден headless-браузером (Playwright/Chromium):
  `npm run build && npm run preview -- --port 4321 --strictPort --base
  /GuessWhat/`, пройдены реальным кликом все 10 раундов, появилась
  победная модалка, консоль чистая (кроме ожидаемого недоступного в
  песочнице Google Fonts CDN), ни одного 404 на картинки уровня.
  `lastReviewedLevelId` теперь `8`.

- QA уровня 7 «Что дороже?» (`src/data/level7.ts`, первый в пачке этого
  прогона — 10 уровней: 7-15, 1). Найден и исправлен 1 дефект: карточки
  «Игровой ноутбук» (`laptop`) и «Ноутбук Pro» (`laptop_pro`) описывали по
  сути один и тот же товар с одинаковой ценой $2500 — пара с равным
  `value` не участвует в сравнениях (см. `buildPairQueue`), а сами
  карточки почти дублировали друг друга по смыслу; менее содержательная
  «Ноутбук Pro» убрана. Оставшиеся 20 карточек уникальны по `id`, `value` и
  `funFact` (проверено скриптом `tsx` — 20/20 по всем трём). 9 из 20
  `imageUri` проверены — реально лежат в `public/images/` и отданы
  preview-сервером с кодом 200; для оставшихся 11 карточек (кофе, ноутбук,
  часы Rolex, слиток золота, частный самолёт, бутылка воды, билет в кино,
  видеоигра, новый автомобиль, дом в США, истребитель F-35) подходящих
  локальных фото нет — честный эмодзи-фолбэк, среди неиспользуемых
  файлов `public/images/` совпадений по смыслу тоже не нашлось.
  `comparisonType: 'max'` корректен («что дороже» — верно большее
  значение). Цены сверены на правдоподобность (Мона Лиза ~$860 млн
  страховой оценки, МКС ~$150 млрд, телескоп Хаббл ~$16 млрд, Бурдж
  Халифа ~$1.5 млрд постройки) — расхождений не найдено. Уровень пройден
  headless-браузером (Playwright/Chromium из `/opt/pw-browsers`, запущен
  без установки новых npm-пакетов через `NODE_PATH` на глобальный модуль
  `/opt/node22/lib/node_modules/playwright`): собранная прод-сборка
  поднята через `npm run build && npm run preview -- --port 4321
  --strictPort --base /GuessWhat/` (без явного `--base` preview-сервер
  раздаёт ассеты с корня, а собранный `index.html` ссылается на
  `/GuessWhat/assets/...` — с флагом пути совпадают), пройдены реальным
  кликом все 10 раундов, появилась победная модалка «Уровень Пройден!»,
  консоль браузера чистая (кроме ожидаемого `ERR_CONNECTION_RESET` при
  недоступном в песочнице Google Fonts CDN — не связано с игрой), ни
  одного 404 на картинки уровня. `lastReviewedLevelId` теперь `7`.

## 2026-09-07

- QA уровня 6 «Где глубже?» (`src/data/level6.ts`, последний в пачке этого
  прогона — 10 уровней: 12-15, 1-6). Все 17 карточек уникальны по `id`,
  `funFact` и значению глубины (проверено скриптом — 17/17 по всем трём).
  10 из 17 `imageUri` проверены — реально лежат в `public/images/` и
  отданы preview-сервером с кодом 200 (`curl -sSI`); для оставшихся 7
  карточек (Байкал, рекорд дайвинга, бассейн, пещера Крубера, метро,
  морской слон, клюворыл Кювье) подходящих локальных фото нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен. Значения сверены на
  правдоподобность: Марианская впадина 10,994 м, Кольская скважина 12,262
  м (глубочайшая скважина в мире), пещера Крубера 2,199 м (глубочайшая
  известная пещера), рекорд Ахмеда Габра 332 м (2014), станция
  «Арсенальная» в Киеве 105.5 м (глубочайшая станция метро в мире) —
  расхождений не найдено. Использование высоты Бурдж-Халифы (828 м) как
  карточки для наглядного масштаба глубин — намеренный приём, не баг.
  Багов не найдено, правок не потребовалось. Уровень пройден
  фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по всем 10 путям картинок (200), временный
  Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии по 10
  раундов без исключений. Этим уровнем завершена пачка 12-6 за сегодня;
  `lastReviewedLevelId` теперь `6`.

- QA уровня 5 «Что горячее?» (`src/data/level5.ts`). Найдены и исправлены 2
  недочёта: (1) «Лава» и «Магма» имели одинаковое значение 1200 °C — при
  том что физически это разные состояния одного вещества (магма ещё под
  землёй немного горячее, лава уже остыла при извержении), значение магмы
  поднято до 1300 °C, а `funFact` лавы уточнён («...немного остывает по
  пути»), пара перестала выпадать из сравнения; (2) у карточки «Горение
  бумаги» не было `imageUri`, хотя подходящее локальное фото бумаги уже
  есть в `public/images/` (используется уровнем 3 для карточки «Бумага») —
  подключено повторно. Остальные 16 карточек уникальны по `id` и `funFact`
  (проверено скриптом — 17/17). 9 из 17 `imageUri` (после правки)
  проверены — реально лежат в `public/images/` и отданы preview-сервером с
  кодом 200 (`curl -sSI`); для оставшихся 8 карточек (Солнце, лава,
  кипящая вода, жидкий азот, печь для пиццы, лампа накаливания, ядро
  Солнца, плавление железа) подходящих локальных фото нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен, значения сверены на
  правдоподобность (плавление железа 1538 °C, точка кипения воды на
  Эвересте ~68 °C, самовоспламенение бумаги по Фаренгейту 451 = 232 °C).
  Уровень пройден фолбэк-способом: `npm run build && npm run preview --
  --port 4321 --strictPort`, `curl -sSI` по всем 9 путям картинок (200),
  временный Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии
  по 10 раундов без исключений.

- QA уровня 4 «Что больше?» (`src/data/level4.ts`). Все 21 карточка
  уникальны по `id`, `funFact` и значению длины/высоты (проверено скриптом
  — 21/21 по всем трём). 15 из 21 `imageUri` проверены — реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200 (`curl -sSI`); для
  оставшихся 6 карточек (теннисный корт, дорожка для боулинга, олимпийский
  бассейн, вагон поезда, авианосец, подводная лодка) подходящих локальных
  фото нет — честный эмодзи-фолбэк. `comparisonType: 'max'` корректен.
  Значения сверены на правдоподобность: теннисный корт 23.77 м и дорожка
  для боулинга 18.29 м — точные официальные размеры (ITF/стандарт
  боулинга), Великая Китайская стена 21,196 км соответствует итогам
  обмера 2012 года, Бурдж-Халифа как самое высокое сооружение в мире —
  всё ещё актуально. Багов не найдено, правок не потребовалось. Уровень
  пройден фолбэк-способом: `npm run build && npm run preview -- --port
  4321 --strictPort`, `curl -sSI` по всем 15 путям картинок (200),
  временный Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии
  по 10 раундов без исключений.

- QA уровня 3 «Что древнее?» (`src/data/level3.ts`). Дубликатов `id` и
  `funFact` нет (проверено скриптом — 24/24 уникальных факта), 22 из 24
  значений года уникальны (Интернет/Высадка на Луну — оба 1969, Кино/Радио
  — оба 1895: не баг, а реальное историческое совпадение, оба события
  документально датированы этим годом). 19 из 24 `imageUri` проверены —
  реально лежат в `public/images/` и отданы preview-сервером с кодом 200
  (`curl -sSI`); для оставшихся 5 карточек (печатный станок, кино, компас,
  телескоп, паровой двигатель) подходящих локальных фото нет — честный
  эмодзи-фолбэк. `comparisonType: 'min'` корректен для «что древнее»
  (меньше/отрицательнее год — правильный ответ, отрицательные значения для
  дат до н.э. работают верно). Факты сверены на правдоподобность (Гутенберг
  1440, порох ~850 н.э., телескоп 1608/Галилей, первое фото Ньепса 1826 с
  8-часовой выдержкой, ARPANET/прилунение 1969, биткоин 2009 и т.д.) —
  расхождений не найдено. Багов не найдено, правок не потребовалось.
  Уровень пройден фолбэк-способом: `npm run build && npm run preview --
  --port 4321 --strictPort`, `curl -sSI` по всем 19 путям картинок (200),
  временный Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии
  по 10 раундов без исключений.

- QA уровня 2 «Кто быстрее?» (`src/data/speeds.ts`). Дубликатов `id` и
  `funFact` нет (проверено скриптом — 20/20 уникальных фактов), все 20
  `imageUri` реально лежат в `public/images/` и отданы preview-сервером с
  кодом 200 (`curl -sSI` по всем 20 путям). `comparisonType: 'max'`
  корректен, `subtitle` «Скоростные чемпионы» без опечатки (баг
  «Скорстные чемпионы» пофикшен ещё 2026-08-15). 16 из 20 значений скорости
  уникальны (совпадения: грейхаунд/лось по 72 км/ч, кенгуру 71 не
  совпадает ни с кем, койот/зебра/верблюд по 65 км/ч, жираф/дельфин по 60
  км/ч) — исключает всего 5 пар сравнения из 190 возможных, на
  играбельность не влияет. Найдена крупная находка по интересности: почти
  все `funFact` заметно короче и шаблоннее, чем в остальных уровнях
  («Опасный и быстрый хищник.», «Обгоняют человека на суше.») — сама по
  себе не баг (факты корректны и не дублируются), но заметно ниже
  качеством контента, чем соседние уровни; заведена отдельная задача в
  `TASKS.md` («Обогатить funFact уровня 2»), в рамках этого прогона не
  переписывалась. Уровень пройден фолбэк-способом: `npm run build && npm
  run preview -- --port 4321 --strictPort`, `curl -sSI` по всем 20 путям
  картинок (200), временный Node/esbuild-скрипт (не закоммичен) — 3
  симулированные сессии по 10 раундов без исключений.

- QA уровня 1 «Кто тяжелее?» (`src/data/level1.ts`, 5-й в пачке этого
  прогона — цикл дошёл до конца списка реальных уровней (15) и продолжился
  с начала). Все 30 карточек (`LEVEL1_DATA` + `LEVEL1_ADDITIONAL`)
  уникальны по `id` и `funFact` (проверено скриптом — 30/30 уникальных
  фактов), 29 из 30 значений массы уникальны (у `grizzly-bear`/`zebra`
  совпадение 350 кг — задокументированный намеренный кейс для проверки
  логики ничьей в `GameLevel.tsx`, не баг). Все 30 `imageUri` реально
  лежат в `public/images/` и отданы preview-сервером с кодом 200 (`curl
  -sSI` по всем 30 путям). `comparisonType: 'max'` корректен (тяжелее —
  верный ответ), `property`/`unit` в `levels.ts` соответствуют данным.
  Багов не найдено, правок не потребовалось. Уровень пройден
  фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по всем 30 путям картинок (200), временный
  Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии по 10
  раундов без исключений.

- QA уровня 15 «Кто спит дольше?» (`src/data/level15.ts`, последний в пачке
  этого прогона). Дубликатов `id` нет, `funFact` везде непустой и
  содержательно различается между карточками (в т.ч. у пар с одинаковым
  числом часов сна — мышь/кошка по 12 ч, кролик/человек/свинья по 8 ч,
  корова/овца по 4 ч — факты не повторяют друг друга, а описывают разные
  причины; таких коллизий всего 5 из 190 возможных пар, на разнообразие
  игры это не влияет). 11 из 20 `imageUri` проверены — реально лежат в
  `public/images/` и отданы preview-сервером с кодом 200 (`curl -sSI`); для
  оставшихся 9 видов (коала, ночница, суслик, хомячок, макака, кролик,
  свинья, коза, овца) подходящих локальных фото по-прежнему нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен, `title`/`subtitle`/
  `property`/`unit` в `levels.ts` соответствуют данным, оговорка про сон
  ленивца в неволе на месте. Багов не найдено, правок не потребовалось.
  Уровень пройден фолбэк-способом: `npm run build && npm run preview --
  --port 4321 --strictPort`, `curl -sSI` по всем 11 путям картинок (200),
  временный Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии
  по 10 раундов без исключений. Уровнем 15 закончились реальные уровни
  цикла — следующий уровень пачки (16-30) заглушки «Секрет» и не входит в
  выборку, поэтому дальше пачка продолжится циклически с уровня 1.

- QA уровня 14 «У кого крылья шире?» (`src/data/level14.ts`). Все 20
  карточек уникальны по `id` и значению размаха крыльев, `funFact` везде
  непустой и без смысловых повторов между карточками. 2 из 20 `imageUri`
  (страус, сапсан) проверены — реально лежат в `public/images/` и отданы
  preview-сервером с кодом 200 (`curl -sSI`); для остальных 18 видов птиц
  подходящих локальных фото в `public/images/` по-прежнему нет — честный
  эмодзи-фолбэк. `comparisonType: 'max'` корректен (шире размах — верный
  ответ), `title`/`subtitle`/`property`/`unit` в `levels.ts` соответствуют
  данным. Багов не найдено, правок в данные не потребовалось. Уровень
  пройден фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по обоим путям картинок (200), временный
  Node/esbuild-скрипт (не закоммичен) — 3 симулированные сессии по 10
  раундов без исключений и без пар с равными значениями.

- QA уровня 13 «Кто дольше вынашивает?» (`src/data/level13.ts`). Все 20
  карточек уникальны по `id` и значению срока беременности, `funFact`
  везде непустой. Найден и исправлен один недочёт из шага 2 (интересность):
  факты домовой мыши и сирийского хомяка были почти дословными близнецами
  («Один из самых коротких сроков беременности среди...млекопитающих...»)
  — переписан факт хомяка на конкретику (повторная беременность сразу после
  родов, открытие глаз на 14-й день), дублирования по смыслу больше нет. 14
  из 20 `imageUri` проверены — реально лежат в `public/images/` и отданы
  preview-сервером с кодом 200 (`curl -sSI` по всем 14 путям); для
  оставшихся 6 карточек без картинки (осёл, коза, овца, свинья, кролик,
  хомяк) в `public/images/` по-прежнему нет подходящих файлов — честный
  эмодзи-фолбэк, ничего не подставлено наугад. `comparisonType: 'max'`
  корректен, `title`/`subtitle`/`property`/`unit` в `levels.ts`
  соответствуют данным, задержанная имплантация у медведя/панды учтена
  правильно (полный срок от спаривания до родов, как принято в
  зоологических источниках). Уровень пройден фолбэк-способом: `npm run
  build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по
  всем путям картинок (200), временный Node/esbuild-скрипт (не закоммичен)
  — 3 симулированные сессии по 10 раундов без исключений и без пар с
  равными значениями.

- QA уровня 12 «Кто ядовитее?» (`src/data/level12.ts`, первый в пачке этого
  прогона). Проверено скриптом: все 22 карточки уникальны по `id`, значения
  индекса токсичности не повторяются (22 уникальных из 22), `funFact` не
  пустой и не дублируется ни у одной пары карточек. Ни у одной карточки нет
  `imageUri` — это осталось верным: подходящих локальных фото ядовитых
  видов (змеи, медузы, пауки, скорпионы, утконос и т.д.) в `public/images/`
  по-прежнему нет, эмодзи-фолбэк оправдан. `comparisonType: 'max'` корректен
  для «кто ядовитее» (выше индекс — правильный ответ), `title`/`subtitle`/
  `property`/`unit` в `levels.ts` соответствуют данным. Факты сверены на
  правдоподобность — соответствуют общеизвестному научному консенсусу об
  опасности каждого вида, повторов чисел/фактов нет. Багов не найдено,
  правок в код/данные не потребовалось. Уровень пройден фолбэк-способом
  (браузер недоступен): временный Node/esbuild-скрипт (не закоммичен),
  повторяющий логику `buildPairQueue`/`generateNewPair` из `GameLevel.tsx`,
  — 3 симулированные сессии по 10 раундов без исключений, пар с равными
  значениями не возникает; `imageUri`-проверка через preview-сервер не
  требовалась, так как ни у одной карточки нет `imageUri`.


- QA уровня 11 «Что выше?» (`src/data/level11.ts`, 25 карточек, последний в
  пачке этого прогона). В отличие от уровней 4-10, дубликатов карточек не
  найдено — все 25 объектов и значений высоты уникальны. Все 25 `imageUri`
  реально лежат в `public/images/`. Найдена и исправлена одна фактическая
  неточность: `funFact` Шанхайской башни утверждал «Второе по высоте здание
  в мире» — с открытием Merdeka 118 в Куала-Лумпуре (678,9 м) в 2024 году
  Шанхайская башня опустилась на 3-е место, а ранговые факты быстро
  устаревают; заменено на не зависящий от рейтинга факт о закрученной на
  120° конструкции с двойным фасадом. `comparisonType: 'max'` корректен,
  остальные высоты и факты проверены и правдоподобны (включая пересечения
  объектов с уровнями 4/6, что ожидаемо и не является багом). Уровень
  пройден фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по всем 25 путям картинок (200), временный
  Node-скрипт (не закоммичен) — 200 симулированных сессий по 10 раундов без
  ошибок. Этим уровнем завершена пачка 2-11 за сегодня;
  `lastReviewedLevelId` теперь `11`.

- QA уровня 10 «Что калорийнее?» (`src/data/level10.ts`). Тот же тип бага,
  что и в уровнях 4-9: из 23 карточек 2 были дубликатами (Куриная грудка,
  Сыр Чеддер — те же значения под другим `id`). Дубликаты убраны, стало 21
  уникальная карточка вместо 23. Остальные 19 `imageUri` реально лежат в
  `public/images/`; для оставшейся без картинки Куриной грудки подходящего
  файла в `public/images/` не нашлось — честный emoji-фолбэк.
  `comparisonType: 'max'` корректен, калорийность на 100 г проверена по всем
  20 позициям и правдоподобна. Уровень пройден фолбэк-способом: `npm run
  build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по всем
  путям картинок (200), временный Node-скрипт (не закоммичен) — 200
  симулированных сессий по 10 раундов без ошибок.

- QA уровня 9 «Что громче?» (`src/data/level9.ts`). Тот же тип бага, что и в
  уровнях 4-8: из 22 карточек 4 были дубликатами одного и того же источника
  звука под другим `id` (Холодильник, Разговор, Сирена скорой — то же
  значение; Рок-концерт — 110/120 дБ под одним названием). Дубликаты убраны,
  стало 18 уникальных карточек вместо 22. Заодно у карточки «Раскат грома»
  без картинки подключено фото `lightning_1786533903072.jpg` (гроза —
  тематически подходит). Остальные 2 `imageUri` (двигатель самолета,
  светошумовая граната) реально лежат в `public/images/`; для 15 карточек
  без картинки подходящего файла не нашлось — честный emoji-фолбэк.
  `comparisonType: 'max'` корректен, значения дБ проверены и правдоподобны
  (включая творческие «хвостовые» карточки — Тунгусский метеорит и
  столкновение чёрных дыр, поданные как образовательное преувеличение
  масштаба, а не выдуманные точные измерения). Уровень пройден
  фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по всем путям картинок (200), временный
  Node-скрипт (не закоммичен) — 200 симулированных сессий по 10 раундов без
  ошибок.

- QA уровня 8 «Кто живет дольше?» (`src/data/level8.ts`). Тот же тип бага,
  что и в уровнях 4-7: из 24 карточек 5 были дубликатами одного и того же
  животного под другим `id` (Мышь/Домашняя мышь — то же значение; Лев —
  15/14 лет, Лошадь — 25/30 лет и Слон (Африканский слон/Слон) под разными
  id, у слона дополнительно дубль с тем же значением 70). Дубликаты убраны
  (для льва и лошади оставлено первое, более подробное описание), стало 19
  уникальных карточек вместо 24. Заодно у карточки «Африканский слон» без
  картинки подключено фото `african_elephant_1786116449273.jpg`
  (переиспользовано из level1/13/15). Остальные 6 `imageUri` реально лежат в
  `public/images/`; для 10 карточек без картинки подходящего файла не
  нашлось — честный emoji-фолбэк. `comparisonType: 'max'` корректен,
  значения продолжительности жизни проверены и правдоподобны. Уровень
  пройден фолбэк-способом: `npm run build && npm run preview -- --port 4321
  --strictPort`, `curl -sSI` по всем путям картинок (200), временный
  Node-скрипт (не закоммичен) — 200 симулированных сессий по 10 раундов без
  ошибок.

- QA уровня 7 «Что дороже?» (`src/data/level7.ts`). Найдены и исправлены 3
  дублирующиеся карточки с расходящимися значениями цены под одним и тем же
  названием (баг того же типа, что в уровнях 4-6): «Часы Rolex» встречались
  дважды ($15,000 и $10,000), «Частный самолет» — дважды ($25 млн и $65 млн),
  «Картина Мона Лиза» — дважды ($860 млн и $870 млн). По каждой оставлена
  одна карточка, стало 21 уникальная вместо 24. «Игровой ноутбук» и «Ноутбук
  Pro» с совпадающим значением $2,500 оставлены — это разные продукты
  (игровой ноутбук/про-ноутбук для разработчиков), совпадение цены случайно
  и правдоподобно, не дубликат. Остальные 8 `imageUri` реально лежат в
  `public/images/`; для 12 карточек без картинки подходящего свободного
  файла в `public/images/` не нашлось (например, для «Частного самолёта» и
  истребителя F-35 в наличии только фото коммерческих пассажирских
  самолётов — подставлять их не по смыслу) — честный emoji-фолбэк.
  `comparisonType: 'max'` корректен. Уровень пройден фолбэк-способом: `npm
  run build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по
  всем путям картинок (200), временный Node-скрипт (не закоммичен) — 200
  симулированных сессий по 10 раундов без ошибок.

- QA уровня 6 «Где глубже?» (`src/data/level6.ts`). Тот же тип бага, что и в
  уровнях 4-5: из 25 карточек 8 были буквальными дубликатами одного и того
  же объекта под другим `id` (Марианская впадина, Обломки Титаника, Рекорд
  дайвинга, Кольская скважина — с тем же значением, причём у Кольской
  скважины дубликат использовал ровно тот же файл картинки; Гранд-Каньон,
  Пещера Крубера, Нырок кашалота, Озеро Байкал — тоже дубликаты). Оставлена
  по одной карточке на объект (для дублей без картинки — та версия, у которой
  она есть), стало 17 уникальных карточек. Заодно у 4 карточек без картинки
  нашлись подходящие неиспользуемые файлы — подключены: Обломки Титаника
  (переиспользован `titanic_1786532003551.jpg` из level4), Нырок кашалота
  (переиспользован `sperm_whale_1786116437079.jpg` из level1), Высота
  Бурдж-Халифы (переиспользован `burj_khalifa_1786531685569.jpg` из level4),
  Погружение синего кита (`blue_whale_size_1786531756325.jpg`, освободился
  после чистки дублей в level4). Остальные карточки без картинки (Байкал,
  Рекорд дайвинга, бассейн, Пещера Крубера, станция метро, морской слон,
  клюворыл Кювье) оставлены с честным emoji-фолбэком — подходящего файла в
  `public/images/` для них нет. `comparisonType: 'max'` корректен, значения
  глубин проверены и правдоподобны. Уровень пройден фолбэк-способом: `npm
  run build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по
  всем путям картинок (200), временный Node-скрипт (не закоммичен) — 200
  симулированных сессий по 10 раундов без ошибок.

- QA уровня 5 «Что горячее?» (`src/data/level5.ts`). Тот же тип бага, что и в
  уровне 4: из 23 карточек 6 пар были буквальными дубликатами одного и того
  же объекта под другим `id` (Кипящая вода, Тело человека, Абсолютный нуль,
  Сухой лёд, Жидкий азот — с тем же значением; Пламя свечи — с расходящимся
  значением 1400 °C и 1000 °C под одним названием). Дубликаты убраны (для
  свечи оставлено значение 1400 °C — с более подробным и точным `funFact` о
  голубой верхушке пламени), стало 17 уникальных карточек. Заодно исправлена
  фактическая ошибка: `funFact` карточки «Магма» утверждал, что «лава
  становится магмой под землёй» — на самом деле наоборот, магма становится
  лавой, извергаясь на поверхность; текст исправлен. Оставшиеся 5 `imageUri`
  реально лежат в `public/images/`; для 9 карточек без картинки подходящего
  свободного файла в `public/images/` не нашлось — честный emoji-фолбэк.
  `comparisonType: 'max'` корректен. Уровень пройден фолбэк-способом: `npm
  run build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по
  всем путям картинок (200), временный Node-скрипт (не закоммичен) — 200
  симулированных сессий по 10 раундов без ошибок.

- QA уровня 4 «Что больше?» (`src/data/level4.ts`). Найден и исправлен
  настоящий баг с данными: в уровне было 25 карточек, но 4 из них —
  буквальные дубликаты одного и того же объекта под другим `id` (Футбольное
  поле, Бурдж-Халифа, Великая Китайская стена — с тем же значением и почти
  тем же `funFact`, только другая картинка; Синий кит — тем же самым, но с
  расходящимся значением длины 33 м и 30 м под одним и тем же названием).
  Дубликаты убраны (оставлена по одной карточке на объект, с более
  распространённым и точнее подтверждённым значением для кита — 30 м), стало
  21 уникальная карточка. Остальные 15 `imageUri` реально лежат в
  `public/images/`; для 6 карточек без картинки (теннисный корт, дорожка для
  боулинга, олимпийский бассейн, вагон поезда, авианосец, подводная лодка)
  подходящего свободного файла в `public/images/` не нашлось — оставлен
  честный emoji-фолбэк. `comparisonType: 'max'` корректен («больше» = большая
  длина/высота). Уровень пройден фолбэк-способом: `npm run build && npm run
  preview -- --port 4321 --strictPort`, `curl -sSI` по всем путям картинок
  (200), временный Node-скрипт (не закоммичен) — 200 симулированных сессий по
  10 раундов без ошибок, дубликатов `funFact` после чистки не осталось.

- QA уровня 3 «Что древнее?» (`src/data/level3.ts`, 24 карточки). Найдено и
  исправлено: у «Первый iPhone» и «Освоение огня» был пустой `imageUri` при
  наличии подходящих неиспользуемых-по-смыслу файлов в `public/images/` —
  подключены `smartphone_1786534090123.jpg` (уже используется в level7, но
  переиспользование практикуется, см. level15) и `candle_flame_1786533824833.jpg`
  соответственно; остальные 5 карточек без картинки (печатный станок, первый
  кинофильм, компас, телескоп, паровой двигатель) оставлены с честным
  emoji-фолбэком — подходящего файла для них в `public/images/` нет. Остальные
  17 `imageUri` реально лежат в `public/images/`. Проверено: `comparisonType:
  'min'` в `levels.ts` — правильно, «древнее» = меньший (более отрицательный)
  год; два безобидных совпадения года (Интернет/Высадка на Луну — оба 1969,
  Кино/Радио — оба 1895) исторически точны, не ошибка данных, `buildPairQueue`
  просто исключает эти 2 пары из 276 возможных; все `funFact` непустые,
  правдоподобные и не повторяются. Уровень пройден фолбэк-способом: `npm run
  build && npm run preview -- --port 4321 --strictPort`, `curl -sSI` по всем
  путям картинок (200 после исправления), временный Node-скрипт (не
  закоммичен) — 200 симулированных сессий по 10 раундов без ошибок.

- QA уровня 2 «Кто быстрее?» (`src/data/speeds.ts`, 20 карточек) — первый
  уровень пачки 5–10 за прогон (`lastReviewedLevelId` был `1`). Проверено:
  все 20 `imageUri` реально лежат в `public/images/` (сверено скриптом по
  файловой системе); значения `value` (макс. скорость, км/ч) разнообразны —
  только 3 небольших совпадения (Грейхаунд/Лось — 72, Койот/Зебра/Верблюд —
  65, Жираф/Дельфин — 60) из 20 карточек, `buildPairQueue` их исключает,
  остальных 185 пар из C(20,2)=190 достаточно на 10 раундов; все `funFact`
  непустые, правдоподобные и не повторяются; `title`/`property`/`unit`/
  `comparisonType: 'max'` в `levels.ts` соответствуют смыслу вопроса «Кто
  быстрее?» (побеждает большая скорость). Багов не найдено, править нечего.
  Уровень пройден без headless-браузера (Playwright не установлен в
  `devDependencies`, установка новых пакетов без интернета недоступна) —
  фолбэк: `npm run build && npm run preview -- --port 4321 --strictPort`,
  затем `curl -sSI` по всем 20 путям картинок уровня (все отдали 200), и
  временный Node-скрипт (не закоммичен), повторяющий логику
  `buildPairQueue`/`generateNewPair` из `GameLevel.tsx`, — 200 симулированных
  сессий по 10 раундов прошли без исключений и с корректным определением
  победителя в каждой паре.

## 2026-09-05

- QA уровня 1 «Кто тяжелее?» (`src/data/level1.ts`, 30 карточек) — первый
  прогон новой ежедневной задачи «разбор одного уровня в день»
  (`lastReviewedLevelId` был `0`). Проверено: все 30 `imageUri` реально лежат
  в `public/images/` (сверено скриптом по файловой системе); значения `value`
  разнообразны — только одна пара с совпадающим весом (Гризли/Зебра, по
  350 кг) при 30 карточках, остальные 434 сравнения из C(30,2)=435 уникальны;
  все `funFact` непустые, правдоподобные и не повторяются; `title`/`property`/
  `unit`/`comparisonType: 'max'` в `levels.ts` соответствуют смыслу вопроса
  «Кто тяжелее?» (побеждает больший вес). Багов не найдено, править нечего.
  Уровень пройден без headless-браузера (в окружении нет установленного
  Playwright/Chromium-пакета в `devDependencies`, а установка новых пакетов
  без интернета недоступна) — использован фолбэк: `npm run build && npm run
  preview -- --port 4321 --strictPort`, затем `curl -sSI` по всем 30 путям
  картинок уровня (все отдали 200), и временный Node-скрипт (не закоммичен),
  повторяющий логику `buildPairQueue`/`generateNewPair` из `GameLevel.tsx` —
  10 симулированных раундов прошли без исключений, верным всегда оказывался
  более тяжёлый участник пары. Preview-сервер остановлен по завершании
  проверки. Новых пунктов в `TASKS.md` не заведено — крупных находок нет.

## 2026-09-04

- Все секции `TASKS.md` были пусты — добавлены 3 новые задачи (кнопка
  «Поделиться результатом», показ прогресса до следующей звезды на
  экране уровня, наполнение уровня 16 контентом), закрыта первая по
  приоритету («Функциональность» выше «Контента»). В победную модалку
  `GameLevel.tsx` добавлена кнопка «Поделиться результатом»: на
  устройствах с Web Share API (`navigator.share`, в основном мобильные)
  открывает системное меню шеринга; без поддержки — копирует тот же текст
  через `navigator.clipboard.writeText` с подтверждением на кнопке
  («Скопировано!» / «Не удалось скопировать»). Добавлен хелпер
  `pluralizeRu` для верного русского склонения числительных (очко/очка/
  очков, звезда/звёзды/звёзд) в тексте шеринга. Новых зависимостей нет —
  использованы существующий `lucide-react` (иконка `Share2`) и
  браузерные Web Share/Clipboard API.

## 2026-09-03

- Закрыта задача «Наполнить уровень 15 контентом» из `TASKS.md`. Создан
  `src/data/level15.ts` — тема «Кто спит дольше?», суточная продолжительность
  сна 20 видов животных в часах (от коалы, 20 ч, до жирафа, 1.9 ч). 11 из 20
  карточек используют уже существующие локальные фото из `public/images/`,
  остальные — emoji-фолбэк. Подключено в `App.tsx`/`getLevelData` (кейс `15`),
  запись уровня 15 в `src/data/levels.ts` обновлена, `isUnlocked: false`
  сохранён — открывается через `UNLOCK_NEXT_LEVEL_STARS` после уровня 14.
  Новых зависимостей нет.

## 2026-09-02

- Закрыта задача «Тактильная отдача (Vibration API) на верный/неверный
  ответ» из `TASKS.md`. `audio.playWrong()` в `src/utils/audio.ts` уже
  вызывал `navigator.vibrate([100, 50, 100])` (паттерн из двух импульсов),
  а `audio.playCorrect()` — нет. Добавлен приватный хелпер
  `SoundManager.vibrate(pattern)` с проверкой `'vibrate' in navigator`
  (не падает в браузерах без поддержки, например iOS Safari);
  `playCorrect()` теперь даёт один короткий импульс (`vibrate(40)`),
  `playWrong()` переведён на тот же хелпер с прежним паттерном. Вибрация
  отключается вместе со звуком через общий флаг `isMuted`. Новых
  зависимостей нет.

## 2026-09-01

- Закрыта задача «Модалка заблокированного уровня всегда предлагает пройти
  Уровень 1» из `TASKS.md`. `LevelLockedModal.tsx` для любого заблокированного
  уровня показывал хардкоженный текст «Пройди Уровень 1, чтобы разблокировать
  этот и следующие уровни!» и кнопку «Играть в Уровень 1», хотя с
  `UNLOCK_NEXT_LEVEL_STARS` уровни открываются строго по цепочке (прохождение
  уровня 1 открывает только уровень 2) — для уровня №12, например, реально
  нужен пройденный уровень 11. Модалка теперь принимает `continueLevel:
  LevelInfo` (вычисляется через уже существующий `getContinueLevelId` в
  `App.tsx`) и показывает/ведёт на реальный следующий шаг игрока вместо
  зашитого уровня 1. Заодно все секции `TASKS.md` были пусты — добавлены 2
  новые задачи в бэклог (тактильная отдача на ответ, наполнение уровня 15).
  Новых зависимостей нет.

## 2026-08-31

- Закрыта задача «Наполнить уровень 14 контентом» из `TASKS.md`. Уровень 14
  (`id: 14` в `src/data/levels.ts`) был заглушкой-секретом. Выбрана тема
  «У кого крылья шире?» — размах крыльев 20 видов птиц в сантиметрах, от
  340 см у странствующего альбатроса до 10 см у рубиновогорлого колибри.
  Создан `src/data/level14.ts` (2 карточки с локальными фото — сапсан,
  африканский страус; остальные 18 — emoji-фолбэк), подключён в
  `App.tsx`/`getLevelData`, запись уровня 14 в `src/data/levels.ts`
  обновлена (title/subtitle/icon/category/property/unit/comparisonType),
  `isUnlocked: false` сохранён — открывается через `UNLOCK_NEXT_LEVEL_STARS`
  при прохождении уровня 13. Новых зависимостей нет.

## 2026-08-30

- Закрыта задача «Кнопка "Продолжить" на главном экране» из `TASKS.md`.
  Кнопка «ИГРАТЬ» в `StartMenu.tsx` всегда открывала уровень 1, даже если
  игрок уже прошёл несколько уровней. Добавлена `getContinueLevelId(levels)`
  в `src/lib/storage.ts` — первый разблокированный уровень без максимума
  звёзд (3), либо последний разблокированный, если всё пройдено на
  максимум. `App.tsx` передаёт её результат и в `onStartGame`, и в новый
  проп `StartMenu.continueLevelId`. Кнопка подписана «ПРОДОЛЖИТЬ Уровень N»,
  как только у игрока есть хотя бы одна звезда где-либо; для нового игрока
  без прогресса подпись остаётся «ИГРАТЬ» (уровень 1). Новых зависимостей
  нет.

## 2026-08-29

- Закрыта задача «Хардкоженная надпись "Доступно сейчас: 11 уровней" на
  главном экране» из `TASKS.md`. Все секции бэклога были пусты — добавлены
  3 новые задачи (эта, кнопка «Продолжить» на главном экране, наполнение
  уровня 14) и выполнена первая по приоритету («Баги»). `StartMenu.tsx`
  показывал статичный текст «11 уровней», не связанный с реальным
  состоянием `levels` — заменено на `unlockedCount = levels.filter(lvl =>
  lvl.isUnlocked).length`, компонент принимает новый проп `levels`
  (передаётся из `App.tsx`). Новых зависимостей нет.

## 2026-08-28

- Закрыта задача «Наполнить уровень 13 контентом» из `TASKS.md`. Уровень 13
  был заглушкой («Секретный уровень», `isUnlocked: false`). Создан
  `src/data/level13.ts` — новая тема «Кто дольше вынашивает потомство?»,
  20 карточек со средним сроком беременности млекопитающих (660 дней у
  африканского слона → 16 дней у сирийского хомяка), 14 из них используют
  уже существующие локальные фото из `public/images/`, остальные — на
  emoji-фолбэке. Подключено в `App.tsx`/`getLevelData` (кейс `13`), запись
  уровня 13 в `src/data/levels.ts` обновлена, `isUnlocked: false` сохранён —
  открывается через `UNLOCK_NEXT_LEVEL_STARS` при прохождении уровня 12.
  Новых зависимостей нет.

## 2026-08-27

- Закрыта задача «Серия (streak) не даёт множитель к очкам» из `TASKS.md`.
  `GameLevel.tsx`: очки за верный ответ теперь зависят от длины серии —
  каждые 3 верных ответа подряд поднимают множитель на 1 (x1 → x2 → x3 → ...,
  `getStreakMultiplier`/`STREAK_BONUS_STEP`). Всплывающий попап и итоговый
  счёт учитывают реальное начисление, добавлен золотой бейдж `×N` рядом со
  счётчиком серии. `STAR_SCORE_THRESHOLDS` пересчитаны с `4`/`8` на `10`/`20`
  под новый потолок очков идеального прохождения (25 вместо 10). Новых
  зависимостей нет.

## 2026-08-26

- Все секции `TASKS.md` были пусты — добавлены 3 новые задачи (хардкоженная
  надпись «Уровень 1 открыт!», множитель очков за серию (streak), наполнение
  уровня 13) и выполнена первая по приоритету («Баги»). В `LevelSelect.tsx`
  подпись под шапкой экрана выбора уровня была статичным текстом «Всего 30
  уровней. Уровень 1 открыт!», хотя по умолчанию уже разблокированы уровни
  1–11 и это число растёт дальше по ходу игры. Заменено на подсчёт открытых
  уровней из актуального пропа `levels` — «Всего {levels.length} уровней.
  Открыто: {unlockedCount}.». Новых зависимостей нет.

## 2026-08-25

- Закрыта задача «Наполнить уровень 12 («Кто ядовитее?»)» из `TASKS.md`.
  Создан `src/data/level12.ts` — 22 карточки ядовитых животных (змеи, пауки,
  скорпион, кубомедуза, конус, лягушка-древолаз, рыба-камень, утконос и
  другие), подключены в `App.tsx`/`getLevelData` и `src/data/levels.ts`
  (title «Кто ядовитее?», property «Индекс токсичности», unit «баллы»,
  comparisonType `max`, `isUnlocked: false` — открывается через уже
  существующий `UNLOCK_NEXT_LEVEL_STARS` после уровня 11). Значение `value`
  — условный индекс токсичности (0–100), а не точный LD50: опубликованные
  LD50 одного вида сильно расходятся между источниками (путь введения,
  вид подопытных животных, методика лаборатории), и без доступа в интернет
  для сверки заявлять точные мг/кг было бы риском выдумать цифру — порядок
  индекса вместо этого фактчекнут по общеизвестному научному консенсусу об
  опасности каждого вида. Ни у одной карточки нет `imageUri` — подходящих
  локальных фото ядовитых видов в `public/images/` нет, а хотлинки запрещены
  правилами проекта; карточки используют уже существующий emoji-фолбэк.
  Поле `GameItem.imageUri` сделано опциональным в `src/types/index.ts` (та
  же логика уже применялась 2026-08-10). Новых зависимостей нет.

## 2026-08-24

- Добавлен визуальный индикатор прогресса раундов в `GameLevel.tsx`: новый
  самодостаточный компонент `src/components/ui/RoundProgress.tsx` — полоса
  из 10 сегментов (по числу `TOTAL_ROUNDS`), пройденные и текущий раунд
  подсвечены неоновым градиентом cyan→purple (текущий дополнительно мягко
  пульсирует), оставшиеся — приглушённое стекло в стиле проекта. Размещён
  под шапкой уровня, дублирует текстовый счётчик «Задание N/M» визуально —
  игрок видит, сколько раундов осталось, не считая в уме. Добавлены
  `role="progressbar"` и `aria-valuenow`/`aria-valuemin`/`aria-valuemax`
  для доступности. Новых зависимостей нет.

## 2026-08-23

- Все секции `TASKS.md` были пусты — добавлены 3 новые задачи (удаление
  неиспользуемых ассетов, визуальный индикатор прогресса раундов, наполнение
  уровня 12 «Кто ядовитее?») и выполнена первая. Удалены полностью
  неиспользуемые папки `public/animals/` (9 файлов, ~1.1 МБ — остаток от
  версии игры до перехода на `src/data/level1.ts` с путями `/images/*`) и
  `public/level11/` (9 файлов, ~4.5 МБ, часть весила 280–980 КБ — заметно
  выше принятого в проекте лимита ≤150 КБ; актуальный `level11.ts` ссылается
  на переименованные файлы в `public/images/`). Проверено `grep` по `src/` и
  `public/`: ни один путь в коде на эти файлы не ссылался. На GitHub Pages
  это ~5.6 МБ меньше при первом заходе — ощутимо для mobile-first игры.
  Правок кода не потребовалось, новых зависимостей нет.

## 2026-08-22

- Закрыта задача «Расширить уровень 1 с 20 до 30 животных с проверенными
  весами и фактами» из `TASKS.md`. `LEVEL1_DATA` уже содержал 31 запись, но 3
  из 11 добавленных дублировали вид из исходных 20 под другим `id` и с другим
  весом (`tiger` 220 кг против `amur-tiger` 250 кг, `grizzly` 300 кг против
  `grizzly-bear` 350 кг — один вид весил по-разному в зависимости от того,
  какая карточка попадалась), а факты у всех 11 были заглушками в одно
  короткое предложение. Переписан `LEVEL1_ADDITIONAL` в `src/data/level1.ts`:
  убраны 3 дублирующих вида, добавлены гепард и гренландская акула (готовые
  локальные фото уже лежали в `public/images/`, новых ассетов не
  потребовалось) — итог ровно 10 уникальных животных, `LEVEL1_DATA.length`
  теперь ровно 30. Веса перепроверены по реальным диапазонам вида, факты
  переписаны в стиле остальной игры (весовой диапазон + доп. факт,
  1–2 предложения). Новых зависимостей нет.

## 2026-08-21

- Закрыта задача «Уровень 2 «Кто быстрее?»» из `TASKS.md`. Данные уровня 2 уже
  существовали (`src/data/level2.ts`), но не соответствовали формулировке: 25
  записей вместо 20 животных — среди них 5 неживотных (`f1-car`, `passenger-jet`,
  `usain-bolt`, `sound`, `bugatti`), а игровой экран уже был generic-компонентом
  (`GameLevel.tsx`, переиспользуется всеми 11 уровнями через `App.tsx`/
  `getLevelData`), так что этой части задача была выполнена побочно вне процесса.
  Создан `src/data/speeds.ts` — ровно 20 животных с максимальной скоростью в
  км/ч (сапсан 390 км/ч — гепард 120 — … — улитка 0.048 км/ч), отсортированные
  по убыванию скорости как в `src/data/level1.ts`; значения и факты перенесены
  без изменений (уже были правдоподобны), только неживотные записи убраны и
  порядок выровнен. Старый `src/data/level2.ts` удалён, `App.tsx` переключён на
  `SPEEDS_DATA` из нового файла. Отдельный игровой компонент для уровня 2 не
  создавался — `GameLevel` уже общий. Новых зависимостей нет, ассеты не
  менялись (все 20 картинок уже лежали в `public/images/`).

## 2026-08-20

- Закрыта задача «Счётчики в меню» из `TASKS.md`. `starsEarned` в `App.tsx`
  считался через `Math.max(prev.starsEarned, stars)` (максимум звёзд за одно
  прохождение, а не сумма лучших по уровням), `totalScore` суммировал очки
  каждого завершения уровня (рос от перепрохождений, а не отражал сумму
  рекордов) — оба поля вёл независимый аккумулятор, не связанный с per-level
  `stars`/`highScore` в `levels`. Добавлена `computeStats(levels)` в
  `src/lib/storage.ts` — единый источник правды, который пересчитывает
  `stats` из `levels`: `starsEarned` — сумма `stars`, `totalScore` — сумма
  `highScore` по всем уровням. `App.tsx` больше не ведёт `stats` отдельным
  `setState`, значение выводится через `useMemo` от `levels`. Побочно тем же
  способом исправлен `levelsCompleted` (раньше не рос выше `1`). Формат
  `localStorage` не менялся. Новых зависимостей нет.

## 2026-08-19

- Закрыта задача «Картинки животных не грузятся в проде (часть 2 из 2)» из
  `TASKS.md`. Фото для всех 31 животного в `src/data/level1.ts` уже были на
  месте (подтянулось побочно более ранним коммитом «подключить изображения к
  187 карточкам всех уровней»), но 16 файлов в `public/images/` превышали
  лимит задачи ≤150 КБ (весили до ~250 КБ). Пережаты через `sharp`/`mozjpeg`
  (resize внутрь 800×800 без увеличения, понижение JPEG quality с 82 при
  необходимости) — теперь все 100–153 КБ, размеры и формат не изменились.
  `sharp` использован только как одноразовый инструмент сжатия вне проекта
  (установлен в scratchpad, не добавлен в `package.json`) — новых
  зависимостей в репозитории нет.

## 2026-08-18

- Реализовано «Доступность карточек» (см. `TASKS.md`): карточки животных в
  `src/components/GameLevel.tsx` были кликабельным `<div>` без `role`, `tabIndex`
  и обработки клавиатуры. Добавлены `role="button"`, `tabIndex`, обработчик
  Enter/Space и `aria-label` с названием животного, плюс видимый
  `focus-visible`-контур; тот же паттерн применён к карточкам уровней в
  `src/components/LevelSelect.tsx`. Иконочным кнопкам без подписи (назад/выход,
  закрытие модалки, переключатель звука) добавлен `aria-label` в
  `GameLevel.tsx`, `LevelSelect.tsx`, `LevelLockedModal.tsx` и `StartMenu.tsx`;
  кнопки звука дополнительно получили `aria-label`/`aria-pressed`, зависящие от
  состояния `isMuted`, вместо статичной подписи «Звук».
- Задача «Картинки животных не грузятся в проде (часть 2 из 2)» снова
  пропущена в этом прогоне — окружение без доступа в интернет, платная
  AI-генерация требует подтверждения пользователя, которого нет в
  автоматическом запуске.

## 2026-08-17

- Реализовано «Единый источник порогов звёзд» (см. `TASKS.md`): пороги `4`/`8`
  очков были захардкожены дважды в `src/components/GameLevel.tsx` — в расчёте
  `stars` при завершении уровня и отдельно в разметке модалки победы (заливка
  второй и третьей звезды). Добавлены `STAR_SCORE_THRESHOLDS` и
  `getStarsForScore(score)` на уровне модуля, обе точки теперь используют их.
- Задача «Картинки животных не грузятся в проде (часть 2 из 2)» снова
  пропущена в этом прогоне — окружение без доступа в интернет, платная
  AI-генерация требует подтверждения пользователя, которого нет в
  автоматическом запуске.

## 2026-08-16

- Реализовано «Разблокировка уровня 2» (см. `TASKS.md`): `isUnlocked` нигде не
  менялся после завершения уровня. В `src/App.tsx` добавлена константа
  `UNLOCK_NEXT_LEVEL_STARS = 2` — при завершении уровня с ≥2 звёздами следующий
  по `id` уровень теперь открывается автоматически. Решение общее для всех
  переходов между уровнями, не только 1 → 2.
- Задача «Дублирующийся `id: 'gorilla'`» отмечена выполненной без правок кода —
  на момент проверки `LEVEL1_ADDITIONAL` в `src/data/level1.ts` больше не
  содержит гориллу, дублирующихся id во всём файле нет.
- Задача «Картинки животных не грузятся в проде (часть 2 из 2)» снова пропущена
  в этом прогоне — окружение без доступа в интернет, платная AI-генерация
  требует подтверждения пользователя, которого нет в автоматическом запуске.

## 2026-08-15

- Реализовано «Сохранение прогресса» (см. `TASKS.md`): `stats` и `levels` в
  `App.tsx` жили только в `useState` и обнулялись при перезагрузке страницы.
  Добавлен `src/lib/storage.ts` с версионированным ключом localStorage
  (`guesswhat:progress:v1`) — `loadProgress`/`saveProgress` с валидацией
  формы сохранённых данных (откат на дефолты при битом/чужом формате или
  недоступном localStorage) и `mergeLevelsWithProgress`, которая накладывает
  сохранённые звёзды/рекорды/разблокировки на актуальный список уровней по
  `id`. `App.tsx` теперь инициализирует `levels`/`stats` из сохранения и
  сохраняет их при каждом изменении.
- Задача «Опечатка в UI (Скорстные чемпионы)» отмечена выполненной без
  правок кода — на момент проверки строка в `src/data/levels.ts` уже
  корректна («Скоростные чемпионы»), опечатка нигде не найдена.
- В `TASKS.md` заведён новый баг: дублирующийся `id: 'gorilla'` в
  `src/data/level1.ts` (одна и та же горилла с разным весом присутствует и
  в `LEVEL1_DATA`, и в `LEVEL1_ADDITIONAL`) — найден по пути при работе над
  сохранением прогресса, не исправлялся в этом прогоне.
- Задача «Картинки животных, часть 2 из 2» снова пропущена — по-прежнему
  нет доступа в интернет и подтверждения пользователя на платную
  AI-генерацию; заодно в `TASKS.md` обновлены устаревшие пути файлов в
  описании этой задачи (`src/data/animals.ts` → `src/data/level1.ts`,
  `public/animals/` → `public/images/`).

## 2026-08-14

- Исправлен баг «Повторы пар» (см. `TASKS.md`): `generateNewPair` в
  `src/components/GameLevel.tsx` тасовал карточки через
  `sort(() => 0.5 - Math.random())` — смещённая перетасовка без защиты от
  повторов, из-за чего одна и та же пара животных могла выпасть несколько
  раз за сессию. Добавлена честная `fisherYatesShuffle` (`src/lib/utils.ts`)
  и `buildPairQueue`, которая на старте сессии/при рестарте строит
  перетасованную очередь всех уникальных пар (без пар с одинаковым
  значением) и раздаёт их по одной за раунд, пока не закончатся комбинации.
  Задача «Картинки животных, часть 2 из 2» пропущена в этом прогоне —
  требует доступа в интернет или платной AI-генерации с подтверждением
  пользователя, которых нет в автоматическом запуске.

## 2026-08-13

- Исправлена утечка таймера (см. `TASKS.md`): в `src/components/GameLevel.tsx`
  (актуальный путь — компонент из задачи назывался `Level1Game.tsx` до
  переработки на Tailwind + Framer Motion) `setTimeout(..., 1400)` в
  `handleCardClick`, который переводит игру в следующий раунд или завершает
  уровень, не сохранялся и не очищался. Выход в меню во время 1.4-секундной
  анимации ответа оставлял колбэк висеть, и он дёргал `setRound` /
  `generateNewPair` или `onLevelComplete` уже после размонтирования
  компонента. Теперь id таймера хранится в `useRef`, предыдущий таймер
  очищается перед постановкой нового, а `useEffect` с cleanup чистит его
  при размонтировании `GameLevel`.

## 2026-08-12

- Исправлен баг «Ничья по весу» (см. `TASKS.md`): у `grizzly-bear` и `zebra`
  в `src/data/level1.ts` одинаковый `value: 350` кг, из-за чего
  `generateNewPair` в `src/components/GameLevel.tsx` всегда засчитывал
  верным `itemA`, а правильный ответ игрока по `itemB` штрафовался на −2.
  Теперь при совпадении значений в случайной паре функция ищет в
  перетасованном списке следующего кандидата на `itemB` с другим значением,
  прежде чем определять правильный ответ — исправление общее для всех
  уровней. Задача про фото 11 животных (часть 2 из 2) вновь пропущена: она
  требует либо доступа в интернет, либо платной AI-генерации с согласия
  пользователя — ни то, ни другое недоступно в автоматическом прогоне.

## 2026-08-10

- Исправлены картинки животных (часть 1 из 2, см. `TASKS.md`): 9 уже скачанных
  JPEG в `public/animals/` сжаты с ~0.6–1 МБ / 1024×1024 @300 dpi до
  ≤150 КБ / 800×800 @72 dpi; `imageUri` для животных без реального локального
  файла (морж, гребнистый крокодил, африканский буйвол, лось — были битые
  локальные пути; гризли, зебра, амурский тигр, африканский лев, горилла,
  комодский варан, императорский пингвин — были заблокированные хотлинки на
  `images.unsplash.com`) убран из данных; поле `Animal.imageUri` стало
  опциональным (`src/types/index.ts`), карточка теперь сразу показывает
  emoji-фолбэк, если фото нет, вместо неудачной попытки загрузки. Добавление
  реальных фото для оставшихся 11 животных вынесено в отдельную подзадачу.
