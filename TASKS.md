# Бэклог GuessWhat

Ежедневный агент берёт **первую невыполненную** задачу сверху, делает её целиком,
отмечает `[x]` и коммитит. Одна задача — один коммит. Не брать больше одной за запуск.

Приоритет: сначала блок «Баги», затем «Функциональность», затем «Контент».
Когда все секции пусты — добавить в конец 3 новые осмысленные задачи по улучшению игры
(новый уровень, новая категория данных, UX-полировка) и выполнить первую из них.

---

## 🐞 Баги

## 🎮 Функциональность

- [ ] **Визуальный индикатор прогресса раундов.** Сейчас прогресс уровня в
      `GameLevel.tsx` виден только текстом «Задание {round}/{TOTAL_ROUNDS}» в шапке.
      Добавить компактную полосу/точки прогресса (10 сегментов, заполняются по мере
      прохождения раундов, в неоново-градиентной стилистике проекта) — частый
      UX-паттерн мобильных квизов, помогает игроку оценить, сколько раундов осталось,
      не считая в уме.

## 📚 Контент

- [ ] **Наполнить уровень 12 («Кто ядовитее?» — сравнение опасности ядов животных).**
      Уровни 12–30 сейчас — заглушки («Секретный уровень», `isUnlocked: false`,
      `category: 'Секрет'`). Создать `src/data/level12.ts` по образцу существующих
      уровней (20+ карточек, реальные и правдоподобные значения — например, LD50 или
      условный индекс токсичности, с фактчекнутыми диапазонами), подключить в
      `App.tsx`/`getLevelData`, обновить запись уровня 12 в `src/data/levels.ts`
      (title/subtitle/icon/category/property/unit/comparisonType) — без разблокировки
      по умолчанию, открывается игроком через существующий механизм
      `UNLOCK_NEXT_LEVEL_STARS`.

---

## ✅ Сделано

<!-- агент переносит выполненные задачи сюда с датой -->

- [x] **Неиспользуемые ассеты раздуваются в проде.** *(2026-08-23)* Все секции
      `TASKS.md` были пусты — добавлены 3 новые задачи (эта, индикатор прогресса
      раундов, наполнение уровня 12) и выполнена первая. Проверено `grep -rn` по
      всему `src/` и по самому `public/` (index.html, манифесты): ни `public/animals/`
      (9 файлов, ~1.1 МБ — остаток от версии игры до перехода на `src/data/level1.ts`
      с путями `/images/*`), ни `public/level11/` (9 файлов, ~4.5 МБ, часть весила
      280–980 КБ — многократно выше принятого в проекте лимита ≤150 КБ; актуальный
      `level11.ts` уже ссылается на переименованные файлы с суффиксами-таймстампами
      в `public/images/`) нигде не упоминаются. Обе папки удалены целиком
      (`git rm -r`) — на GitHub Pages это ~5.6 МБ мёртвого веса меньше при первом
      заходе, ощутимо для mobile-first игры. Код не менялся, так как ни один путь
      на удалённые файлы и так не резолвился.

- [x] **Расширить уровень 1** с 20 до 30 животных с проверенными весами и
      фактами. *(2026-08-22)* На момент проверки `LEVEL1_DATA` уже содержал
      31 запись (20 исходных + 11 в `LEVEL1_ADDITIONAL`), но формально
      «расширение до 30» не выполнялось — вдобавок 3 из 11 добавленных
      записей дублировали вид из исходных 20 под другим `id` и с другим
      весом (`lion` 190 кг vs `african-lion` 190 кг — совпадало случайно,
      но `tiger` 220 кг vs `amur-tiger` 250 кг и `grizzly` 300 кг vs
      `grizzly-bear` 350 кг — расходились: один вид животного весил
      по-разному в зависимости от того, какая карточка попадалась), а
      факты у всех 11 были заглушками в одно короткое предложение
      («Лучший друг человека.»), не в стиле остальной игры. Переписан
      `LEVEL1_ADDITIONAL` в `src/data/level1.ts`: убраны 3 дублирующих вида,
      добавлены гепард и гренландская акула (готовые локальные изображения
      уже были в `public/images/`, новых ассетов не потребовалось) — итог
      ровно 10 уникальных животных, не пересекающихся по виду с исходными
      20 (`LEVEL1_DATA.length` теперь ровно 30). Веса перепроверены по
      реальным диапазонам вида и приведены к правдоподобным серединным
      значениям (лошадь 550 кг, корова 650 кг, верблюд 480 кг и т.д.),
      факты переписаны в стиле остальной игры (весовой диапазон вида +
      дополнительный факт, 1–2 предложения). Дубликат `value` между
      `grizzly-bear`/`zebra` (350 кг у обоих) в исходных 20 не трогался —
      это осознанный кейс, под который уже есть отдельная логика ничьей в
      `GameLevel.tsx` (см. «Ничья по весу», 2026-08-12).

- [x] **Уровень 2 «Кто быстрее?»** *(2026-08-21)* Данные уровня (`src/data/level2.ts`)
      и generic игровой экран уже существовали, сделанные вне этого процесса, но
      не соответствовали формулировке: 25 записей вместо 20 животных (5 —
      неживотные: болид F1, Boeing 747, Усэйн Болт, скорость звука, Bugatti).
      Создан `src/data/speeds.ts` — ровно 20 животных, отсортированы по убыванию
      максимальной скорости (сапсан 390 км/ч → улитка 0.048 км/ч), значения и
      факты сохранены как были (уже правдоподобны). Старый `level2.ts` удалён,
      `App.tsx` переключён на `SPEEDS_DATA`. `GameLevel.tsx` — уже общий
      компонент для всех уровней, отдельный экран для уровня 2 не создавался.

- [x] **Счётчики в меню.** *(2026-08-20)* Подтверждено: `starsEarned` в `App.tsx`
      обновлялся через `Math.max(prev.starsEarned, stars)` — держал максимум звёзд
      за одно прохождение *любого* уровня, а не подписанное в меню «общее число
      звёзд» (сумму лучших результатов по всем уровням). `totalScore` суммировал
      `gainedScore` при каждом завершении уровня, включая повторные прохождения, —
      рос от простого перефарма, а не отражал сумму личных рекордов. Оба поля были
      отдельным аккумулятором, который вёлся независимо от per-level
      `stars`/`highScore` в `levels` и мог с ними разойтись. Исправлено переносом
      на единый источник правды: добавлена `computeStats(levels)` в
      `src/lib/storage.ts`, которая всегда пересчитывает `stats` из актуального
      `levels` — `starsEarned` как сумму `stars`, `totalScore` как сумму
      `highScore` по всем уровням. `handleLevelComplete` в `App.tsx` больше не
      ведёт `stats` отдельным `setState`, счётчики выводятся через `useMemo` от
      `levels`, поэтому по построению не могут разойтись с тем, что подписано в
      UI. Побочно тем же способом исправлен `levelsCompleted` (раньше застревал
      на `1` навсегда через `Math.max(prev.levelsCompleted, 1)`, теперь — число
      уровней хотя бы с одной звездой); поле нигде не отображается в UI, но
      сохраняется в прогрессе, так что расхождение с реальным состоянием было
      того же рода багом. Формат `localStorage` (`guesswhat:progress:v1`) не
      менялся — `stats` по-прежнему сохраняется, просто теперь как производное
      значение, а не независимо ведённое состояние.

- [x] **Доступность карточек.** *(2026-08-18)* Карточки животных в `GameLevel.tsx`
      (`renderItemCard`) были кликабельным `motion.div` без `role`, `tabIndex` и
      обработки клавиатуры — недоступны с клавиатуры и скринридерами. Добавлены
      `role="button"`, `tabIndex` (0 пока раунд не раскрыт, -1 после — чтобы нельзя
      было повторно ответить по уже показанной паре), `onKeyDown` на Enter/Space,
      `aria-label` с названием животного и `aria-disabled` на раскрытом раунде, плюс
      видимый `focus-visible`-контур. Заодно приведены к тому же паттерну карточки
      уровней в `LevelSelect.tsx` (тот же кликабельный `<div>`-паттерн через
      `GlassCard`). Иконочным кнопкам без подписи добавлен `aria-label`: кнопки
      «назад»/«выход» в `GameLevel.tsx` и `LevelSelect.tsx`, кнопка закрытия (`X`) в
      `LevelLockedModal.tsx`, кнопка звука в `GameLevel.tsx` и `StartMenu.tsx` —
      последние две дополнительно получили `aria-label`, зависящий от состояния
      (`isMuted`), и `aria-pressed`, а не статичный текст «Звук».

- [x] **Картинки животных не грузятся в проде (часть 2 из 2): нет фото ещё для 11
      животных.** *(2026-08-19)* Проверено скриптом по `src/data/level1.ts`: все 31
      записи (`LEVEL1_DATA` + `LEVEL1_ADDITIONAL`, включая гризли, зебру, амурского
      тигра, африканского льва, гориллу, комодского варана, императорского пингвина)
      уже указывают на `imageUri: '/images/<id>.jpg'`, и все 31 файл реально лежат в
      `public/images/` — отсутствующих фото нет. Это подтянулось побочно коммитом
      «подключить изображения к 187 карточкам всех уровней» вне этого процесса (как и
      с опечаткой «Скорстные чемпионы» 2026-08-15 и дублем `id: 'gorilla'`
      2026-08-16), но не соблюдало вторую часть требования задачи — лимит ≤150 КБ:
      16 из 31 файла весили 169–250 КБ (до ~250 КБ вместо заявленных ≤150 КБ, при
      этом уже 800px). Пережаты через `sharp`/`mozjpeg` (resize «внутрь» 800×800 без
      увеличения + `jpeg({ quality, mozjpeg: true })` с понижением quality с 82 при
      необходимости) — все 16 файлов теперь 100–153 КБ, размеры и формат не
      пострадали (проверено `sharp().metadata()`), правок кода не потребовалось.
      Библиотека `sharp` использована только как одноразовый инструмент сжатия вне
      репозитория (в scratchpad, установлена не в проект) — в `package.json`
      постоянной зависимостью не добавлена.

- [x] **Единый источник порогов звёзд.** *(2026-08-17)* Путь из формулировки задачи
      устарел (`Level1Game.tsx` → актуальный `src/components/GameLevel.tsx`), сама
      проблема была на месте: пороги `4`/`8` были захардкожены дважды — в расчёте
      `stars` при завершении уровня (`if (finalScore >= 8) ... else if (finalScore
      >= 4) ...`) и отдельно в разметке модалки победы (заливка второй и третьей
      звезды по `score >= 4`/`score >= 8`). Правка одного места без второго легко
      могла развести показанные звёзды с реально начисленными. Добавлена константа
      `STAR_SCORE_THRESHOLDS = { second: 4, third: 8 }` и функция
      `getStarsForScore(score)` на уровне модуля; обе точки — расчёт `stars` в
      `handleCardClick` и заливка/цвет/свечение звёзд 2 и 3 в JSX модалки — теперь
      ссылаются на общий источник.

- [x] **Разблокировка уровня 2.** *(2026-08-16)* `isUnlocked` действительно нигде не
      менялся после старта игры — `handleLevelComplete` в `src/App.tsx` обновлял
      только `stars`/`highScore` пройденного уровня. Добавлена константа
      `UNLOCK_NEXT_LEVEL_STARS = 2` и логика: при завершении уровня с ≥2 звёздами
      следующий по `id` уровень получает `isUnlocked: true`. Решение общее для
      любого перехода, не только 1 → 2. *Побочно замечено:* в `src/data/levels.ts`
      уровни 1–11 сейчас и так стартуют с `isUnlocked: true` (это изменение внесено
      коммитом «UI Overhaul», в обход `TASKS.md`/`CHANGELOG.md`, и расходится с
      описанием проекта «уровень 1 активен, уровни 2–30 заблокированы» в
      `.github/daily-task.md`) — из-за этого добавленный механизм пока заметен
      только на переходе 11 → 12 (открывает первый из уровней-заглушек 12–30).
      Возврат уровней 2–11 к заблокированному стартовому состоянию не входил в
      формулировку этой задачи и не делался, чтобы не переписывать данные, которых
      задача не касалась.

- [x] **Дублирующийся `id: 'gorilla'`.** *(2026-08-16)* Проверено скриптом по
      `src/data/level1.ts`: `LEVEL1_ADDITIONAL` больше не содержит `gorilla` (там
      теперь `grizzly`/`lion`/`tiger` — свои id, отличные от `grizzly-bear`,
      `african-lion`, `amur-tiger` в `LEVEL1_DATA`), дублирующихся id по всему
      файлу нет (проверено скриптом: 31 запись — 31 уникальный id). Похоже,
      исправлено побочно в одном из более поздних коммитов вне этого процесса —
      как и с опечаткой «Скорстные чемпионы» 2026-08-15. Правок не потребовалось,
      задача отмечена выполненной по факту.

- [x] **Сохранение прогресса.** *(2026-08-15)* `stats` и `levels` в `App.tsx` жили
      только в `useState`, перезагрузка страницы обнуляла звёзды, рекорды и
      разблокировки. Добавлен `src/lib/storage.ts` с версионированным ключом
      localStorage (`guesswhat:progress:v1`, поле `version` в самой записи):
      `loadProgress`/`saveProgress` читают и пишут прогресс с рантайм-валидацией
      формы данных (битый JSON, чужой формат или недоступный localStorage —
      `null`, откат на дефолты, игра не падает), а `mergeLevelsWithProgress`
      накладывает сохранённые `stars`/`highScore`/`isUnlocked` на актуальный
      `INITIAL_LEVELS` по `id` (переживает добавление новых уровней между
      версиями игры; `isUnlocked` берётся как «или» с исходным значением, чтобы
      уровень, открытый по умолчанию в новой версии, не оказался вдруг заблокирован
      старой записью). В `App.tsx` `levels` и `stats` инициализируются из
      `loadProgress()` через ленивые инициализаторы `useState`, а `useEffect`
      сохраняет их при каждом изменении — прогресс переживает не только выход
      в меню, но и случайное закрытие вкладки посреди уровня.

- [x] **Опечатка в UI («Скорстные чемпионы»).** *(2026-08-15)* На момент проверки
      строка в `src/data/levels.ts:20` уже гласит «Скоростные чемпионы» — опечатка
      отсутствует нигде в кодовой базе (проверено `grep -rn` по `src`). Похоже,
      исправлена побочно в одном из более поздних коммитов (`UI Overhaul` /
      `complete UI redesign`), которые не велись через этот процесс и не
      отражены в `TASKS.md`/`CHANGELOG.md`. Правок не потребовалось, задача
      отмечена выполненной по факту.

- [x] **Повторы пар.** *(2026-08-14)* Путь из формулировки задачи устарел
      (`Level1Game.tsx:33` → актуальный `src/components/GameLevel.tsx`,
      `generateNewPair`), сама проблема была на месте: `sort(() => 0.5 -
      Math.random())` — известная неравномерная перетасовка (не
      Фишер-Йетс), и пары не отслеживались, поэтому одна и та же пара могла
      выпасть два раунда подряд или несколько раз за уровень. Добавлена
      `fisherYatesShuffle` в `src/lib/utils.ts` и новая функция
      `buildPairQueue` в `GameLevel.tsx`, которая на старте сессии строит
      перетасованную очередь всех уникальных пар карточек (исключая пары с
      одинаковым значением — по ним нечего сравнивать) и раздаёт их по
      одной за раунд без повторов, пока не закончатся комбинации (для
      уровня из 20 карточек это 190 уникальных пар — на 10 раундов сессии
      повтора не будет ни разу). Очередь пересоздаётся при рестарте уровня
      (`handleRestart`) — новая сессия получает новую перетасовку.

- [x] **Утечка таймера.** *(2026-08-13)* Путь из формулировки задачи устарел
      (`src/components/Level1Game.tsx` → актуальный `src/components/GameLevel.tsx`
      после переработки на Tailwind + Framer Motion), сама проблема была на
      месте: `setTimeout(..., 1400)` в `handleCardClick` (переход к следующему
      раунду / завершение уровня) не сохранялся и не очищался, поэтому выход
      в меню кнопкой «Меню»/`onBack` во время 1.4-секундной анимации ответа
      оставлял колбэк висеть и он дёргал `setRound`/`generateNewPair` или
      `onLevelComplete` уже после размонтирования `GameLevel`. Исправлено:
      id таймера теперь хранится в `useRef` (`nextRoundTimeoutRef`),
      предыдущий таймер очищается перед постановкой нового, и добавлен
      `useEffect` с cleanup, который чистит его при размонтировании
      компонента.

- [x] **Ничья по весу.** *(2026-08-12)* Проект с прошлого прогона был полностью
      переработан (Tailwind CSS + Framer Motion), поэтому пути из формулировки
      задачи устарели: `src/data/animals.ts` → `src/data/level1.ts`,
      `src/components/Level1Game.tsx` → `src/components/GameLevel.tsx`. Сама
      проблема осталась: у `grizzly-bear` и `zebra` в `src/data/level1.ts`
      одинаковый `value: 350`, а `generateNewPair` в `GameLevel.tsx` считал
      верным всегда `itemA` при равенстве значений (`isAMax = itemA.value >=
      itemB.value`), из-за чего верный ответ по `itemB` штрафовался на −2.
      Исправлено: `generateNewPair` теперь при равных `value` у первой
      случайной пары ищет в перетасованном списке следующего кандидата на
      `itemB` с другим значением, прежде чем определять корректный ответ.
      Решение общее для всех уровней, не только для этой пары.

- [x] **Картинки животных не грузятся в проде (часть 1 из 2): локальные фото и
      битые хотлинки.** *(2026-08-10)* Проверено на
      https://supremegoogle.github.io/GuessWhat/ — `<img>` на `images.unsplash.com`
      отдаёт `error` (хотлинк заблокирован в проде и даже недоступен из этого CI —
      прямая проверка `curl` на `images.unsplash.com` и `upload.wikimedia.org`
      вернула `403`), поэтому карточки показывали emoji-фолбэк вместо фото. Сделано:
      9 готовых JPEG в `public/animals/` сжаты с ~0.6–1 МБ / 1024×1024 до
      ≤150 КБ / 800×800 / 72 dpi (mozjpeg, без перекодирования размеров сверху);
      `imageUri` в `src/data/animals.ts` для этих 9 животных уже указывал на
      локальные пути и оставлен как есть; для животных без реального локального
      файла (морж, гребнистый крокодил, африканский буйвол, лось — были битые
      локальные пути; гризли, зебра, амурский тигр, африканский лев, горилла,
      комодский варан, императорский пингвин — были хотлинки на Unsplash) поле
      `imageUri` убрано из данных и стало опциональным в `src/types/index.ts`
      (`imageUri?: string`), `Level1Game.tsx` теперь не пытается грузить `<img>`,
      если `imageUri` нет, — сразу честный emoji-фолбэк без лишнего сетевого
      запроса и ошибки в консоли. Источники реальных фото для оставшихся 11
      животных — отдельная подзадача (см. «Баги» выше), т.к. требует либо
      доступа в интернет, либо платной AI-генерации с согласия пользователя.
